# אכיפת התהליך · שלב 6 — החיווט

> **‏✅ בוצעה — סומן 10.8.26.** כל 5 המשימות אומתו קיימות בקוד 10.8.26: שערי החוקים ב-`check-meta` · `session-rules.mjs` קורא מהמסד · `/enforce` מכיר את המסד · המשימה המתוזמנת `matconetesh-watchman-30min` במצב Ready · `test-hooks-wiring.mjs`
>
> **למה הסימון הזה קיים:** ‏`check-plan-complete` דיווח על 11 מ-34 התוכניות כפגומות. הבדיקה הראשונה
> אי-פעם של השער מול הקורפוס הקיים — הוא נבנה ב-L27 והורץ רק על מה שנכתב אחריו. **התוכניות אינן
> קטועות; הן היסטוריות, וכתובות בסגנון שקדם לדרישת בלוקי-הקוד.** רשום כ-R-119, הוכרע ע"י הבעלים
> ‏10.8.26: קו-בסיס מוצהר + סימון ביצוע, בלי שכתוב תיעוד של עבודה שכבר נחתה.

> **For agentic workers:** REQUIRED SUB-SKILL: `superpowers:subagent-driven-development`.
> מפרט: `docs/superpowers/specs/2026-08-06-process-enforcement-design.md` (מאושר 6.8.26) — ‏§8.3, §9, §10.

**‏Goal:** מה שנבנה בשלבים 1–2 מפסיק להיות ספרייה ומתחיל לפגוש את מי שעובד. ‏133 כללים, שלושה
שערים ושבעה רכיבי ניטור **קיימים ואינם רצים מעצמם.**

**מדוע זה השלב שנבחר לפני 3–5:** הוא **מפעיל את מה שכבר קיים ובדוק**, ולכן זול ומסוכן פחות מבניית
שכבות חדשות. **והראיה שהוא נחוץ נוצרה תוך כדי:** ‏7.8.26 הבקר לא קרא את §10.6 ולא שאל את מסד
החוקים אף פעם — **כי שום דבר לא שאל אותו.** הכללים היו במסד, נכונים ומלאים, וזה לא עזר.

---

## Global Constraints

- **‏DoD-2:** ‏RED נצפה על הפריט שנשלח, **מהסיבה המכוונת**, ומודבק. בדיקה שעברה בהרצה ראשונה בטלה.
- **‏§9 במפרט:** *"כל בודק חייב להוכיח שהוא נכשל על קלט רע לפני שסומכים עליו על קלט טוב."*
  **וגם ההתאוששות נבדקת** — מנגנון שמעולם לא רץ הוא הנחה.
- **לכל בדיקה, שורה בדו"ח:** *"אילו הלוגיקה הייתה שגויה אבל הכול קיים — האם זו הייתה נופלת?"*
- **הכרעת חומרה (בעלים, 6.8.26):** *"אזהרה אם פוגע ביעילות ובביצועים, חסימה אם פוגע במהות או
  באפשרות לבצע פעולות שאין להן חלופה — ולעולם לא מנגנון עקיפה, אלא רק דרך פחות יעילה."*
- **‏היעדר אינו כשל.** ‏CI, שכפול טרי, מכונה בלי מסד — **מדלגים בהצהרה, לעולם לא עוברים בשקט
  ולעולם לא נכשלים.** נשרף על זה פעמיים ב-24 שעות (‏L54, ו-`build-audit` שהדפיס `FAIL` ב-CI).
- **שער אדום קבוע הוא שער כבוי.** אם משהו כאן יידלק בכל יום — הוא שגוי, לא הכללים.
- ‏Windows · ‏PowerShell 5.1 **וגם** pwsh 7 · **אין תווים לא-ASCII ב-`.ps1`** (שבר את המפרסר 3 פעמים).
- אין `git add`/`checkout`/`restore`/`stash` עד ההפקדה · ‏hook = 480s · **לעולם לא `META_SKIP_GATE`**.
- **סיום = הופקד.**

---

## Task 1: שלושת שערי החוקים נכנסים ל-`check-meta`

**‏Files:** ‏`scripts/check-meta.mjs`

‏`check-rules-fresh` · `check-rules-mirror` · `check-rules-complete` נבנו, נבדקו, **ואינם מחווטים.**

- [ ] **‏1.** לקרוא את הכותרת של `check-meta.mjs` — היא מסבירה **מי חוסם ומי מדווח, ולמה**. הכלל:
      שער חוסם רק אם התיקון שלו **זול ומיידי מהקומיט שהפיל אותו.**
- [ ] **‏2.** לשבץ את השלושה, **ולנמק לכל אחד בנפרד** אם הוא חוסם או מדווח. הרמז מהכותרת:
      ‏`check-rules-fresh` מרפא את עצמו, ולכן חסימה היא התמריץ הנכון; **`check-rules-complete`
      אינו מרפא בכוונה** (הוא נכתב מחדש לקריאה-בלבד), ולכן שאלת החסימה שלו שונה.
- [ ] **‏3.** ‏RED: להפיל כל אחד בכוונה ולראות את `check-meta` נוקב **בשמו**.
- [ ] **‏4.** ‏GREEN: `node scripts/check-meta.mjs` מלא, מודבק.

## Task 2: `session-rules.mjs` קורא מהמסד ולא מהמסמך

**‏Files:** ‏`scripts/session-rules.mjs`

הוא **כבר מזריק את הכללים** בפתיחת session — מהמסמכים. המסד הוא היחיד שיודע לענות *"אילו כללים
מסוג X"*, *"מה השתנה"*, ו*"מה נפרש"*.

- [ ] **‏1.** לקרוא את הקיים לפני שינוי. **אסור לפגוע במה שכבר עובד:** ההזרקה חייבת להמשיך להיות
      טקסט הכללים **מילולית** — ‏CLAUDE.md קובע שזו קריאה-חוזרת ולא פרפרזה.
- [ ] **‏2.** לקרוא מ-`rules.sqlite` (המראה — **בלי גישת מסד לסוכן**), ולנפול חזרה למסמך אם המראה
      חסרה. **‏⚠️ המראה היא היטל:** ‏R-103 הוא בדיוק המקרה שבו המראה החזיקה ערך שהמקור לא.
      לכן: להצהיר במפורש **מאיזה מקור ההזרקה הגיעה** בכל ריצה.
- [ ] **‏3.** להוסיף את מה שהמסמך לבדו לא יכול לתת: **כמה כללים הוזרקו, ומה השתנה מאז ה-session
      הקודם.** זה הערך שהמסד מוסיף — ובלעדיו החיווט הוא החלפת צנרת בלי רווח.
- [ ] **‏4.** ‏RED: מראה חסרה → נפילה-חזרה מוצהרת, **לא שקט**. מראה מיושנת → אמירה מפורשת.

## Task 3: `/enforce` מכיר את מסד החוקים

**‏Files:** ‏`.claude/commands/enforce.md`

נכתב לפני שהמסד היה קיים, **ואינו מזכיר אותו אף פעם.**

- [ ] **‏1.** לקרוא אותו כולו. **מה שהוא עושה היום נשאר** — זו הוספה, לא כתיבה-מחדש.
- [ ] **‏2.** להוסיף: הרצת שלושת שערי החוקים, הרצת השומר, **ודיווח שלוש-חלקי** לפי §10.6 —
      ‏**‏DONE / NEXT / LEFT UNTIL THE GRAND FINAL, כשהשלישי הוא המספר מהמרשם ולא פרוזה.**
      זו סטייה מתועדת של הבקר ב-7.8.26, והפקודה היא המקום לקבע אותה.
- [ ] **‏3.** ‏`check-commands-exist` חוסם על מסמך-חובה שנוקב בסקריפט שאינו קיים — לוודא שכל פקודה
      שהוספת קיימת.

## Task 4: השומר רץ מעצמו

**‏Files:** ‏`.claude/settings.json` · ‏`scripts/watchman.ps1`

**‏§8.3 קובע מתי:** *פתיחה · resume · **אחרי compact** · לפני סוויטה · כל 30 דקות · `/enforce`.*

- [ ] **‏1.** ‏SessionStart (matcher `startup|resume|compact`) — **אחרי compact הוא המקרה החשוב**,
      כי זה בדיוק הרגע שבו הזיכרון שהמשמעת נשענת עליו נמחק.
- [ ] **‏2.** **⚠️ השומר לוקח ~6 שניות ומריץ תת-תהליכים.** ‏hook איטי בפתיחת session הוא מס על כל
      אינטראקציה. **למדוד, ולהחליט לפי המדידה** — ואם יקר מדי, להריץ רק את הרכיבים בחומרת `block`
      בפתיחה ואת השאר בטריגר אחר. **לנמק בדו"ח.**
- [ ] **‏3.** ‏"כל 30 דקות" — ‏`Register-ScheduledTask` **נכשל ב-`Access is denied` ללא הרשאות**
      (נמדד 7.8.26). **לא להמציא מנגנון:** לתעד את הפקודה המדויקת שהבעלים יריץ, בדיוק כפי שנעשה
      עם התקנת Neo4j, **ולבדוק אם קיימת חלופה שאינה דורשת הרשאה.**
- [ ] **‏4.** ‏RED: להפיל רכיב במתקן מבודד ולראות שהחיווט מדווח. **אל תיגע בשירות אמיתי.**

## Task 5: ההוכחה שהחיווט עובד — ולא שהוא קיים

**‏Files:** ‏`scripts/tests/run-all.mjs`

‏§9 דורש שגם **ההתאוששות** תיבדק: *"מנגנון התאוששות שמעולם לא רץ הוא הנחה."*

- [ ] **‏1.** בדיקה שמפילה כלל במסד ומוודאת ש**פתיחת session מדווחת עליו** — לא שהקובץ קיים.
- [ ] **‏2.** בדיקה שמוודאת ש-`check-meta` **נוקב בשם השער** שנפל.
- [ ] **‏3.** **וזו החשובה:** בדיקה ש**המערכת שמה לב לדריפט בין המסמך למסד** — כי זה הכשל היחיד
      שהופך את כל השכבה לתיאטרון: כללים שנאכפים ואינם הכללים שכתובים.
- [ ] **‏4.** להריץ את הסוויטה המלאה ולהדביק.

---

## מה נשאר בחוץ במפורש

| | טריגר |
|---|---|
| שלבים 3–5 (‏hooks · מונים · שופט) | אחרי שלב 6 |
| מסמך הארכיטקטורה הנייד (§14) | **אחרי שהמערכת הוכיחה את עצמה בשימוש** — הכרעת בעלים |
| מישור התוכן (§11) | קשת נפרדת |
