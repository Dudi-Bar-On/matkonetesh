# המלצת מתודולוגיה ואכיפה — 2026-07-30

> **משימה 7 מתוך 7 בקשת התכנון.** המסמך משלים את מפת הדרכים (`docs/ROADMAP-2026-07-30.md`) ואת אודיט
> הציות (`docs/process/COMPLIANCE-AUDIT-2026-07-30.md`): הוא קובע **איך** ממשיכים לפתח כך שהמשמעת
> נאכפת מכנית ולא נשענת על זיכרון. כל מנגנון כאן שמסומן **Phase 0** הופך למשימה בפועל ב-Phase 0
> של מפת הדרכים.

---

## 1 · הפסיקה: superpowers מספיק — GSD לא מתווסף

**פסיקה חד-משמעית: נשארים על superpowers בלבד. לא מאמצים GSD.**

הנימוק, מבוסס על ממצאי האודיט ולא על טעם:

1. **הליבה של superpowers עובדת — האודיט הוכיח זאת.** הצינור brainstorm → spec → plan →
   subagent-driven-development → review מיושם בעומק: 15 מפרטים, 17 תוכניות, ~40 דוחות-משימה,
   ~70 חבילות `review-*.diff` ב-`.superpowers/sdd/`, שערי-Phase עצמאיים שנבדקים **מול המפרט ולא מול
   המרשם** (`e3-phase-gate-verdict.md` — "הארטיפקט החזק ביותר ברפו" לפי האודיט). עדי-RED מודבקים,
   שער ה-Waiver (§4) מכובד מאז equipPlan. זו בדיוק הליבה ש-GSD היה בא לספק — והיא כבר קיימת,
   מוטמעת, ועם הרגלי-עבודה בנויים סביבה.

2. **מה שנשבר הוא לא מה ש-GSD פותר.** כל ממצאי הסחף באודיט הם בשכבת ה-META: יומן לקחים שקפא
   ב-L22, גרף ידע 53 מסמכים מאחור, אין תקרת מקבילות סוכנים, אין בדיקת שלמות-קוד לתוכנית מחוללת,
   משמעת כלים (grep במקום serena/graphify), סיכומי ברן-דאון שמופקים רק לפי דרישה. מערכות בסגנון
   GSD מספקות עוד שכבת spec→task→execute — כלומר **שכפול** של מה שכבר עובד — ואינן מספקות אף אחד
   מהמנגזרים החסרים. הכשלים דורשים **אכיפה מכנית מקומית** (סקריפטים, hooks, צ'קליסטים), לא מסגרת
   עבודה חדשה.

3. **עלות ההחלפה ממשית, התמורה אפסית.** מעבר ל-GSD פירושו הגירת מוסכמות של 15 מפרטים ו-17
   תוכניות, אוצר-מילים כפול לסוכנים (שכבר יורשים את CLAUDE.md הנוכחי), ואיבוד הידע המוטמע
   ב-`.superpowers/sdd/`. לפי §10.15 מחליפים רכיב רק כשהוא **מועד שוב ושוב** — וכאן הרכיב דווקא
   עמד במבחן.

4. **מה שכן לוקחים מרוח ה-GSD** (בלי לאמץ את המערכת): העיקרון "הכול קובץ, הכול שער" — וזה בדיוק
   מה שמפת הדרכים 2026-07-30 כבר עושה (בסיס יחיד, שער בעלים פר-Phase, מרשם ברן-דאון בסעיף 5).
   ההשלמה היחידה הנדרשת היא הפיכת שערי ה-META למכניים — סעיפים 3–4 להלן.

**שורה תחתונה:** superpowers + מפת הדרכים + חבילת האכיפה של Phase 0 = המערכת. אין רכיב חסר
ש-GSD ממלא.

---

## 2 · מודל ההפעלה: subagent-heavy, קונטקסט ראשי דק (H6)

### 2.1 · חלוקת העבודה

| מה | רץ איפה | למה |
|---|---|---|
| החלטות, שערים, תקשורת עם הבעלים, פסיקות §4 | **Main בלבד — לעולם לא מואצל** | provenance של החלטות חייב לשבת ברצף שיחה אחד; הבעלים מדבר עם ישות אחת |
| קבלת/דחיית verdict של שער-Phase; הכרזת "done" | **Main בלבד** | "Being wrong is worse than being silent" — האחריות לא עוברת בירושה |
| ניהול המרשם (ROADMAP §5) + סיכום תלת-חלקי §10.6 | **Main** | זו הליבה של "ניהול פיתוח" — הדבר היחיד שאסור שיאבד בקומפקציה |
| כתיבת מפרט/תוכנית (טיוטה) | Subagent, אישור ב-Main | כתיבה כבדה = קונטקסט כבד; האישור והצגה לבעלים נשארים ב-Main |
| מימוש משימות (SDD פר-משימה, סוכן טרי) | **Subagent** | הדפוס הקיים ב-`.superpowers/sdd/` — עובד, נשמר |
| code-review + spec-audit (שני verdicts, §7) | **Subagent** (טרי, בלי גישה ל-progress.md) | עצמאות המבקר — הוכחה כמנגנון החזק ביותר ברפו |
| קריאה כבדה: מחקר, סינתזה, סריקות תרגום, איסוף ראיות | **Subagent** | הראשי מקבל מסקנות + נתיבי קבצים, לא dumps |
| רענון גרף, ריצות סוויטה, איסוף צילומי 390×844 | **Subagent** (סדרתי, ראו 2.3) | עבודה מכנית ארוכה; הראשי רק מוודא את התוצר |

### 2.2 · חוזה ה-brief והדוח (file-based handoffs)

התשתית כבר קיימת — `scripts/task-brief` ו-`review-package` של superpowers ודוחות
`.superpowers/sdd/*-report.md`. מקבעים אותה כחוזה:

- **Brief = קובץ**, לא פרומפט חופשי. חובה בכל brief: (א) שורות המפרט המדויקות שהמשימה מספקת
  (DoD-1); (ב) הקוד המדויק מהתוכנית; (ג) צ'קליסט ה-DoD הרלוונטי; (ד) **חוזה דוח** — שם קובץ
  הדוח, מה חייב להיות מודבק בו (פלט RED, פלט GREEN, exit code, נתיבי צילומים);
  (ה) **שדה "כלי ראשי"**: serena לעבודה סימבולית, graphify לשאלות מסמכים/יחסים, grep רק כ-fallback
  מוצהר. שדה חסר = brief פסול.
- **דוח = קובץ** ב-`.superpowers/sdd/`, והסוכן מחזיר לראשי רק תקציר + נתיב. הראשי **מאמת בעצמו
  ב-diff** (כלל ה-Reporting) — לא מאמין לדוח לבדו.
- **תקציב קונטקסט של הראשי:** הראשי לא קורא קבצי מקור מלאים, לא פלטי סוויטה מלאים, ולא מסמכים
  ארוכים — כל אלה יורדים לסוכן שמחזיר תמצית. כלל אצבע: אם קריאה צפויה לעלות יותר מ-~2k שורות
  מצטברות — היא של subagent. הראשי שומר את עצמו מתחת לאזור הקומפקציה כדי שהמרשם, ההחלטות
  והשיחה עם הבעלים לא יימחצו.

### 2.3 · תקרת מקבילות (הלקח מה-fan-out wedge)

נכתב ל-§10.5a (תיקון Phase 0) והופך חלק מכל brief של `dispatching-parallel-agents`:

- ברירת מחדל: **סדרתי**.
- עבודות בלתי-תלויות וקלות (קריאה/סריקה): עד **3 במקביל**; תקרה קשיחה **5**.
- בזמן ריצת סוויטה, build, או תור ה-GPU של התרגום: **סוכן כבד אחד לכל היותר** (הרחבת כלל
  הסריאליזציה של §11a מ-workers לסוכנים).
- שלושה תיקוני-באג בלתי-תלויים = שלושה dispatches נפרדים, לא סוכן-על אחד (הלקח מה-over-bundling).

---

## 3 · מנגנוני הקשחה — ממצא ← אכיפה מכנית

עיקרון מנחה: **כל ממצא מקבל מנגנון שרץ מעצמו או נבדק בשער — לא "נשתדל לזכור".**

### 3.1 · חמשת תיקוני האודיט

| # | ממצא | מנגנון אכיפה | צורה | פאזה |
|---|---|---|---|---|
| 1 | יומן לקחים קפא ב-L22; לקחים חיים רק בזיכרון פרטי | (א) Backfill L23–L27 ל-§11; (ב) `scripts/gate-lessons.mjs`: מדפיס את ה-L האחרון + תאריכו, סופר קומיטי `release(v` מאז; **חוסם** קומיט release כשיש קשת סגורה בלי L-entry או הצהרת "אין לקח" מפורשת | script + doc-edit | Phase 0 |
| 2 | גרף מקומי 53 מסמכים מאחור; הפקדות §10.16 לא רצו | (א) רענון מיידי `--mode deep` + הפקדת קשת v5; (ב) `scripts/check-graph-fresh.mjs`: משווה mtime של `docs/**/*.md` מול חותמת בניית `graphify-out/graph.json`, מדפיס רשימת מסמכים סוטים, **נכשל** אם יש; מחווט ל-`sync-docs.sh` (רץ לפני כל push של docs) | script + hook | Phase 0 |
| 3 | סתירת ×1/×2 בין §9 ל-DoD-12; הוראת "פעמיים לפני שילוח" רק בזיכרון | **פסיקת H7 (בעלים, 30.7) נכתבת למסמך**: שער-משימה (DoD-12) = ריצה נקייה **×1**; שער-release = **×2**; מוחקים את הסתירה מ-§9 | doc-edit | Phase 0 |
| 4 | חילוצי מודולים — רשומים, לא מבוצעים | מפת הדרכים כבר קובעת: ORCH ב-Phase 3, NAV ב-Phase 5 (+המשך 9/12) — החילוץ הוא **משימת תוכנית** עם שער, לא עיקרון רשום; Phase 0 מאשרר את התזמון מול הבעלים | roadmap (כבר מעוגן) | Phase 0 אשרור |
| 5 | תוכנית CP2 נקטעה בשקט — משימות בלי קוד | `scripts/check-plan-complete.mjs <plan.md>`: מפרק לפי כותרות `## Task`, סופר בלוקי-קוד מגודרים פר-משימה, **נכשל** על 0 או על קובץ שמסתיים בתוך fence (גלאי קטיעה); רץ לפני כל review של תוכנית ובפתיחת כל Phase (פרוטוקול ROADMAP §0.3) | script | Phase 0 |

### 3.2 · שאר אזורי הסחף

| ממצא | מנגנון אכיפה | צורה | פאזה |
|---|---|---|---|
| Fan-out wedge + over-bundling (אין תקרה כתובה) | §10.5a חדש: התקרה מסעיף 2.3 במילים מחייבות; כל brief של dispatch מצטט אותה | doc-edit + brief-template | Phase 0 |
| משמעת כלים — grep במקום serena/graphify (תוקן פעמיים בעל-פה) | (א) שדה "כלי ראשי" חובה בכל brief (2.2); (ב) שורת צ'קליסט פתיחת-session; (ג) מבקר-קוד מסמן תשובה סימבולית שהופקה ב-grep בלי הצהרת fallback | checklist + brief-template | Phase 0 |
| סיכומים/ברן-דאון מופקים לפי דרישה ולא שוטף (§10.6) | §10.6 מתוקן: החלק השלישי = **שורת המרשם מ-ROADMAP §5** (מספר, לא פרוזה); סוכן שער-ה-Phase בודק שהמרשם עודכן — מרשם לא-מעודכן = שער לא עובר | doc-edit + gate-check | Phase 0 |
| ניתוח חיצוני רדוד (מספרים במקום קונספטים) — נדרש re-run | כלל קבוע ב-§7: הצעה חיצונית נסקרת **בשני מעברים ע"י סוכנים שונים** — מעבר קונספטים ("אילו פערים אמיתיים זה חושף אצלנו") ומעבר מספרים; verdict אחד בלבד = סקירה לא שלמה | doc-edit (§7) | Phase 0 |
| §10.10 בלי מסלול חלופי כשכלי הדפדפן נשברים | פרוטוקול fallback ל-§10.10: curl של ה-URL החי → אימות `.foot-stamp` = `מהדורה NNN` + מחרוזת-פיצ'ר מהמהדורה, הפלט מודבק בדוח; אימות דפדפן מלא מושלם באותו יום; **בלי אחד מהשניים אין הכרזת "live"** | doc-edit | Phase 0 |
| Co-Authored-By מומצא (245 קומיטים עם "Opus 4.8 (1M context)") | כלל §9: ה-trailer = **מחרוזת המודל שה-session מצהיר, כלשונה** — לעולם לא מנוחשת ולא מועשרת; אכיפה קלה: `commit-msg` hook עם allowlist של מחרוזות מדויקות | doc-edit + hook (רשות) | Phase 0 |
| סלקטורים שבירים ומרחב-שמות בבדיקות | כלל §11a/§9: בדיקות בוחרות רק דרך **חוזה סלקטורים** (`tests/selector-contract.md` — data-testid/מחלקות יציבות); כל מפתחות אחסון חדשים תחת קידומת `mk-`; codemod של Dec-H3 (Phase 3) נשען על החוזה הזה | doc-edit + contract-file | Phase 0 (חוזה), Phase 3 (אכיפה מלאה) |

### 3.3 · חיבור הכול לנקודת ריצה אחת

**`scripts/check-meta.mjs`** — עוטף את `check-graph-fresh` + `gate-lessons` + בדיקת
**no-unlanded-items** (H8, סעיף 3.4) (+ בדיקת trailer אם תמומש) לריצה אחת. רץ: (א) בצ'קליסט
פתיחת-session; (ב) מתוך `sync-docs.sh` לפני push; (ג) לפני כל קומיט `release(v`;
(ד) **בכל שער-Phase ובכל סגירת קשת** (חובת H8). **צ'קליסט פתיחת-session** — בלוק בן 5 שורות שנוסף ל-CLAUDE.md:

1. קרא `docs/process/development-discipline.md` — §10 ואז §3 (הכלל הקיים, נשאר ראשון).
2. קרא את ה-Phase הפעיל ב-`docs/ROADMAP-2026-07-30.md` + מצב המרשם (§5).
3. הרץ `node scripts/check-meta.mjs` — טפל בכל אדום לפני עבודה.
4. עבודה סימבולית → serena; שאלות מסמכים/יחסים → graphify; grep = fallback מוצהר.
5. תקרת מקבילות: סדרתי כברירת מחדל; ≤3 קלים; ≤5 קשיח; 1 בזמן סוויטה/GPU.

### 3.4 · H8 — כלל הנחיתה המלאה ("שום דבר באוויר") — כלל אדוק (פסיקת בעלים 30.7)

לכל פער, החלטה או רעיון יש **בדיוק אחד** משלושה מצבים חוקיים:

1. **פאזה נקובה** במפת הדרכים (Phase N / Thread, בשם).
2. **דחייה מנומקת עם טריגר מוגדר** לפתיחה מחדש (דוגמה: band H — טריגר: אבן-הדרך זהות/Paddle
   של ה-Sync Thread; D6 — טריגר: השלמת S1).
3. **משימת דיון/סיעור רשומה בפאזה נקובה** — המשימה הרשומה **היא** הדיון עצמו, והחלטותיו משולבות
   חזרה בתוכנית אחריו (דוגמה: "Brainstorm: הזנת פרמטרי מתכון" ב-Phase 9).

**"נדחה בלי מועד" — אסור.** אין "לא מטופל", אין "נשקול בהמשך", אין פריט שחי רק במסמך-דלתא.
זה הלקח מביקורת הכיסוי 30.7: 43 פערים ללא נחיתה, 9 מהם נשמטו מהתוכנית כליל.

**האכיפה המכנית — הרחבת מפרט `check-meta.mjs` בבדיקת no-unlanded-items:** הבדיקה מפרסרת את
מרשם ה-ROADMAP (§5 + שורות ה"סוגר" של הפאזות); **כל מזהה פער במרשם חייב למפות לפאזה נקובה או
לדחייה מעוגנת-טריגר; כל מצב אחר — exit nonzero.** הבדיקה רצה **בכל שער-Phase ובכל סגירת קשת**,
בנוסף לריצות הקבועות של check-meta (סעיף 3.3).

### 3.5 · H9 — טבלת סיכום-משימה מחייבת (GSD-style; פסיקת בעלים 30.7)

**כל משימת פיתוח** — ב-Main או ב-subagent — מסתיימת בטבלה קבועה בת 5 שורות:

| # | שורה | תוכן |
|---|---|---|
| 1 | **מה היה** (Before) | המצב/הבעיה לפני המשימה |
| 2 | **מה נעשה** (Done) | מה בוצע בפועל + ראיות (קומיט, בדיקות, קבצים) |
| 3 | **מה נשאר** (Remaining) | מה נותר פתוח מהמשימה/מה-Phase |
| 4 | **איפה אנחנו** (Position) | מיקום מול ה-Roadmap: "Phase X, משימה Y מתוך Z" + מרשם "N נסגרו / M נותרו" — **נקרא מ-`docs/STATUS-BOARD.md`** (H10) |
| 5 | **הבא בתור** (Next) | המשימות הבאות בתור |

- אצל **subagents** זהו חלק מ**חוזה הדוח** (סעיף 2.2 — הטבלה נוספת כשדה חובה בתבנית ה-brief;
  brief בלי דרישת-טבלה = brief פסול); ה-**Main מאמת ומעביר** אותה לבעלים.
- זו ההרחבה המוּבנית של §10.6: שלושת החלקים (DONE/NEXT/LEFT) מקבלים צורה קבועה עם עיגון
  Roadmap+מרשם, במקום פרוזה חופשית.
- **סיום כל משימה מעדכן את `docs/STATUS-BOARD.md`** (H10) — חלק משגרת סגירת-המשימה, נאכף
  בצ'קליסט arc-close ובשערי-Phase (לוח לא-מעודכן = שער לא עובר).

### 3.6 · H10 — לוח מצב חי (`docs/STATUS-BOARD.md`; פסיקת בעלים 30.7)

קובץ יציב-שם, **מקור האמת למיקום מול התוכנית**: שורה פר Phase (משימות בוצעו/סה"כ · סטטוס ·
פערים שנסגרו) + שורת סך-פרויקט (משימות, פערים/156, % והפער עד הסיום). שורת "איפה אנחנו" של H9
נקראת ממנו; סיום כל משימה מעדכן אותו. נבדק בכל שער-Phase יחד עם no-unlanded-items.

**H10a — תחזוקה מול תצוגה (עדכון בעלים 30.7):** טבלת H9 והלוח **מתוחזקים בכל משימה** — דוח כל
subagent נושא את טבלת H9 אל ה-Main, והלוח מתעדכן בכל סגירת-משימה — אך **מוצגים לבעלים רק
באבני-דרך** (שער-Phase · release · סגירת קשת) **או לבקשתו המפורשת**, לא אחרי כל משימה.
מעקב אדוק בלי רעש.

---

## 4 · משימות Phase 0 הנגזרות (סקריפטים והוקים, קונקרטית)

| תוצר | מה בודק | מתי רץ |
|---|---|---|
| `scripts/check-plan-complete.mjs` | בלוקי-קוד פר-`## Task` > 0; אין קובץ שנגמר בתוך fence | לפני review של כל תוכנית; בפתיחת Phase |
| `scripts/check-graph-fresh.mjs` | mtime של `docs/**/*.md` מול חותמת הגרף; פולט רשימת סוטים | מתוך `sync-docs.sh`; פתיחת session |
| `scripts/gate-lessons.mjs` | L אחרון ב-§11 מול קומיטי release מאז; חוסם release בלי L או הצהרה | לפני קומיט `release(v`; סוף קשת |
| `scripts/check-meta.mjs` | עוטף את שלושת הנ"ל + בדיקת **no-unlanded-items** (H8): פרסור מרשם ה-ROADMAP — כל מזהה ממופה לפאזה או לדחייה מעוגנת-טריגר, אחרת exit nonzero | פתיחת session; לפני push/release; **כל שער-Phase וסגירת קשת** |
| `docs/STATUS-BOARD.md` (H10) | לוח המצב החי — שורה פר Phase + סך-פרויקט; מוזן משורת H9 בסיום כל משימה | מתעדכן בסיום כל משימה; נבדק בשער-Phase |
| תבנית brief (H9) | שדה חובה: טבלת 5 השורות של H9 בחוזה הדוח | כל dispatch |
| `tests/selector-contract.md` | רשימת הסלקטורים היציבים המותרים לבדיקות + כללי `mk-` | reference לכל כותב-בדיקות ולמבקר |
| `docs/process/checklists/session-start.md` + בלוק ב-CLAUDE.md | 5 השורות מסעיף 3.3 | כל פתיחת session |
| `docs/process/checklists/arc-close.md` | לקחים→§11 · הפקדות graphify global · רענון גרף · מרשם · סיכום §10.6 · **טבלת H9 + עדכון STATUS-BOARD (H10) + no-unlanded-items (H8) ירוקים** | סגירת כל קשת/Phase |

---

## 5 · רשימת תיקונים ל-`docs/process/development-discipline.md` (Phase 0 מחיל; לא כותבים מחדש)

1. **§3 DoD-12** — נוסח H7: משימה = ריצה נקייה ×1 (plain, בלי overrides); release = ×2.
2. **§9** — מחיקת "FULL suite ×2 per task"; הוספת פסיקת H7; הוספת כלל חוזה-סלקטורים + `mk-`;
   הוספת כלל Co-Authored-By (מחרוזת session בלבד).
3. **§7** — הוספת כלל שני-המעברים (קונספטים/מספרים, סוכנים שונים) להצעות חיצוניות.
4. **§10.5a חדש** — תקרת מקבילות סוכנים (סעיף 2.3 כאן, כלשונו).
5. **§10.6** — החלק השלישי = שורת המרשם מ-ROADMAP §5; שער-Phase בודק עדכון מרשם; **וההרחבה
   המוּבנית של H9**: כל משימת פיתוח מסתיימת בטבלת 5 השורות (מה היה · מה נעשה+ראיות · מה נשאר ·
   איפה אנחנו [מ-STATUS-BOARD] · הבא בתור) — סעיף 3.5 כאן, כלשונו.
6. **§10.10** — פרוטוקול ה-fallback (curl + `.foot-stamp` + feature probe).
7. **§10.12** — הפניה ל-`check-graph-fresh.mjs` כאכיפה; `sync-docs.sh` מריץ אותו.
8. **§10.16** — הפניה ל-`gate-lessons.mjs` + `arc-close.md` כאכיפה.
9. **§11** — Backfill **L23–L27**: v267 proxy-metric · token-cap truncation · fan-out wedge ·
   over-bundling · CP2 plan truncation.
10. **§2 / שימוש ב-writing-plans** — שורה אחת: תוכנית מחוללת אינה מוגשת ל-review לפני
    `check-plan-complete.mjs` ירוק.
11. **נספח חדש (Operating Model)** — טבלת Main/Subagent + חוזה brief/report (סעיף 2 כאן) —
    כדי שסוכנים יירשו אותו דרך המסמך, לא דרך זיכרון פרטי.
12. **H8 (חדש)** — כלל הנחיתה המלאה כסעיף discipline: שלושת המצבים החוקיים, איסור "נדחה בלי
    מועד", והפניה לאכיפת no-unlanded-items ב-`check-meta.mjs` (סעיף 3.4 כאן, כלשונו).
13. **H9 + H10 (חדשים)** — טבלת סיכום-המשימה המחייבת (סעיף 3.5) + לוח המצב החי
    `docs/STATUS-BOARD.md` (סעיף 3.6): חובת עדכון בסיום כל משימה, אכיפה בשער-Phase וב-arc-close;
    הטבלה נכנסת לתבנית ה-brief כשדה חובה בחוזה הדוח.

---

*נכתב כמשימה 7/7 של קשת התכנון. סטטוס: ממתין לשער בעלים של Phase 0 (יחד עם פסיקת H7 שכבר
ניתנה, אשרור תזמון החילוצים, ונחיתות היתומים).*
