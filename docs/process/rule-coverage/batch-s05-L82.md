# מנה s05 — `L82`, הלקח שנולד מהריצה האמיתית הראשונה של שער

**אושר על-ידי הבעלים 9.8.26 ("מאשר תמשיך").**

## למה הלקח הזה קיים

שער `check-test-waits` נבנה לפי תוכנית, עבר את כל הבדיקות המומצאות שלו — ואז הורץ בפעם הראשונה
על הסוויטה האמיתית ומצא **שלושה דברים שאף בדיקה מומצאת לא הייתה מוצאת**:

1. **דפוס ה-regex שלי מעולם לא התאים לדוגמה שלי עצמה** — ‏`[^)]*` אינו יכול לחצות את הסוגר של
   `waitForFunction(() => ...)`. השער היה נשלח **אינרטי** ונראה תקין.
2. **חמש התרעות שווא**, כולן בתוך הערות `//` — כלומר השער ירה על **פרוזה על הכלל** במקום על הפרה שלו.
3. **הפרה אמיתית אחת בסוויטה החיה** — ‏`tests/vg-classifier-wiring.spec.ts:53` נשא
   `waitForFunction(() => true)`, המתנה שמחכה לכלום. תוקנה, והקובץ רץ פעמיים 9/9.

## הסיווג

**שתי המסווגות העיוורות הסכימו: `B`.** שתיהן גם ציינו שהעובדה הקודמת שצריך לדעת אינה בדיף אלא
ברצף — האם הסקריפט **הורץ** מול הקורפוס האמיתי לפני ההפקדה, ולא רק נכתב.

| מזהה | קבוצה | מנגנון | העובדה הקודמת |
|---|---|---|---|
| **`L82`** | `B` | `commit-gate` · יעד `scripts/check-*.mjs` | האם שער חדש בהפקדה הזו הורץ על העץ האמיתי — הדיף מוכיח שהוא **נכתב**, לא שהוא **רץ** |

```json
{
 "approved_by_owner": "2026-08-09",
 "entries": [
  {
   "rule_id": "L82",
   "rule_group": "B",
   "mechanism": "commit-gate",
   "mechanism_target": "scripts/check-*.mjs — a new gate in this commit must have been run against the real tree"
  }
 ]
}
```
