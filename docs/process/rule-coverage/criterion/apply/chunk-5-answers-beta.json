[
  {"token":"S77","group":"C","reason":"Deciding whether a given occurrence of the word is 'the generic device word' colliding with the oven category, versus a legitimate reference to an actual oven, requires judging the meaning of the term in its context — mere presence of the string decides nothing."},
  {"token":"S78","group":"C","reason":"Compliance means the agent verified the MECHANISM behind an it's-unrelated diagnosis by reading the named lines — deciding whether an explanation was genuinely verified versus plausibly asserted requires evaluating the content and intent of the diagnosis."},
  {"token":"S79","group":"none","cost":"Enforcing would require new scheduled index-health probe infrastructure (the watchman layer-0 component the text itself says does not exist) plus a verified REINDEX recovery action; the text as written is a lesson narrative with a named gap, not a per-action compliance condition.","importance":"High — a silently corrupted vector index passes every existing freshness gate and only surfaces via one acceptance test; the gap is real and worth building, but the rule itself carries no checkable condition today."},
  {"token":"S80","group":"C","reason":"Compliance means every clock/focus/scroll-dependent pixel in a baseline was named and pinned or masked with a written justification — deciding whether a screen region actually depends on the clock requires understanding what the rendered content means, not matching a pattern in the baseline file."},
  {"token":"S81","group":"C","reason":"The rule says a false-alarming gate must be fixed by making its metric precise rather than lenient — deciding whether a given gate change 'loosens' versus 'sharpens' the measurement requires evaluating the quality and intent of the change, not its text."},
  {"token":"S82","group":"C","reason":"DoD-5 compliance requires naming the render path AND confirming it fires on the real shipped data — whether a named reader actually executes on real rows is a question about runtime behaviour and evidence quality, not a textual pattern in any artifact."},
  {"token":"S83","group":"A","artifact":"the Playwright spec file being written (the Edit/Write payload)","pattern":"a spec containing `page.clock` that also contains a bare `new Date(` (or Date.now()) in test-body/assertion code outside a page.evaluate callback — Node-side clock reads co-occurring with a pinned page clock","mechanism":"pretooluse:Edit|Write","mechanism_target":"tests/**","reason":"The forbidden mix is structurally present in the spec text itself; co-occurrence of page.clock with a Node-side Date read decides the violation without reading intent."},
  {"token":"S84","group":"A","artifact":"the requirements manifests plus requirements-overrides.txt (and pip check output over the resolved environment)","pattern":"a version pin that conflicts with an installed package's declared dependency range (pip check reports the conflict) with no corresponding entry-and-reason line in requirements-overrides.txt, or an overrides entry lacking its in-force/still-needed tests","mechanism":"ci-gate","mechanism_target":"requirements*.txt","reason":"A deliberate dependency exception is mechanically visible as a resolver conflict; presence of the conflict without a matching overrides declaration decides non-compliance."},
  {"token":"S85","group":"B","mechanism":"pretooluse:Edit|Write","mechanism_target":"implementation files governed by an approved spec under docs/superpowers/specs/**","observed_prior_facts":"whether the governing spec file was actually opened (a Read of its path recorded in the session's tool-call log/state store) since session start, before the first implementing edit","reason":"The rule is 'open the spec before implementing from it' — compliance depends on a prior event (the spec was read this session), which the recorded tool-call history observes."},
  {"token":"S86","group":"none","cost":"Enforcing 'measure that a quiet gate actually sees the common path before trusting it' would require per-gate observation telemetry and a defined common-path corpus for every gate — a measurement campaign per gate, not a checkable condition on any single action.","importance":"High — two knowledge rules reported green for a whole shift while seeing 0% of the real traffic; but as written it is a verification-methodology lesson, with no per-action compliance condition."},
  {"token":"S87","group":"none","cost":"The rule is a one-line principle: agreement between two artifacts proves nothing because both can be stale; enforcing it would require an independent ground-truth source for every consistency claim, which is exactly what is missing in each instance.","importance":"Medium — a recurring failure shape worth keeping in view, but it names no artifact, no action and no compliance condition; it is a principle, not a rule that can fire."},
  {"token":"S88","group":"A","artifact":"the test-helper source being written (the Edit/Write payload)","pattern":"a broad exception handler — `except Exception:` or a bare `except:` — whose body calls skip()/pytest.skip, i.e. an excuse path with no positive marker (exception type, marker list, exit code) for the condition being excused","mechanism":"pretooluse:Edit|Write","mechanism_target":"tests/**","reason":"The forbidden shape — broad catch routed to skip — is a concrete structural pattern in the code itself; its presence decides the violation without reading intent."},
  {"token":"S89","group":"C","reason":"The rule requires that at least one test exercise the real entry-point with NO environment override — deciding whether a given test genuinely runs the production load path (versus injecting the test-only variable somewhere in its setup chain) requires understanding what the test's environment handling means, not matching a pattern."},
  {"token":"S90","group":"C","reason":"Compliance means a justification quoting another file uses that file's words verbatim and was checked against it — identifying which prose is a citation, what its claimed source is, and whether an added clause changes the source's meaning requires evaluating meaning and intent."},
  {"token":"S91","group":"C","reason":"Deciding whether each DoD line of the governing spec is genuinely MET with evidence — versus asserted — requires judging the sufficiency and relevance of pasted evidence against spec text; no textual pattern in any artifact decides it."},
  {"token":"S92","group":"C","reason":"The rule demands gates that distinguish a tool's absence from its failure, state what they ran against, and are never 'fixed' by weakening — deciding whether a gate's logic conflates those results, or whether a change weakens versus sharpens it, requires reading and judging the gate's semantics."},
  {"token":"S93","group":"none","cost":"The text documents a resolved design decision — state isolation keyed per-actor for §5 and per-session for §10.16, plus a ruling on legacy rows; enforcing it anew would mean auditing the state-store schema, a one-time design fact rather than a per-action condition.","importance":"High for the enforcement infrastructure it fixed (cross-agent blocking observed live twice), but as a classified rule it is documentation of a decision — there is no ongoing compliance condition to check."},
  {"token":"S94","group":"C","reason":"The reflex is 'verify the instrument measures what you think before reporting a fault in the system under test' — deciding whether a verification step was genuinely performed and whether it validates the instrument requires evaluating the intent and quality of the debugging process."},
  {"token":"S95","group":"B","mechanism":"stop","mechanism_target":"answer text claiming a document/change 'landed'/'committed' (e.g. the phrases committed, landed, נחת, in the stop payload)","observed_prior_facts":"whether a `git show HEAD:<path>` / `git log` Bash call (or a CI result check) for the claimed path was recorded in the session's tool-call state store after the last edit to that path — as opposed to only geniza/projection queries","reason":"The rule forbids answering 'did this land' from a disk-built projection; compliance depends on which evidence-gathering events preceded the claim, which the recorded tool history observes."}
]
