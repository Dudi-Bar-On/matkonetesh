[
  {"token":"S58","group":"C","reason":"Deciding whether a numeric limit is a language-sensitive threshold that was validated only on English (vs. measured on Hebrew or discovered at runtime) requires judging the meaning and provenance of the constant — no textual pattern in any artifact decides it."},
  {"token":"S59","group":"none","cost":"The entry is a historical failure record that names the gates (DoD 8, §10.2) that now catch this class; it states no compliance condition of its own — enforcing it would mean enforcing those already-existing gates.","importance":"נמוכה — the value is documentary: it justifies DoD 8 / §10.2, which carry the actual enforcement."},
  {"token":"S60","group":"C","reason":"Deciding whether a constant cited inside a reason is a measured value or an invented cap — and whether its origin is stated 'in the same breath' — requires evaluating the meaning and provenance claims of prose reasoning."},
  {"token":"S61","group":"C","reason":"The rule's check — 'for every language and artifact class in the repo, name the gate that would go red' — requires enumerating artifact classes and judging whether a named gate actually observes each one; that is a coverage-completeness judgement about meaning, not a pattern in any single artifact."},
  {"token":"S62","group":"C","reason":"Recognizing that a written sentence is an environment-capability claim ('there is no network', 'this tool cannot X'), and whether it rests on two independent tools with the refusing layer distinguished, requires evaluating the meaning of the claim — the trigger itself is not pattern-decidable."},
  {"token":"S63","group":"C","reason":"Deciding whether a green test proves behaviour — fixture matches the live shape, precondition actually established, interceptor actually fired, remote-only contract acknowledged — requires judging test quality and what the assertion means, not a textual pattern."},
  {"token":"S64","group":"A","artifact":"the shell command in the pretooluse:Bash payload","pattern":"a command segment invoking `sudo` (or `winget install` / a known elevation-requiring installer) through this TTY-less non-interactive channel","mechanism":"pretooluse:Bash","mechanism_target":"sudo / winget install in non-interactive shell commands","reason":"The presence of an elevation/password-requiring invocation in a channel with no TTY decides that the silent-failure trap applies, without reading intent."},
  {"token":"S65","group":"A","artifact":"the shell command in the pretooluse:Bash payload (or a diff in Edit|Write)","pattern":"`GEMINI_API_KEY` (or another *_API_KEY / key-shaped literal) appearing as an argument to an output command — echo / Write-Host / console.log — or inside written file content","mechanism":"pretooluse:Bash","mechanism_target":"echo/print/log or commit of *_API_KEY values","reason":"The operative rule — never print, log, echo, or commit a key — is decided by the presence of the key token in an output/write context, with no intent-reading."},
  {"token":"S66","group":"C","reason":"Deciding whether the situation is 'identical-looking code behaving differently' and whether the agent then compared bytes rather than glyphs, and verified scripted edits by running rather than grepping, requires judging the debugging conduct — no artifact pattern decides compliance."},
  {"token":"S67","group":"B","mechanism":"subagentstop","mechanism_target":"subagent completion reports claiming 'committed'/'pushed' vs `git status`/`git log`","observed_prior_facts":"whether a commit touching the reported files actually exists in the repository since the agent was dispatched — the report text is in the subagentstop payload, the repository state is the authoritative prior-fact store","reason":"Compliance is a comparison between a claimed prior event (a commit) and the repository's recorded state, not a pattern in a single artifact."},
  {"token":"S68","group":"C","reason":"Deciding whether at least one test feeds bytes captured from the real endpoint rather than a fixture authored from the parser's own assumption requires judging the provenance and meaning of the fixture — the file itself carries no pattern distinguishing captured from invented."},
  {"token":"S69","group":"none","cost":"A historical failure record pointing at existing gates (§5, DoD 11); it defines no compliance condition of its own — enforcement lives entirely in the gates it references.","importance":"נמוכה — documentary value only; the 3-fix stop and no-arbitrary-waits rules are enforced elsewhere."},
  {"token":"S70","group":"C","reason":"Compliance means interpreting a specific error message correctly (timeout, not crash) and running the one-minute isolation discriminator before hunting a crash — evaluating the agent's diagnostic conduct and interpretation, which is judgement of meaning."},
  {"token":"S71","group":"none","cost":"It is Occam/L14 restated with a fresh scar plus an observation about user corrections — a reasoning principle and historical record; judging whether a hypothesis 'needs several independent things to be true' would require modelling the agent's entire reasoning.","importance":"בינונית — it genuinely shapes debugging quality, but it carries no compliance condition an enforcement point could test."},
  {"token":"S72","group":"A","artifact":"the configuration file content in the pretooluse:Edit|Write payload (compose/deployment/dependency declaration)","pattern":"an image or dependency reference carrying the floating `latest` tag (e.g. `image: …:latest`) or no version pin at all, instead of a pinned version number","mechanism":"pretooluse:Edit|Write","mechanism_target":"docker-compose*/deployment/dependency config files with image/version declarations","reason":"The presence of a `:latest` (or absent) version specifier in a written config decides non-compliance textually, with no intent-reading."},
  {"token":"S73","group":"C","reason":"Deciding whether a multi-state field's consumers all read it through one collapsing reader, and whether a sentinel lives inside the value's own domain, requires judging the semantics of the field and of every consumer — a meaning judgement across code, not a pattern."},
  {"token":"S74","group":"C","reason":"Deciding whether the assertion accompanying a screenshot reads the rendered output rather than internal JS state requires understanding what the asserted expression means — state variable vs. DOM — which is a judgement of meaning, not a nameable textual pattern."},
  {"token":"S75","group":"B","mechanism":"commit-gate","mechanism_target":"git commit closing a task — full `npx playwright test` run since the last code edit","observed_prior_facts":"whether a complete green suite run occurred after the task's final edit (and whether the implementer's RED run preceded GREEN) — a sequence of prior runs observable in recorded run history/state, not in any single artifact","reason":"The crispest condition — the controller runs the entire suite after every task, and RED precedes GREEN — is decided by the order and existence of prior events, not by a pattern in one artifact."},
  {"token":"S76","group":"C","reason":"Deciding whether at least one test 'enters where the user enters' and asserts on what the user gets out — versus injecting an intermediate structure at the seam — requires judging what the test's entry point means relative to the real user path."}
]
