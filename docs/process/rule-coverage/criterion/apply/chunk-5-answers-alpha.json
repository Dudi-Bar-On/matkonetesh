[
  {"token":"S77","group":"C","reason":"Whether `תנור` is used as a generic device word or legitimately means the oven category requires judging the meaning of the string in its domain context; mere presence of the word decides nothing."},
  {"token":"S78","group":"C","reason":"Deciding whether an agent verified the mechanism behind an it's-unrelated diagnosis — versus fabricating a convenient supporting claim — requires evaluating the meaning and honesty of an explanation against the code it names."},
  {"token":"S79","group":"none","cost":"A scheduled watchman component that exercises the real semantic-query path and runs REINDEX CONCURRENTLY on failure — new monitoring infrastructure, not a compliance check on any actor's action.","importance":"High — a corrupted vector index is silent, every presence gate stays green, and only a full-suite acceptance test caught it; but the entry itself is an incident record naming a gap, with no per-action compliance condition."},
  {"token":"S80","group":"C","reason":"Deciding whether every clock-, focus-, or scroll-dependent pixel in a new baseline has been pinned or masked requires understanding what the photographed screen renders and why — a judgement about content, not a nameable textual pattern in the image or diff."},
  {"token":"S81","group":"C","reason":"The rule is 'fix a false-alarming gate by making its measure precise, never by loosening it' — telling a precision fix from a loosening fix requires judging the intent and effect of the change to the gate."},
  {"token":"S82","group":"C","reason":"Presence of a named render path in DoD-5 evidence does not decide compliance — the reader can exist and still be dead; deciding that the path actually fires on the shipped data requires evaluating the evidence's meaning."},
  {"token":"S83","group":"A","artifact":"the edited Playwright spec file under tests/**","pattern":"the token `page.clock` present in the same spec file as a `new Date(` call outside any `page.evaluate` body — i.e., a Node-side clock read co-occurring with a pinned page clock","mechanism":"pretooluse:Edit|Write","mechanism_target":"tests/**","reason":"Both halves of the hazard are literal tokens in one file; their co-occurrence decides the sweep-is-needed condition without reading intent."},
  {"token":"S84","group":"C","reason":"Recognising that a pin is a deliberate exception, and judging whether its declaration, in-force test, still-needed test, and visible cost genuinely embody the ruling, requires reading the intent behind the dependency change — no textual pattern distinguishes a deliberate override from an ordinary pin."},
  {"token":"S85","group":"B","mechanism":"pretooluse:Edit|Write","mechanism_target":"implementation files (app.js|tests/**) edited under a spec-governed task","observed_prior_facts":"whether the governing spec file was Read (a prior Read tool call on the spec path) in this session before the first implementation edit — visible in the session's tool-use history / state store","reason":"Compliance is 'the spec was opened before implementing from it' — a question about a prior event, not about the edit's content."},
  {"token":"S86","group":"none","cost":"A meta-measurement harness that replays a real shift's traffic through each gate to prove it sees the common path — measuring the gates, not the work; nothing in a single action's payload decides it.","importance":"High — two green gates watched 105 non-compliant calls in silence; but the entry is a lesson about validating gates ('no findings is not compliance'), not a per-action compliance condition."},
  {"token":"S87","group":"none","cost":"Nothing to enforce: 'consistency with another artefact is not correctness' is a pure principle with no artifact, no prior facts, and no content whose evaluation would constitute compliance.","importance":"Medium — it names a real reasoning trap (two stale things agreeing), but it guides judgement rather than constraining any checkable action."},
  {"token":"S88","group":"A","artifact":"the edited Python test/helper file","pattern":"a broad exception handler — `except Exception:` or a bare `except:` — whose body calls `skip(` / `pytest.skip(`; absence of a positive marker (named exception types) on the excusing path","mechanism":"pretooluse:Edit|Write","mechanism_target":"tests/**","reason":"The forbidden shape — skip fed by a catch-all — is a literal structural pattern in the diff; its presence decides non-compliance without reading intent."},
  {"token":"S89","group":"C","reason":"Deciding whether at least one test genuinely exercises the entry point with no environment override requires understanding what each test actually spawns and with what env — the compliance condition lives in the tests' semantics, not in a nameable token."},
  {"token":"S90","group":"A","artifact":"the report/comment/commit text containing a quoted span attributed to a repo file path","pattern":"the quoted span checked for verbatim substring presence in the named file — a quote-plus-path pair whose quoted words do not appear in that file is the violation","mechanism":"stop","mechanism_target":"quote-with-path claims in the assistant's answer/report","reason":"Once a quote is attributed to a file, verbatim presence is a mechanical substring check; the rule itself demands exact words, not faithful paraphrase."},
  {"token":"S91","group":"C","reason":"Deciding whether each spec DoD line is genuinely MET by the pasted evidence — versus merely declared MET — requires judging the evidence against the spec's meaning; a present-but-hollow audit block satisfies any textual pattern."},
  {"token":"S92","group":"C","reason":"Judging whether a gate script distinguishes tool-absence from tool-failure and truthfully states what it ran against requires understanding the script's error-handling semantics — a code-quality judgement, not a token match."},
  {"token":"S93","group":"C","reason":"Deciding whether enforcement state is scoped to the right axis per counter (per-actor for §5, per-session for §10.16) and whether legacy rows were handled as a decision requires understanding the state store's keying semantics and each counter's meaning."},
  {"token":"S94","group":"C","reason":"Compliance is 'before blaming the system under test, verify your instrument measures what you think' — evaluating whether a verification tool was itself validated requires judging the meaning and adequacy of the debugging evidence."},
  {"token":"S95","group":"C","reason":"Recognising that a claim asserts 'this landed in git' and that its supporting evidence is a disk-fed projection rather than `git show HEAD` requires reading what the claim means and what its evidence actually attests — the check is mechanical only after that semantic identification."}
]
