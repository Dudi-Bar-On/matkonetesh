# מנה s01 — הפיצול הראשון: `L43` ⟵ `L43a` + `L43b`

**אושר על-ידי הבעלים 9.8.26 ("מאשר") — הצעת הפיצול, עם התיקון שלו ל-`L43a`.**

זו **מנת-הבוחן** של קשת הפיצול. היא נעשית לבדה, לפני שמונת הפיצולים הנותרים, כדי שהמעגל המלא
— מסמך ⟵ מחלץ ⟵ `mk_rules` ⟵ מראה ⟵ שער — יוכח על כלל אחד לפני שהוא מופעל על תשעה־עשר.

## מה הכלל המקורי נשא, ולמה הוא היה מורכב

`L43` נקב **במפורש** בשני שערים באותו טקסט: "השווה בתים, לא גליפים" — שיפוט על אופן החקירה —
ו"עריכה סקריפטית היא מקור לשחיתות" — הנחיית כלים. שתי המסווגות העיוורות נחלקו עליו בדיוק שם.

## התיקון של הבעלים

ההצעה המקורית ל-`L43a` הייתה **לחסום `sed -i`**. נדחתה: היא חוסמת כלי לגיטימי ואינה מזהה את הפגם.
הפגם היה **בייט בקרה בלתי-נראה** שנכנס לקובץ — ולזה יש דפוס ישיר, זול, וללא תוצאות-שווא.

| מזהה | קבוצה | מנגנון | הנימוק |
|---|---|---|---|
| **`L43a`** | `A` | `ci-gate` · יעד `קובצי מקור במעקב git` | **ארטיפקט:** תוכן הקובץ. **דפוס:** בייט C0 שאינו tab/LF/CR — `[\x00-\x08\x0B\x0C\x0E-\x1F]`. אפס תוצאות-שווא: טקסט אמיתי אינו מכיל אותם |
| **`L43b`** | `C` | — | השופט מעריך האם, כשקוד שנראה זהה התנהג אחרת, בוצעה **השוואת בתים** לפני היפותזות נוספות; והאם עריכה סקריפטית אומתה **בהרצה** ולא ב-grep |

## מה אובד אם נאכוף רק את `L43a`

הסריקה תופסת את הבייט **אחרי** שהוא נכנס, לא את ההרגל שמכניס אותו. משמעת השוואת-הבתים
ואימות-בהרצה — ליבת הלקח — נשארת אצל השופט. **וזו בדיוק הסיבה לפצל ולא להכריע:** אילו סיווגנו
את `L43` כולו כ-`A`, החצי הזה היה נקרא כמכוסה בלי שאיש בודק אותו.

## מה יוחל

```json
{
 "approved_by_owner": "2026-08-09",
 "entries": [
  {
   "rule_id": "L43a",
   "rule_group": "A",
   "mechanism": "ci-gate",
   "mechanism_target": "tracked source files"
  },
  {
   "rule_id": "L43b",
   "rule_group": "C"
  }
 ]
}
```
