# מנה s04 — `L81` ⟵ `L81a` + `L81b`

**אושר על-ידי הבעלים 9.8.26 ("מאשר תמשיך").**

## למה זו מנה ולא החלטה חדשה

הלקח `L81` נכתב היום, אחרי שהבעלים תיקן היקף: קשת אכיפה אינה פותחת שאלת תוכן. השער החדש
(`check-rules-classified`) חסם מיד — כלל בלי קבוצה אינו נכנס לקורפוס — ולכן הוא עבר את הנוהל העיוור.

**שתי המסווגות נחלקו:** אלפא `C`, בטא `A`. וזו **בדיוק הצורה המורכבת** שהכרעת עליה ב-9.8.26:
חצי אחד נושא דפוס נקוב (רשימת ההחרגות שכל שער חייב לשאת), והשני דורש שיפוט (מה עושים עם ממצא
שנמצא מחוץ לתחום). **לא הכרעתי ביניהן — יישמתי את המדיניות שלך ופיצלתי.** זו הפעם הראשונה
שהמדיניות מופעלת על כלל חדש במקום על כלל ישן, וזה מה שהיא נועדה לעשות.

| מזהה | קבוצה | מנגנון | הנימוק |
|---|---|---|---|
| **`L81a`** | `A` | `ci-gate` · יעד `scripts/check-*.mjs` | **ארטיפקט:** קובץ השער. **דפוס:** שער שנתיבי הסריקה שלו כוללים `docs/sources/`, ‏`data.py` או `sources.py` בלי החרגה מפורשת. זה מה שבטא נקבה בו, והוא עומד בחובת ההוכחה של ש1 |
| **`L81b`** | `C` | — | השופט מעריך האם ממצא שנמצא **מחוץ לתחום הקשת** נרשם והושאר, או נגרר להכרעה בקשת שאינה שלו. זה מה שאלפא נקבה בו, והוא דורש שיפוט על היקף |

## מה אובד אם נאכוף רק את `L81a`

הרשימה תישמר בקוד, אבל **הנטייה לגרור ממצא לקשת הלא-נכונה אינה ניתנת לדפוס** — היא בדיוק מה
שקרה כאן, ובלי `L81b` היא הייתה נשארת בלי בעלים.

```json
{
 "approved_by_owner": "2026-08-09",
 "entries": [
  {
   "rule_id": "L81a",
   "rule_group": "A",
   "mechanism": "ci-gate",
   "mechanism_target": "scripts/check-*.mjs scan roots"
  },
  {
   "rule_id": "L81b",
   "rule_group": "C"
  }
 ]
}
```
