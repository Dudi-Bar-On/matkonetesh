# HANDOVER — paste this into the new session

> ⚠️ **2026-08-05 — `graphify` was removed from this project.** Any instruction below that names it, `graphify-out/`, `/graphify` or `check-graph-fresh` is a **record of what was done at the time**, not something to run. The live equivalents are:
> `python scripts/memsync.py` (ingest, delta by content hash) · `--query "<text>"` / `--tool <name>` (search) · `python scripts/memenrich.py` (embeddings, never blocking) · `node scripts/check-memory-fresh.mjs` (the gate). See discipline §10.11–§10.13.


You are continuing work on **מתכונת · מדריך האש** at `C:\Users\dudib\source\repos\matconetesh`.
The previous session ran two days and ended at 8% context. Everything below is committed; nothing is lost.

## 0. FIRST, BEFORE ANYTHING ELSE (owner's standing instruction)

1. Read `CLAUDE.md` (root) — the always-in-context loader.
2. Read `docs/process/development-discipline.md` — **start at §10 (owner's standing instructions) and §3 (the DoD gate)**, then skim §11's lesson log **L1–L22** and "Adopted wins". This is mandatory every task, not once.
3. Read `docs/analysis/program/MASTER-ONBOARDING.md` — the single index: audit corpus, 141 gaps by subsystem, the four activity arcs, the live queue, and a reading order.
4. Then `docs/analysis/program/P0-kickoff-brief.md` — the gap reconciliation + P0's quoted scope.

**Verify first (two agents were finishing as the session ended):** `git log --oneline -8` and `git status` — confirm the **P0 spec** (`docs/superpowers/specs/2026-07-24-p0-app-spoken-safety-design.md`) and the **graph scope-fix** (root `.graphifyignore` + refreshed `graphify-out/graph.json`) are committed. If either is uncommitted or half-done, finish it before new work. Also check `git rev-list --left-right --count origin/main...main` — push if commits are ahead and the tree is clean.

## 1. Where the product stands

- **מהדורה 261 is LIVE** and verified (Cloudflare Pages, matkonetesh.pages.dev): the AI model-selection registry shipped, running **gemini-3.6-flash** (text) + **gemini-3.1-flash-tts-preview** (TTS). Rollback = flip the commented `textLegacy` pin in `GEM_MODELS` (`app.js`).
- **The test suite is 439 tests in ~54s**, `workers: 20`, certified 7/7 clean. It uses a **warm-page architecture** (`tests/_fixtures.ts`: one pre-loaded page per worker, `seedApp` = localStorage seed + reload) and serves `/index.html` via `route.fulfill` from memory — that last part is the cure for a **Windows/Chromium loopback connection serialization** bug (see `docs/research/flake-refactor-rootcause.md`). Don't "simplify" it away; an L19 firing-guard test proves it stays active.
- **Serena is live** (8 languages, 207 files indexed) — §10.17: use it for symbol-shaped work on `app.js` (~9.6k lines). **graphify**: local graph + global KB (7,068 nodes) — §10.11 (query global before the web), §10.13 (graph before grep).

## 2. THE IMMEDIATE NEXT STEP

**P0-app.** The design is owner-approved and the spec was just drafted. The pipeline's next gate is **the owner reviewing the spec file** — do NOT start planning or coding before that.

Approved design (do not re-litigate):
- **Spoken safety guard** on `vcAskAI` + `vcTranslateToEn`: detect safety numbers → resolve the entity **active-cook items first, catalog fallback** → **speak the APP's verified value**; no match → strip the number, give qualitative guidance + spoken redirect. **Invariant: no model-originated safety number is ever voiced.**
- Fix the safety-number guard's **unit-blindness (F→C)** *and* its **range-phrase gap** (both proven by eval).
- **Conditional `google_search`** (COGS $1.22→$0.39), **TTS managed routing**, **`addDays` DST bug**, **false cross-event warning**.
- ONE spec, phased: **A = safety (items 1–3)**, **B = utility riders (4–6)**.

Evidence backing it: `docs/analysis/program/eval/comparison-2.5-vs-3.6-2026-07-24.md` — the model still leaks ungrounded safety numbers on **both** models (B11: 12→9 numbers; Hebrew B3-02: 12→5), numeric-safety leaks went 3/16→4/16, and **no cost signal exists** (`app.js` never reads `usageMetadata`).

After spec approval: `writing-plans` → `subagent-driven-development` (fresh implementer + reviewer per task).

## 3. Owner working preferences — learned the hard way, obey them

- **Long-running executions go to SUBAGENTS**, never the main thread — a held shell makes the session look stuck. Bundle adjacent small steps (edits, commits, checks) into the subagent's brief. Main thread = coordination, ledger, owner conversation, short commands.
- **Show progress as work advances** — report at every task/agent boundary, not only at the end.
- **Never declare anything final without owner approval** (memory: `fight-for-the-goal-and-ask-before-final`). A safe interim is fine — label it interim.
- **Don't downscope a stated goal** — invent + research (§10.14/§10.15) instead of settling.
- **§10.18 debug-then-measure**: a failure STOPS the measurement train; root-cause it, then measure. Campaigns certify stable systems; they don't diagnose.
- **Canary middle values** when debugging (timeouts AND worker counts) so the defect still fires; generous settings mask it.
- Instruct implementer subagents: run tests **inline (foreground)** and **commit before returning** — never background-and-wait (they terminate and the work is lost).
- **§11a**: never kill a suite mid-flight; every setup owns a verified teardown (port refuses + 0 orphans).
- **§10.16**: close every arc with lessons (§11 log) + deposit useful docs into the graphify global.

## 4. The live queue after P0

- **Tier 2 (ride along with P0):** gap **R11** (the footer still claims "data is local, no network" — `build.py:334`), the **app heap leak** (~2.5 MB/reload, newly registered gap), capture **`usageMetadata`** so AI cost is answerable.
- **Tier 3:** GPU queue — find a capable local extraction model (qwen2.5-coder:7b was unusable, 14b timed out), wire graphify's Ollama backend, then embedding semantic search. Worker runtime model-override (awaits the managed tier).
- **Monitor only:** certification runs 3–5 were ~30 s slower but clean — recorded, undiagnosed.
- **Micro:** one discipline bullet still headlines a stale `15_000` navigation timeout (config is `20_000`).

## 5. Ground truth files

`.superpowers/sdd/progress.md` — the durable ledger (gitignored, append-only; read its tail for the full chronology).
`docs/research/flake-refactor-rootcause.md` · `docs/research/measurements/m1b-capacity-probes-2026-07-23.md` — why the suite is built the way it is.
`docs/superpowers/specs/2026-07-22-gap-closing-program-charter.md` — the 141-gap programme (11 subsystems, 10 phases).
Assistant memory: `~/.claude/projects/C--Users-dudib-source-repos-matconetesh/memory/` (MEMORY.md is the index).

**Status line:** `~/.claude/statusline-command.sh` now shows model · repo · branch/unpushed/dirty · context bar · **focus** (`.claude-focus` at repo root — keep it current) · cost. It no longer depends on `jq` (which isn't installed).

---

## ADDENDUM — final state at session end (both in-flight agents landed)

- **P0 spec IS committed** (`06b5ef4`, 924 lines) — line numbers verified against HEAD via Serena (the
  charter's citations were stale by ~+85 lines). The 3.6 eval landed mid-draft and was folded in.
  **Four things in it need your explicit confirmation on review** (the drafter flagged them honestly, they
  are NOT part of your original six decisions): (1) item 2's scope **expansion** to the range-phrase gap
  (`"30-40%"` extracts only `40`, never `30` — a real second defect, in extraction not comparison);
  (2) the new **item 7** `usageMetadata`-capture rider (without it, item 3's COGS claim is unverifiable);
  (3) the proposed **Hebrew guard copy** (needs a native-speaker DoD-9 pass);
  (4) item 3's `AI_SEARCH='auto'` trigger condition — the drafter's design choice, not your instruction.
- **Graph refresh COMPLETE and healthy** (not partial — an earlier note in this file said otherwise; this
  supersedes it): **4547 → 5009 nodes**, **846 `app.js` code nodes** (guard was ≥800), edges 14268 → 15351,
  health check clean (0 dangling/missing/collapsed). Root + `docs/` `.graphifyignore` now exclude
  mockups/scratch/.claude/.serena/test-results/dist/images/raw-measurement data. Three verification queries
  surfaced this session's work top-ranked. **Not yet in the graph:** a few docs created during its own
  ~50-min run, and the new P0 spec — the next routine incremental `--update` (NEVER `extract`) picks them up.
- **Two real graphify bugs were found and fixed by that agent — carry them into the §10.16 lessons pass:**
  (1) a `.graphifyignore` rule cascades by **filesystem ancestry**, not by which scan invoked it — a root-file
  `docs/` rule silently collapsed the docs scan 51→0; (2) `build_merge()` replace-on-re-extract matches by
  `source_file` only, **ignoring `file_type`** — an AST-only code refresh would have destroyed 12 files'
  existing semantic content. Same silent-destruction class as the `extract`-prunes-the-graph hazard.
- **Third agent to hit the background-and-wait trap this session.** When dispatching: state
  "run every command in the FOREGROUND, never background a job and end your turn — you will terminate and
  the work is lost. Commit before returning."
- **Unpushed commits exist** — `git rev-list --left-right --count origin/main...main`, then push when clean.
- **FIRST INFRA TASK (owner instruction, §10.17a): wire ONE shared Serena server.** Today every subagent
  spawned its own `serena start-mcp-server` + dashboard (4 concurrent instances, ports 24282→24283 flapping,
  8 language servers duplicated each). Fix: read Serena's docs/`--help`, start ONE long-lived server on a
  fixed port with the SSE/streamable-HTTP transport, and change `.mcp.json` from the `command`/`args` stdio
  form to a URL-based server. **Verify:** exactly one `serena` process, ONE dashboard port, tools resolve
  from inside a subagent, project stays activated across agents. Until then, enable Serena only for agents
  doing genuinely symbol-shaped work.

### Final commit state at handoff
`769d7d8` §10.17a one-Serena rule · `81f00ae` graph refresh doc · `18f490e` .graphifyignore ·
`5add700` handover addendum · `06b5ef4` **P0 spec** · `446818b` handover · `cc19658` 3.6-flash eval.
Working tree clean except intentionally-untracked scratch/mockups. **Unpushed commits exist — check
`git rev-list --left-right --count origin/main...main` and push once verified.**

### The three things to do first, in order
1. Push the pending commits (after a quick `git log` sanity read).
2. **Wire the single shared Serena server** (§10.17a) — first infra task, checklist above.
3. **Owner reviews the P0 spec** (`docs/superpowers/specs/2026-07-24-p0-app-spoken-safety-design.md`),
   confirming the four flagged items → then `writing-plans` → `subagent-driven-development`.
