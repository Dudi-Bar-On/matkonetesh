# v290 — אימות על ה-URL החי (§10.10)

**נבדק:** 2026-08-03 · `https://matkonetesh.pages.dev/` · Playwright, ‏390×844, קליקים אמיתיים.

## 1 · מדבקת הגרסה

```
.foot-stamp  →  "מהדורה 290 · 3.8.26"        ✅ תואם לגרסה שנבנתה ונשלחה
```

**הפצה לקחה ~5 סבבי סקירה** (‏Cloudflare Pages בונה מהמקור). ‏4 הסבבים הראשונים החזירו 289 —
לא הוכרז "חי" עד שהמדבקה התהפכה, לפי §10.10.

## 2 · בדיקת פיצ'ר — לא רק המדבקה

נטען בדפדפן, נפתח "🔥 שאל את האש", מצב **⚡ מנוע מקומי**, הוקלד "האם תירס בטוח":

```
תירס    → "הבטיחות של הפריט הזה אינה נקבעת בטמפרטורת ליבה — הוא נאכל לפי
           מרקם וטעם, ואין לו טמפ׳ בטיחות."          ✅ ללא מספר
בריסקט  → "טמפ׳ בטיחות 63°C."                        ✅ בדיקת-נגד: הערך האמיתי נשמר
```

**סריקה מלאה על ה-build החי** — כל שורת `cuts` עם `safe` שקרי, מול הרגקס `/טמפ׳ בטיחות\s*\d/`:

```
stillBroken: []        ← 0 מתוך 27
```

חזותי: `mockups/v290-live-corn-390x844.png`.

## 3 · הערה על שיטת הסקירה — ליקוי בכלי שלי, לא במוצר

ה-probe הראשוני שכתבתי החזיר `citedSafeC: true` כבר בסבב שבו המדבקה עוד אמרה 289 — דבר
בלתי-אפשרי, שכן הפונקציה נוצרה באותו קומיט. **לא מצאתי לכך הסבר מבוסס ראיה ואיני ממציא אחד.**
מה שכן נקבע: הבדיקה הקובעת נעשתה **בדפדפן על התשלובת החיה**, ושם הכל תואם. בנוסף, הביטוי
`c.safe||63` עדיין מופיע בתשלובת — **בתוך הערת הקוד שמצטטת את הבאג**, לא בקוד עצמו.
**הלקח לשימוש חוזר: probe מבוסס `includes()` על טקסט מקור סופר גם הערות, ולכן אינו probe.**

## 4 · מה שוחרר

- **R-82** — מנוע השאלות המקומי המציא 63°C ל-27 שורות ירקות/פירות. סגור.
- **R-74** — תיקון 48 טענות הבטיחות, שהוקפא 2.8.26, **הופשר בהוראת בעלים 3.8.26**. שוחרר.
- **R-77** — מאגר המקורות נחת ברפו. אינו משנה ערך במוצר.
