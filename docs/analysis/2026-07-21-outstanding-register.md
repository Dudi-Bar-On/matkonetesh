# Outstanding register — verified against current source

**Audit date:** 2026-07-21 (post-hoc, against build v257 / commit `f8e9bb7`)
**Method:** every claim below was checked by reading current `app.js` (9457 lines), `app.css`, `equipment_map.py`/`data.py`, and `tests/*.spec.ts` directly — not the ledger (`.superpowers/sdd/progress.md`), which is itself confirmed stale/incomplete for Phase 3 (no Phase-3 entry exists in it despite Phase 3 having shipped and tagged v257). Where a test exists, it was treated as evidence only if it asserts an **observable effect** (rendered output, stored state, a value a real consumer reads) — a passing test that only asserts a computed field is not treated as proof of DONE, per the project's own DoD rule (`docs/process/development-discipline.md` §3, DoD-4/DoD-5).
**Sources enumerated:** `docs/analysis/2026-07-21-refactoring-report.md` (§7 plan items 1–24, §9 walkthrough defects), `2026-07-21-walkthrough-defects.md`, `2026-07-21-requirements-conformance.md`, `2026-07-21-scheduling-architecture.md`, `2026-07-21-hebrew-terminology.md`, `2026-07-21-device-visualization.md`, `2026-07-21-nitrite-dosing-threshold.md`, plus the Phase 2/4 specs in `docs/superpowers/specs/`.

Legend: **DONE** — implemented + has a behavioural test. **PARTIAL** — some of the requirement exists; the rest is named. **NOT DONE** — absent, or present-but-unread (computed-and-unread counts as NOT DONE per the project's own process rule).

---

## 0. SAFETY-bearing items (read this section first)

| ID | Requirement | State | Evidence | Notes |
|---|---|---|---|---|
| S1 | Cure-dosing scale guard (nitrite dosing safety) | **DONE** | `cureScaleGuardHTML` `app.js:1831-1856`, thresholds `hardMax=5*d`, `advMax=20*d` — matches the derived NIST HB44 rule in `2026-07-21-nitrite-dosing-threshold.md` exactly. Reads the owned scale's readability via `scaleReadability()` (`app.js:1818-1826`), fail-safe default when no scale registered. Wired into all 3 cure-calculator paths (`app.js:1881,1893,1902`). Tests `tests/cure-scale-guard.spec.ts` G1-G10 — run live, 10/10 pass, assert visible DOM state. | Owner-approved deviation (Waiver Gate correctly invoked): guard reads the *live computed dose* against the *owned scale's* readability, not `spec.scale_res` directly. `spec.scale_res` itself still only feeds a separate static display hint (`equipSpecNote`, `app.js:6153-6161`) — confirmed to render on cuts, specials, and makes/charcuterie alike. The safety mechanism is real and independent of that static hint. |
| S2 | `occupancyCompat.setpoint` fenced as advisory-only | **DONE** | Code fence `app.js:377-380`. Sole reader is a display chip (`_occHeaderHtml`, `app.js:532`). Test `tests/setpoint-fence.spec.ts` SF1-SF3 — SF2 proves `cookerContention` still detects the temp clash while never mutating a stage's `temp` — run live, 3/3 pass. | |
| S3/D6 | A second same-class cooker must not silently make an item invisible to clash detection | **PARTIAL** | `cookerFor` (`app.js:242-254`) still returns `null` for 2+ ambiguous same-class devices; `cookerContention` (`:264`) and `deviceOccupancy` (`:406,442`) still `if(!d) return;` — the item is still **excluded** from contention/occupancy math. What changed: a separate "awaiting cooker assignment" advisory now renders instead of total silence (`app.js:5797-5806`, tests `tests/cooker-ambiguity.spec.ts` A1-A4, run live 4/4 pass). | The audit's original instruction was "ambiguous items must **stay in contention**" — that is still not true; only the silence was fixed. A second smoker still makes the item vanish from the actual clash detector, with a separate, easy-to-miss banner as the only mitigation. |
| — | Runtime enforcement that no move ever alters `hours`/`temp`/`kind`/order on a `smoke`/`cook`/`sv`/`bcheck` stage (scheduler safety boundary, scheduling-architecture.md §6.4: *"Run on every accepted schedule, in production, not only in tests"*) | **NOT DONE** | `equipPlan` only ever writes `fuelNote`/`refuelEveryMin` (structurally narrow, `app.js:955-968`) and `schedulePlacements` only ever writes new `startMs`/`endMs` of unchanged length (`app.js:3001-3085`) — both are safe **by construction today**, but no named runtime guard (`safetyDiff`-equivalent) exists anywhere (`grep` for `safetyDiff`/`assert`/`invariant`: zero hits). Enforcement is test-only (`tests/equipplan-seam.spec.ts` P3d, `tests/scheduler-placement.spec.ts` B3). | Low risk today because so little can move a stage yet — but the spec explicitly asked for a production invariant, not test coverage, precisely so the guard survives a future engineer extending the scheduler without having read the spec. |
| — | `cookerCandidates()` gates a device by its max temperature (`propOf(dev,'maxC')`) before proposing it | **NOT DONE** | `cookerCandidates` (`app.js:233-238`) filters by category/type only; `maxC` is captured (`propOf`, declared on 5 categories) and never read by `cookerCandidates`, `cookerFor`, `equipPlan`, or `schedulePlacements` (confirmed by two independent greps). | A 150°C recipe can still be scheduled on a 135°C-max electric smoker with no flag — this is a real thermal-safety-adjacent gap (audit's own framing, "must be flagged not scheduled"), not merely a UX nicety. |
| — | Plan-depth model safety commitment: refuse to schedule poultry/charcuterie without a registered thermometer | **NOT DONE** | `gearThermoNote()` (`app.js:1008-1012`) only renders an advisory note; it never blocks scheduling. No refusal-to-schedule logic exists anywhere (confirmed by grep sweep). | Explicitly still outstanding — 0% of the plan-depth model shipped (see §5 below). |
| — | Plan-depth model safety commitment: cure task BLOCKS (not just warns) without a registered 0.1 g scale | **NOT DONE** | S1 above is an advisory/hard-warn (`data-cureguard="hard"`), not a hard block on task completion. No scale-registration gate blocks the cure task itself. | Same document, same 0% status. |

---

## A. Refactoring-report §7 plan — items 1–24

### Phase 0 — Safety (do first, small)
| # | Item | State | Evidence |
|---|---|---|---|
| 1 | S1 — wire `scale_res` guard | **DONE** | See §0 above. |
| 2 | S2 — fence `setpoint` | **DONE** | See §0 above. |
| 3 | S3 — fix D6 | **PARTIAL** | See §0 above — silence fixed, exclusion from contention not fixed. |

### Phase 1 — make the detect layer honest
| # | Item | State | Evidence |
|---|---|---|---|
| 4 | H1 — unknown footprint → `known:false`/`null`, never `0` | **DONE** | `itemOccupancy`, `app.js:357-358`: `cm2=(fp!=null && !isNaN(Number(fp))) ? Number(fp) : null`. Tests `tests/occupancy-unknown-footprint.spec.ts` U1-U4, run live 4/4 pass. |
| 5 | H2 — separate requirement (`max(min_bath_l)`) from consumption; stop summing minimums | **DONE** | `app.js:476-481`: `out.usedLitres=maxReq` (max, not sum), with a comment explaining the prior false-over bug. Tests `tests/occupancy-sv-volume.spec.ts` V1-V5, run live 5/5 pass. True displacement/bag-clearance modelling was deliberately never invented (correct, per the project's own "never invent a measurement" rule) — the vessel shows bag count + litres sentence, never a fabricated `%`. |
| 6 | H3 — hanging reads the device's own `canHang`/`hooks`; wire `hooksOver` to a real warning; delete the masking fixture | **DONE** | `deviceCanHang(dev)` (`app.js:319`) reads device props, not a generic `hooks` accessory token (zero remaining uses of `equipOwnsToken('hooks')` for gating). `hooksOver` has 3 live occurrences: computed (`:504`), CSS state class (`:634`), user-facing overflow text (`:650`). Tests `tests/occupancy-hanging.spec.ts` H3a-H3d, run live 4/4 pass; masking fixture (separate hooks accessory) removed. |
| 7 | H4 — `deviceOccupancy` returns `slots[]`, chronological arrival-order packer, percentage becomes a claim about slots that exist | **DONE, with a residual detector gap** | `deviceOccupancy` returns `out.slots[]`; UI reads per-slot `%` (`app.js:464`), not whole-device. Deterministic packer confirmed by test. Oven added as a rack-based device (`cookerCandidates`, `app.js:237`). Tests `tests/occupancy-slots.spec.ts` S1-S7 + `tests/occupancy-oven.spec.ts` OV1-3, run live 11/11 pass. **Gap found in this audit:** `cookerContention`'s area-clash branch (`app.js:274`) still reads the whole-device `o.over` (`app.js:483-484`), not `slotOver`/`fit.verdict`. For the exact regression scenario the occupancy view now correctly flags (an item that fits no single shelf), `cookerContention` reports **zero clash** because the whole-device total is well under capacity. The occupancy *diagram* is honest; the work-plan's "Cooker clash" banner — the surface a user is more likely to see — is not. This is the same class of defect the original report described (a truthful number in one place, a false "fine" in the adjacent one), now shifted one layer down. |
| 8 | Terminology: תנור→מכשיר/מכשירים; shared plural helper; stop aliasing zones into racks | **PARTIAL** | תנור→מכשיר: **DONE**, all 6 distinct strings / 8 call sites confirmed changed and test-fenced (`tests/hebrew-cooker-term.spec.ts` H1-H3, run live 3/3 pass, H2 scans the whole rendered doc for every old phrase). Racks/zones mislabel: **DONE** (`occupancyDevHtml` now reads `EQUIP_CATS[cat].capHe/capEn`, `app.js:400,530`; test `occ-view-grill.spec.ts` pass). Shared plural helper: **NOT DONE** — no such helper exists anywhere (`heCount`/`Lc(`/`plural`: zero hits); the 4 originally-named broken sites are **still broken today**, verbatim: `occupancyDevHtml` slot label (`app.js:530`), `chipsFor` (`app.js:6282`), probe-channel footer "ערוצים" (`app.js:6318`, three tokens after a *correctly*-pluralized probe count on the same line), wood-reload "פעמים" hint (`app.js:5888`). No test asserts correct n=1 rendering anywhere. |

### Phase 2 — device diagrams (depends on 7)
| # | Item | State | Evidence |
|---|---|---|---|
| 9 | Draw the container, then place items (cabinet=N shelves incl. empty, grill=side-by-side zones, bath=vessel+bags) | **DONE** | `deviceSilhouette` dispatcher (`app.js:323-329`) branches by `dev.cat`/type; 4 structurally distinct templates (`_occCabinetBody`/`_occOffsetBody`/`_occGrillBody`/`_occVesselBody`). Tests `occ-silhouette.spec.ts`, `occ-view-{bay,cabinet,grill,offset,vessel}.spec.ts`, all run live and pass. |
| 10 | Hanging renders suspended, shelves stay empty below | **DONE** | `_occBayHtml` (`app.js:624-634`) is a separate overlay channel; `_occCabinetBody` always draws all `cap.slots` regardless. Test H3d asserts `emptyShelves===4` while 2 items hang. |
| 11 | Honesty ladder: solid=measured, dotted=present-but-unmeasured, empty=free; shelf count may be drawn without area, shelf assignment may not | **DONE** | `_occTile` (`app.js:550-558`), `.occ2-dashed{border-style:dashed}` (`app.css:1658`); `out.fit` model (`app.js:490-499`, `areaMeasured`/verdict ok-tight-over). Test `occ-fit-ladder.spec.ts`, 3/3 pass. |
| 12 | Fix RTL gradient; stop `innerHTML`-per-scrub-frame; retire/replace test W5 | **PARTIAL** | RTL gradient: **DONE** — no `linear-gradient(90deg,…)` remains anywhere; replaced with solid `flex:0 0 <frac>%` tiles + `dir="ltr"` numeric islands (7 occurrences, confirmed correct). `innerHTML` full rebuild per scrub frame: **NOT DONE** — `app.js:718-735`, `paint()` still does `body.innerHTML=occupancyViewHtml(...)` on every `input` event; this was a Phase-2 non-goal per the spec, not a regression, but it is unfixed. Test W5: **not literally retired**, but rewritten to target the new `.occ2-tile` markup and now passes for the *right* reason — the old `.occ-item{min-width:104px}` class is gone entirely, replaced by proportional `flex:0 0 <frac>%` with no width floor. One specific recommendation was not carried through: `.occ2-tile`'s `min-height:30px` is a fixed px value, not `--fscale`-scaled, so the original concern (clipping at 1.3–1.4× text scale) remains untested — W5 still only runs at default scale. |

### Phase 3 — the `equipPlan` seam
| # | Item | State | Evidence |
|---|---|---|---|
| 13 | `equipPlan(meta, methodKey, stages, scope)` — pure, no-op with no kit, may enrich never change a stage | **DONE**, one sub-part convention-only | `app.js:955-968`. No-op confirmed (`app.js:957`, test P3d `same:JSON.stringify(out)===JSON.stringify(stages)` true). Purity confirmed (shallow-copies before writing, test P3d `mutatedInput` false). "May never change hours/temp/kind/order" is **true today only because the function's body literally only sets 2 new fields** — enforced by tests (`equipplan-seam.spec.ts` P3d lines 95-96), not a runtime invariant (see §0 above). |
| 14 | D1 — one source of truth for preheat | **DONE** | `PREHEAT` table `app.js:920-927` feeds both `preheatMinutes()` (the schedule) and `preheatHint()` (the label) via `_preheatRow()` (`:928-933`) — cannot drift. Pellet = 15 in both places. Tests `equipplan-seam.spec.ts` P3b/P3c assert the schedule and label agree and neither says "45 דק׳" for a pellet. |
| 15 | D3 — fuel from `dev.fuel`, not the recipe | **DONE** | `equipPlan` reads `DEVICE_FUEL[dev.type]` (`app.js:946-950,963`) via the actually-owned, `cookerFor`-resolved device; feeds the "add fuel" task (`app.js:5761,5764`). Test P3f. (Recipe `wood`/`coal` fields still feed a separate "recommended wood flavor" line — different concern, not a regression.) |
| 16 | D4 — refuel cadence per device type | **DONE** | `REFUEL_MIN` table (`app.js:939-945`: stick-burner 45, WSM/drum 90, kettle 60, pellet/kamado/gas/electric/cabinet 0) drives real scheduled tasks (`app.js:5757-5768`). Test P3f asserts stick-burner produces refuel tasks, pellet produces none. |
| 17 | Hung vs. laid durations diverge where the data supports it | **NOT DONE (explicit, by design)** | Confirmed zero mount-mode branching anywhere in `itemStages`/`equipPlan`. The Phase-3 commit message itself states this was deliberately skipped: "the data does not carry separate hung durations, and a cook duration is exactly the thing this layer may not invent." Ribs hung vs. laid still show one duration regardless of mount mode. |

### Phase 4 — the scheduler refactor
| # | Item | State | Evidence |
|---|---|---|---|
| 18 | Extract `planSchedule()` to a top-level pure function; delete the duplicate walk | **DONE** | `planSchedule()` at `app.js:2960-2974`; both `buildList` (`:5581`) and `combinedEventsRows` (`:7743`) call the same function — the duplicate copy is gone. Tests `scheduler-planschedule.spec.ts` P1-P5. |
| 19 | Stage data model gains earliest-start/latest-finish/shiftable/interruptible | **PARTIAL** | `latestFinish`/`slack` fields exist (part of `planSchedule`'s relaxation output). No `shift:'fixed'\|'elastic'\|'floating'` classification and no `interruptible` flag were found anywhere in `app.js` (not confirmed present by either verification pass). |
| 20 | Backward serial schedule-generation, priority = least-slack→largest-demand→fewest-devices→stable key; per-device feasible-window sweep | **PARTIAL** | `schedulePlacements` (`app.js:3001-3085`) sorts by `slackMs → -demandCm2 → tid` (`:3029-3031`) — 3 of the 4 named criteria confirmed; "fewest feasible devices" tiebreak not confirmed present. The window sweep only searches earlier windows **on the single already-resolved device** (`cookerFor` runs once, before placement, `:3007`) — it is not a sweep *across* candidate devices, because device reassignment doesn't exist (see item 21). |
| 21 | Repair ladder (share→hang→re-assign→shift→hold→advise), limited-discrepancy search | **NOT DONE**, one rung DONE | Only "pull earlier on the same device, bounded" is implemented (`SCHED_PULL_MAX_MS=2h`, `app.js:2987`, enforced `:3075-3079`, advisory `_schedAdviceHtml` wired at `:5632`; tests `scheduler-placement.spec.ts` C1-C3). **Reassign to another device: NOT DONE** (device is fixed before placement). **Share/batch as an active move: NOT DONE** (only the pre-existing "don't double-count a shared bath" check exists; nothing actively groups items). **Hang as an automatic move: NOT DONE** (hang-mode is fixed by `itemOccupancy` upstream, never toggled by the scheduler). **Hold (pull-earlier + refrigerate): NOT DONE**, explicitly gated off — code comments (`app.js:2984-2986`, `3070-3071`) still say "until the hold/refrigerate spine exists (Phase 3) the model cannot state where it waits," written in present tense even after Phase 3 shipped, because Phase 3 built only fuel/preheat/refuel enrichment, not a hold spine. No limited-discrepancy search exists — there is exactly one schedule ever produced, never multiple candidates scored. |
| 22 | Lexicographic objective, never a weighted sum | **NOT DONE (moot — no multi-candidate objective exists yet)** | `schedulePlacements`' sort is a single-pass item-processing priority order, not an objective comparing whole candidate schedules. This requirement cannot be assessed as met or violated because the thing it would govern (a repair-ladder search producing multiple candidates) does not exist yet. |

### Phase 5 — remaining specified work
| # | Item | State | Evidence |
|---|---|---|---|
| 23 | Consumption-layer Slice B (charcuterie: cylinder loads, nozzle selection, grind, vacuum-liquid) | **NOT DONE (0%, confirmed)** | Final grep sweep: 3 total `Math.ceil(` in `app.js`, none related to stuffer cylinders; `grind_mm` zero occurrences in app.js; `casing_mm` display-only; `bagKind` zero consumers beyond its own declaration. |
| 24 | C2 probe channels; the plan-depth model (49 requirements incl. 2 safety commitments) | **NOT DONE (0%, confirmed)** | `probeChannels()` still exactly 2 call sites (definition `app.js:229`, footer `app.js:6318`) — unchanged since the original audit. `planDepth`/`mk-pref-depth`: 0 occurrences. Detail toggle still 2 chips (Short/Full, `app.js:5817`), not 3. Both safety commitments confirmed absent (see §0 above). |

---

## B. Walkthrough defects (docs/analysis/2026-07-21-walkthrough-defects.md)

| ID | Defect | State | Evidence |
|---|---|---|---|
| C1 | Shopping list ignored guest count, disagreed with Print menu by 2.7× | **DONE** | `rawGramsFor()` (`app.js:4619-4626`) is now the single source of truth, reading the *live* menu's guest count (not a wizard-dependent cached map). `shopData()` calls it directly (`app.js:2577,2588,2596`); `crossEventShopData` falls back to it too (`:7793`). Tests `tests/cart-quantity.spec.ts` Q1-Q6 (seeded via raw `mk-menu`, i.e. exactly the guided-wizard-only path that originally broke) — Q1 asserts ~3.7kg not 5.5kg, Q2 asserts cart==print-menu equality. |
| C2 | Auto cooker-assignment double-books sous-vide while grill sits idle | **NOT DONE** | Root cause (M1) is unfixed, and the new scheduler does not compensate: `schedulePlacements` only reshuffles *time* on an already-resolved device (`cookerFor`, `app.js:242-254`, resolved purely by stage `kind`, no notion of home-screen category) — it never reassigns an item to a different device class. Conflicts are detected and shown as advisory text only, never auto-resolved — matches the walkthrough's own note that "the suggestion is correct but is not applied automatically." |
| M1 | Grill cuts (picanha, kebab) default to sous-vide+smoke | **NOT DONE** | `methodRules()` (`app.js:798-812`), line 807-808 unchanged: every item with `doneness` still defaults to `['sv','smoke']` with `allowed` including `'grill'` but never chosen by default. No branch checks the home screen's own Grill categorization. No test covers `methodRules`/picanha/kebab defaults. |
| M2 | Equipment form silently discards invalid values | **DONE** | (Phase 0 Task 4, commit `cf9458a`.) `doSave` validates numeric property fields via `propParse` before saving; invalid → toast + `.eq-invalid` flag + form stays open + prior value preserved (not wiped). Tests M2a/M2c/M2d. |
| M3 | Wizard category filter chips can't be deselected by a second tap | **NOT DONE** | `app.js:7098`: click handler still unconditionally does `cwActiveCat=el.dataset.cwcat||null`, no toggle-off branch when the same chip is already active. No test covers `cwActiveCat` or chip deselection. |
| M4 | English mode leaves the Events screen partially untranslated (badge, 2 buttons, delete-all, mixed-language date) | **DONE** | `app.js:7888` (status badge), `7893-7894` (both action buttons), `7901` (delete-all) all now route through `L(he,en)`. Month-name mixed-language bug also fixed — `toLocaleDateString` now switches locale with app language (`app.js:7885`). Traced to commit `0061fc9` (i18n coverage pass, v187). No dedicated Events-English test exists, but the code is unambiguous. |
| N1 | Timeline date field clips the year at 390px | **NOT DONE** | `app.css:555-556`, `.calcrow input,.calcrow select{width:120px}` — the timeline's date input still inherits this fixed width, unchanged. |
| N2 | "1 אירועים" — wrong Hebrew plural for exactly 1 event | **NOT DONE** | `app.js:7866`: `` `${list.length} ${L('אירועים','events')}` `` — simple concatenation, no singular branch. Same class of bug as the plural-helper gap in item 8 above; no test covers it. |
| N3 | Non-ISO date string assigned to a native `<input type="date">`.value (console warning) | **DONE** | Field rests as `type="text"` with a Hebrew placeholder and only flips to `type="date"` on focus/when a value exists (`app.js:7335-7343,7958`); `.value` is now set only inside branches where `type='date'` was just set, and the stored value is always ISO. Test `tests/wizard-date-locale.spec.ts` D1-D5, D5 specifically asserts no non-ISO string ever reaches a date input. (v252, commit `834d3f1`.) |

---

## C. Device-property consumption gaps (requirements-conformance.md D1–D11 + follow-ons)

All items below were re-checked directly against current `app.js`/`equipment_map.py` and are **confirmed unchanged from the original 2026-07-21 audit** — none were incidentally fixed by the Phase 0–4 work that landed since, which touched rendering/occupancy honesty but added no new gating logic for these specific properties.

| ID | Requirement | State | Evidence |
|---|---|---|---|
| D8 | `casing_mm` (71 recipes) should gate against stuffer `cap.nozzles` (largest ≤ casing, warn if none fits) | **NOT DONE** | `casing_mm` has exactly one consumer: `equipSpecNote` (`app.js:6158`), pure display chip text. Zero comparison against `nozzles` anywhere. |
| D9 | Vacuum edge-seal vs. liquid marinade should warn "freeze first" | **NOT DONE** | `bagKind` (`app.js:81-82`) has zero consumers beyond its own field declaration. No liquid/marinade-vs-seal-type check exists. |
| D11 | Cooler/cambro hold-advice gated on ownership | **NOT DONE** | Advice now lives in `_copilotPaceHtml` (`app.js:5323-5340`) and `copilotAdviceLocal` (`5342-5353`) — moved from the original locations, functionally unchanged: neither checks `equipOwnsToken('cooler')` or any equipment ownership before recommending "hold in a cooler." |
| — | `maxC`/`lid` capability gates | **NOT DONE** | Confirmed independently by two verification passes: zero uses of `propOf(dev,'maxC')` or `propOf(dev,'lid')` in `cookerCandidates`, `cookerFor`, `equipPlan`, or `schedulePlacements`. A 150°C recipe is still freely assignable to a 135°C-max device. (Also listed in §0 as safety-adjacent.) |
| — | `grind_mm` (59 recipes) → smaller batches + chill-between-passes | **NOT DONE** | Zero occurrences in `app.js`. Captured Python-side (`equipment_map.py`) at build time, never read by the frontend. |
| — | `kg`/`shape` (130 recipes) feeding occupancy, and guest-count scaling of grate-area demand | **NOT DONE** | `itemOccupancy` reads only the static, catalog-baked `spec.footprint_cm2` (computed at Python build time from unscaled catalog `kg`). C1's guest-scaling fix (`rawGramsFor`) is never called from `itemOccupancy`/`packDevice`/`deviceOccupancy` — an 8-guest and a 20-guest event still demand identical grate area for the same cut. This is the same gap scheduling-architecture.md §5.3 flagged, still true after C1 shipped. |
| — | `curechamber` capacity model (27 recipes, hang-only) | **NOT DONE** | Category defines only `tempC`/`rhPct` props, no area/volume/hooks prop; `deviceCapacity` has no branch for it, so capacity is always 0/unknown. Pure display token. |
| — | Coarse vertical-clearance gate for `long` vs `short` hung items | **NOT DONE** | `_hang_class()` correctly classifies long/short server-side and the value is threaded into `spec.hang`, but `itemOccupancy` (`app.js:356`) only uses it as a truthy gate — every hung item consumes exactly `hooks:1` regardless of class; the long/short distinction is carried into data and never read again. |
| — | Probe-channel budgeting (leave-in vs. spot-check by concurrent monitored items) | **NOT DONE** | Same as Phase-5 item 24 above — `probeChannels()` still has only its definition + one footer-line consumer. |

---

## D. Phase 3a orchestrator remnants (requirements-conformance.md §1.2)

These predate the refactoring-report's 24-item plan (they belong to the earlier `2026-07-17-cookout-orchestrator-phase3a-design.md`) but remain live requirements documents with unmet DoD lines.

| Requirement | State | Evidence |
|---|---|---|
| `orchestrate()`, `movesForClash()`, `applyMove()`, `safetyGate()`, `holdCapMin()`, `dangerZoneMin()` | **NOT DONE** | Zero occurrences of all 6, confirmed by direct grep against current `app.js`. |
| `PREF_PRESETS` / `prefPreset()` (Simple/Balanced/Pro preset selector) | **NOT DONE** | Zero occurrences. A plan task that referenced this was marked complete in the ledger with the function never actually added — a pre-existing ledger-accuracy problem, still true today. |
| `PREFS` orchestrator knobs: `autonomy`, `shareTolC`, `woodSwap`, `holdEnabled`, `aiRank`, `slotModel`, `holdMaxH` | **NOT DONE (computed-and-unread by design)** | Registered at `app.js:6694-6714` with an explicit code comment: "intentionally render NO hub UI yet (no consumer = no dead controls)." Zero consumers anywhere else in the file. This is the exact failure mode the project's own process doc calls out (DoD-5) — the difference here is it is *labelled* as intentional, but the requirement (a working preference that does something) is still unmet. |

---

## E. Device-visualization polish items explicitly out of Phase 2's shipped scope

Per `docs/superpowers/specs/2026-07-21-occupancy-view-phase2-spec.md`, these were never committed to for Phase 2 (not in its acceptance tests or DoD) even though they appear in the fuller `2026-07-21-device-visualization.md` design. Listed here so they don't silently disappear from the register.

| Item | State | Notes |
|---|---|---|
| Time-density ribbon + ±20min ghosting (predictive "coming off soon" states) | **NOT DONE** | No plan exists; `deviceTimeline()` (the additive model function the design calls for) was never built. |
| Tiles as tappable `<button>`s opening the cooker-assignment sheet directly | **NOT DONE** | Tiles render as static elements in the diagram; the only tap-target work is unrelated to the diagram itself. |
| Keyed DOM reconciliation instead of full `innerHTML=` rebuild per scrub frame | **NOT DONE** | See item 12 above — confirmed still present. |
| `--fscale`-aware tile sizing (vs. fixed `min-height:30px`) | **NOT DONE** | See item 12 above — text-scale clipping at 1.3–1.4× remains untested. |
| Definite/indefinite title consistency ("תפוסת המכשירים" vs "תפוסת מכשירים") | **NOT DONE (cosmetic)** | Panel title still definite, toolbar button still indefinite — the terminology report's Rule/finding #3 was only half-applied. |
| Deferred device properties: `zoneMix` (direct/indirect grill zones), per-rack non-uniform dimensions, rotisserie as a third occupancy channel | **NOT DONE** | No plan exists; explicitly deferred pending new device properties, per the design doc itself. |

---

## Summary

Everything in §0 (safety), §A Phase 4 items 19–22, §A Phase 5 items 23–24, §B (5 of 9 walkthrough defects: C2, M1, M3, N1, N2), §C (all 9 items), §D (all 3 items), and §E (all 6 items) is outstanding. Phases 0–3 and most of Phase 1–2's honesty work are genuinely delivered with real tests, confirmed independently in this audit — the project's execution quality on what it *did* build is good. The gap is unchanged in character from the original report: the parts that make equipment change *the plan itself* (device reassignment, sharing, hanging-as-a-move, holding, temperature/fuel capability gates, guest-count-scaled occupancy demand, the entire plan-depth and consumption-Slice-B bodies of work) remain almost entirely unbuilt, one layer further in than before.
