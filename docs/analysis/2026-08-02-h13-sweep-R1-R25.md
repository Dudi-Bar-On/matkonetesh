# H13 Relevance Gate — סריקה מרוכזת של R-1..R-25 (23 שורות)

**תאריך:** 2026-08-02 · **מבצע:** סוכן טריאז' · **מקור השורות:** `docs/ROADMAP-2026-07-30.md` §5a
**היקף:** R-1, R-4..R-25 (‏23 שורות; ‏R-2/R-3 כבר נסגרו בפסק בעלים 31.7).
**מה זה כן:** חצי-החקירה וחצי-ההמלצה של H13 (‏discipline §16 שלבים a+b).
**מה זה לא:** אין כאן החלטה. שלב (c) — ‏**ASK THE OWNER** — הוא חובה, והוא לא בוצע. לא שונה שום קובץ
מלבד המסמך הזה, לא נעשה commit, לא הורצה הסוויטה ולא הופעל דפדפן Playwright (טריאז' סטטי + git בלבד).

**שיטה:** לכל שורה — קריאת הטקסט המלא, איתור הסימבול בקוד של היום (מספרי שורות מ-30.7 מיושנים ולא נסמכתי
עליהם), בדיקה מול `docs/analysis/2026-07-22-ULTIMATE-knowledge-and-gaps.md`, ו-git log. כל פסק נושא
`file:line` או hash שנקרא בפועל.

---

## טבלת סיכום

| R | נושא בשורה אחת | פסק | פעולה מומלצת | גודל |
|---|---|---|---|---|
| R-1 | שער content-fidelity/אלרגנים לפני תרגום המוני | STILL-OPEN | keep-open-and-schedule | M |
| R-4 | scoping דליפת ה-heap | STILL-OPEN (חלקית לא-נמדדת) | keep-open-and-schedule | M |
| R-5 | בר-ההשוואה PRE-4 + עלות thinking + כלכלת-יחידה | ALREADY-DONE ‏(2 מ-3) | close-as-done + שורה צרה חדשה | S |
| R-6 | מודל plan-depth — 47 דרישות | NEEDS-OWNER-DECISION | escalate-to-owner | — |
| R-7 | שארית C8b — overrides משך/מדף/חימום | STILL-OPEN | keep-open-and-schedule | M |
| R-8 | ברירת-מחדל sv+smoke לנתחי גריל | STILL-OPEN | keep-open-and-schedule | S |
| R-9 | צ'יפ קטגוריה באשף בלי toggle-off | STILL-OPEN | keep-open-and-schedule | S |
| R-10 | אימות-קליק לטופס גריל — zones/area | ALREADY-DONE | close-as-done | — |
| R-11 | חבילת הפוליש של תצוגת-המכשירים | OBSOLETE (חלקי) + שארית | escalate-to-owner (ניסוח מחדש) | M |
| R-12 | AI cache/dedup + in-flight guard | STILL-OPEN | keep-open-and-schedule | S |
| R-13 | מחליף 3 המצבים beginner/home/pro | NEEDS-OWNER-DECISION | escalate-to-owner (המלצה: obsolete) | — |
| R-14 | החלפת אייקון 🧫→⚗️ | STILL-OPEN | keep-open-and-schedule | S |
| R-15 | ‏3 ציטוטי `cold:True` בכבש | STILL-OPEN | keep-open-and-schedule | S |
| R-16 | משטח שלמות פרופיל-ציוד | STILL-OPEN | keep-open-and-schedule | M |
| R-17 | פריטי-תהליך graphify | ALREADY-DONE (חלקי) → צמצום | keep-open-and-schedule (מצומצם) | S |
| R-18 | מפרט home-screen של R7 | STILL-OPEN | keep-open-and-schedule | S |
| R-19 | Photo Analyze בלי downscale | STILL-OPEN — **המניע הופרך** | keep-open-and-schedule (מנוסח מחדש) | S |
| R-20 | `overscroll-behavior: contain` נעדר | STILL-OPEN | keep-open-and-schedule | S |
| R-21 | ‏bottom nav מתעלם מ-safe-area | STILL-OPEN | keep-open-and-schedule | S |
| R-22 | אין `-webkit-tap-highlight-color` + `transition:all` ×2 | STILL-OPEN | keep-open-and-schedule | S |
| R-23 | מסך-ריק של Voice Cook הוא משפט מת | STILL-OPEN | keep-open-and-schedule | S |
| R-24 | re-extract בגרנולריות-claim של W1-* | STILL-OPEN — **המדד לא משוחזר** | keep-open-and-schedule | M |
| R-25 | pass יישוב-מסמכים | STILL-OPEN | keep-open-and-schedule | S |

**ספירה:** STILL-OPEN 19 · ALREADY-DONE 2 (‏R-10 מלא, R-5 חלקי-מהותי) · NEEDS-OWNER-DECISION 2 ·
OBSOLETE-חלקי 1 (‏R-11, נספר גם ב-STILL-OPEN כשארית) · UNDETERMINED 0 (ראו הסתייגות ב-R-4).

---

## R-1 · שער content-fidelity/אלרגנים — STILL-OPEN (M)

הרתמה היחידה שקיימת היא `scratch/translate-eval/gates.mjs` (‏381 שורות). הייצואים שלה:
`loadShippedGuard`, `foldFractions`, `mtSafeFolded`, `hebrewLeak`, `UNIT_FAMILY_RULES`,
`extractNumUnitPairs`, `unitLiteralCheck`, `SAFETY_LEXICON_GROUPS`, `safetyLexiconCheck`
(`scratch/translate-eval/gates.mjs:29,50,57,63,172,243,267,318,360`).
חיפוש `allergen|recall|fidelity` בקובץ → 0 התאמות מלבד הערת-כותרת. השער שפסק הבעלים 24.7 קבע
(recall ‏≥0.90 בטיחות / ≥0.70 כללי) **אינו קיים**. השורה מדויקת כלשונה.
**החמרה שלא הייתה בשורה:** הקובץ יושב תחת `scratch/` שאינו מנוהל ב-git — כלומר גם השערים שכן קיימים
אינם חוזה מנוהל. ה-ROADMAP עצמו כבר מכיר בזה (שורה 139, "חוזה-שערים קבוע").
**המלצה:** keep-open-and-schedule. זה תנאי-כניסה חוסם ל-Language Thread (‏ROADMAP:137) — 20 שפות
תלויות בו. הערך הגבוה ביותר בכל הסריקה.

## R-4 · scoping דליפת ה-heap — STILL-OPEN (M), עם הסתייגות מפורשת

`git log -i --grep="scoping|heap|leak|memory"` על כל הענפים לא מחזיר שום commit שביצע את ה-scoping.
הפריט היחיד שקרוב הוא `1d6ca93` — "fix(voice): attach a handler to gemSpeak's lookahead prefetch at
creation, closing an unhandledrejection leak" — דליפת promise, לא דליפת heap, ולא בעקבות מדידה.
**ההסתייגות:** אימתתי ש-**המשימה** (scoping) לא בוצעה. לא אימתתי ש-**הדליפה** עדיין קיימת — זה דורש
מדידת heap חיה בדפדפן, שנאסרה עליי במפורש במשימה הזאת. הנתון "‏~2.4-2.8MB לכל reload" הוא מ-24.7 ולא
נמדד מחדש מאז, ומאז שונו שכבות שלמות (‏Voice Wave 0, streaming, i18n ×23).
**המלצה:** keep-open-and-schedule — אבל השלב הראשון של המשימה הוא **מדידה מחדש**, לא תיקון.

## R-5 · PRE-4 + עלות thinking + כלכלת-יחידה — ALREADY-DONE (2 מ-3)

השורה אורזת שלושה דברים. שניים בוצעו:
1. **בר-ההשוואה של PRE-4 — הורץ.** ‏`18e23ba` "eval(PRE-4): close the comparative regression bar — run
   CLOSED, numeric-safety axis STILL OPEN" (24.7), דרך `.github/workflows/eval.yml`, run 30117127600,
   13/13 ירוק. גם `cc19658` "eval(baseline): gemini-3.6-flash CI scorecard + comparison vs 2.5-flash".
2. **עלות/התנהגות thinking על payload אמיתי — נמדד.** ‏`39a20a0` + `bcf9d81` (‏R-50, 1.8): 204 קריאות
   API חיות, 17 שאלות × 4 רמות × 3 חזרות; תוצר `docs/analysis/2026-08-01-thinking-floor-measurement.md`
   ‏+ `2026-08-01-thinking-latency-is-the-bottleneck.md`.
3. **רענון כלכלת-היחידה — לא בוצע.** אין מסמך עלויות חדש; `docs/analysis/sweep/W4-A-unit-economics.md`
   עדיין מבוסס תמחור `gemini-2.5-flash` שנשלף ב-22.7, בעוד הייצור עבר ל-3.6-flash. גם ההנחה
   `thinkingBudget:0` "בכל אתר קריאה" שהמסמך נשען עליה סותרת את R-50 (‏ask מוצמד ל-low).
**המלצה:** close-as-done על (1)+(2) — ולפתוח שורה צרה חדשה (S) רק ל-(3): רענון W4-A לתמחור 3.6-flash
ולמצב ה-thinking בפועל.

## R-6 · מודל plan-depth (47 דרישות) — NEEDS-OWNER-DECISION

בקוד: `grep "planDepth|depthPref|mk-depth"` על `app.js` → 0 התאמות. המודל לא נבנה, כטענת השורה.
אבל זו לא שאלה הנדסית: זו פסיקת close-or-commit על 47 דרישות מוצר לא-בטיחותיות. השורה כבר נחתה נכון
כמשימת-דיון BR-2 (‏`docs/ROADMAP-2026-07-30.md:370`).
**המלצה:** escalate-to-owner. אין מה "לאמת" — צריך הכרעה.

## R-7 · שארית C8b — overrides משך-שלב/מדף/חימום — STILL-OPEN (M)

החימום היום הוא ערך **גלובלי אחד** הנגזר מהמכשיר: `_preheatRow()` ‏(`app.js:1283`),
`preheatMinutes()` ‏(`app.js:1289`), `preheatHint()` ‏(`app.js:1290`); הצרכן היחיד
`app.js:9150` ‏(`const _pmins=preheatMinutes()`) מזין את קלף התזמון, ההתראה ושורת התוכנית
(`9173`, `9181`, `9194`, `9364`). אין נתיב override למשתמש.
`grep "override"` ב-`app.js` מחזיר את שער-הציוד בלבד (`app.js:1349`) — אין override למשך-שלב ואין למדף.
**המלצה:** keep-open-and-schedule (‏Phase 12 כפי שנקבע).

## R-8 · ברירת-מחדל sv+smoke לנתחי גריל — STILL-OPEN (S)

מספר-השורה בשורה (`app.js:999`) מיושן; הסימבול הוא `methodRules(c)` — היום `app.js:1157`.
```
app.js:1166  if(c.doneness) return {allowed:['sv','smoke','grill'], def:['sv','smoke'], minOne:true,
app.js:1169  return {allowed:['sv','smoke','grill'], def:['sv','smoke'], minOne:true,
```
כל נתח בעל `doneness` — כלומר בדיוק הנתחים דמויי-הסטייק (פיקנייה/קבב) — מקבל `def:['sv','smoke']`.
היחידים שקיבלו טיפול ייעודי הם ירקות (`1158`, ‏`def:['grill']`) והאיברים הפנימיים (`1160-1165`).
הבאג השורשי של C2 עומד בעינו.
**המלצה:** keep-open-and-schedule (‏Phase 2, עבודת methodRules).

## R-10 · אימות-קליק לטופס גריל — zones/area — ALREADY-DONE

השורה טוענת ששדה-הקיבולת של טופס גריל-הפחם לא אומת מעולם בקליק אמיתי. זה כבר לא נכון:
`tests/equipment-walkthrough.spec.ts:139` ‏("FORM manual: category matrix, manual add, custom sub-type,
cancel, back") **צועד בקליק אמיתי בכל 8 קטגוריות המכשיר** (‏`152-166`), ולכל אחת קובע
`HAS_CAP` ומאשר `expect(hasCap > 0)` על `#eqCapKey`+`#eqMultiWrap` — עבור `grill` הציפייה היא `true`.
מפתח-הקיבולת של גריל הוא `capKey:'zones'` והוא **מוגדר ברמת הקטגוריה** (`app.js:80`) ולא לפי תת-סוג,
לכן הבדיקה על `eqCat='grill'` מכסה גם את "פחם".
שדה ה-area מכוסה בנפרד ובקליק אמיתי ב-`tests/e1-area-field.spec.ts:24-33` (‏`page.click('[data-eqedit]')`
ואז אימות ש-`#eqProp-areaCm2` קיים יחיד ו-`#eqvArea` הוסר).
**המלצה:** close-as-done.

## R-9 · צ'יפ קטגוריה באשף בלי toggle-off — STILL-OPEN (S)

מספר-השורה (`app.js:9067`) מיושן; הצ'יפ באשף הוא היום `app.js:11452-11456`:
```
app.js:11456  chips.querySelectorAll('[data-cwcat]').forEach(el=>el.addEventListener('click',
              ()=>{ cwActiveCat=el.dataset.cwcat||null; cwCont=''; cwPaintPicker(); }));
```
השמה, לא toggle — הקשה שנייה על אותו צ'יפ משאירה אותו דלוק.
**להשוואה — הקטלוג כן מבטל:** `app.js:2695` ‏`activeCats.has(c)?activeCats.delete(c):activeCats.add(c)`.
כלומר שני משטחי-צ'יפים באותה אפליקציה מתנהגים הפוך; זו חוסר-עקביות ולא רק חוסר-פיצ'ר.
**המלצה:** keep-open-and-schedule.

## R-11 · חבילת הפוליש של תצוגת-המכשירים — OBSOLETE (חלקי) + שארית תקפה

**מה התיישן:** `deviceTimeline` — הסימבול שהשורה בנויה סביבו — **אינו קיים בקוד**. הארכיטקטורה הוחלפה:
היום `deviceOccupancy(devId, tMs, computed, scope)` ‏(`app.js:736`) הוא מקור-האמת היחיד,
`occupancyDevHtml(o)` ‏(`app.js:837`) מרנדר אותו ותו לא ("the diagram and the clash advisories must never
be able to disagree", `app.js:833-834`), ו-`occupancyViewHtml` ‏(`app.js:1029`) הוא המשטח. זו תצוגה
צורת-מכשיר פר-קוקר (‏Phase 2) ולא סרט-זמן. "‏±20 דק' ghosting על deviceTimeline" כבר לא ניתן לביצוע כפי
שנוסח.
**מה נשאר תקף מילולית:**
- אין reconciliation — `_occWire` מצייר מחדש בכל scrub: `app.js:1090` ‏`body.innerHTML=occupancyViewHtml(...)`.
- אין `zoneMix` — ‏`grep zoneMix app.js app.css` → 0.
- רוטיסרי קיים כתכונה בוליאנית בלבד (`app.js:89` ‏`{key:'rotisserie', kind:'bool'}`) ואינו מיוצג בתפוסה.
- אריחי הגריל מרונדרים כ-`div` בתוך `occ2-zones` ‏(`app.js:942`) ולא כ-`<button>` פותח-שיוך.
**המלצה:** escalate-to-owner — לא לסגור ולא לתזמן כלשונה. השורה צריכה **ניסוח מחדש** מול משטח ה-occupancy
של היום. שימו לב שה-ROADMAP כבר מחזיק שורה נקובה נפרדת לסקרבר ב-Phase 12b (`ROADMAP:130`) — יש סיכון
לכפילות.

## R-12 · AI cache/dedup + in-flight guard — STILL-OPEN (S)

`gemFetch(model, body, opts)` ‏(`app.js:6078`) הוא שער-הרשת היחיד. הוא מחזיק timeout, backoff
(`app.js:6088-6089`), נפילה מ-managed ל-BYOK (`6098`) ורישום שימוש (`6096`) — אבל **אין בו מפה של
תשובות ואין דגל in-flight**. `grep "inFlight|aiCache|_aiCache"` ב-`app.js` מחזיר רק הערות dedup בהקשרים
אחרים (`2084`, `5917`, `8778`, `13394`). פתיחה-חוזרת של פאנל תשלח שוב את אותה קריאה.
**המלצה:** keep-open-and-schedule (‏Phase 9 כפי שנקבע). ראו הערת-ההצלבה ב-R-19 — עלות הקריאה נשלטת
ברובה בדמי-בקשה של `google_search`, ולכן dedup הוא כאן חיסכון אמיתי, בניגוד ל-downscale.

## R-13 · מחליף 3 המצבים — NEEDS-OWNER-DECISION (המלצה: obsolete)

**גרסה מצומצמת של הרעיון כבר חיה ומשולחת.** בקוד היום:
`UI_LEVELS` + `uiLevel()` ‏(`app.js:11176`), `setUiLevel(l)` ‏(`app.js:11177`),
פאנל-בחירה `openUiLevel()` ‏(`app.js:11182`) עם כפתורי `[data-lvl]` ‏(`11198`),
צורת-תוכנית פר-רמה `LEVEL_SHAPE={beginner:'5', mid:'1', pro:'3'}` ‏(`app.js:11172`),
כרטיס-הכרות בהפעלה ראשונה (`app.js:11226`), מחלקות-גוף `lvl-beg/lvl-mid/lvl-pro` ‏(`app.js:1149`),
ונקודת-כניסה קבועה בתפריט ההגדרות: `'🧭', 'רמת ממשק — מתחיל/בינוני/מתקדם', 'openUiLevel'`
(`app.js:13929`).
מסמך-המקור עצמו כבר אמר את זה: `2026-07-22-audit-product.md:154` מסווג את UXR-1 כ-
**"NOT DONE / SUPERSEDED-in-spirit"**, ומוסיף "existing `mk-uilevel` (RV-4) covers a narrower version of
the same idea". מה שלא נחת הוא רק ה-UI המוצע (‏table-mode / beginner-tile) ושמות המצבים (‏home מול mid).
**המלצה:** escalate-to-owner עם המלצה מפורשת ל-**close-as-obsolete**: המנגנון קיים, והיתרה היא הצעת-עיצוב
נפרדת ולא חוב. ‏BR-3 (`ROADMAP:371`) צריך להיות מנוסח מחדש כשאלת-עיצוב, לא כ-close-or-commit על מנגנון.

## R-14 · החלפת אייקון 🧫→⚗️ — STILL-OPEN (S)

`grep "⚗️"` על `app.js`/`app.css`/`data.py` → 0 התאמות. ‏🧫 עדיין בשימוש ב-20+ אתרים, בהם התפריט הראשי
(`app.js:10695`, `11800`, `13928`), כותרת המזווה (`app.js:5072`) וכפתורי הציר (`app.js:9471`).
מסמך-המקור מסווג את זה "genuinely still open" (`2026-07-22-audit-product.md:156`) והתמונה לא השתנתה.
**המלצה:** keep-open-and-schedule (‏Phase 12b craft pass) — טריוויאלי.

## R-15 · שלושה ציטוטי `cold:True` בכבש — STILL-OPEN (S)

שלושתם קיימים כלשונם ב-`sources.py`:
- ערך **35** (‏"ThermoWorks — Reverse-Seared Leg of Lamb", `sources.py:1063`) →
  `sources.py:1094` ‏`'smoke': {'t': 60, 'h': '1-2', 'cold': True}`
- ערך **36** (‏"AmazingRibs — Rack of Lamb", `sources.py:1103`) →
  `sources.py:1132` ‏`'smoke': {'t': 55, 'h': '0.5-1', 'cold': True}`
- ערך **60** (‏"Vindulge — Grilled Lamb Chops (loin)", `sources.py:1214`) →
  `sources.py:1241` ‏`'smoke': {'t': 55, 'h': '0.5-1', 'cold': True}`
כל שלושתם מסומנים `cold: True` בטמפ' 55-60°C — מעל כל תקרת cold-smoke מקובלת. ההערות שלהם אכן מזכירות
"‏Observe the 4h danger-zone rule during smoke" (`sources.py:1138`, `1247`), אבל התיוג עצמו לא תוקן.
**המלצה:** keep-open-and-schedule. **הערת DoD-10:** כל נגיעה כאן היא נגיעה בערך בטיחות — המשימה חייבת
לצאת ממקור ראשוני (Baldwin/USDA), לא מפרשנות, ולעבור את שער אי-שינוי-הבטיחות במפורש.

## R-16 · משטח שלמות פרופיל-ציוד — STILL-OPEN (M)

`grep "completeness|eqProfileScore|profileComplete|שלמות"` על `app.js` → 0 התאמות רלוונטיות.
המחרוזת "דיוק" בקוד היא תכונת-מכשיר (`app.js:121`, ‏`{key:'accuracy', unit:'±°C'}`) ופרוזה של מחשבון
המלח — לא משטח-שלמות. הרמז הקרוב ביותר הוא תג "🔧 בדוק ציוד" ‏(`app.js:4274`), שהוא התראת-חוסר פר-מתכון
ולא מדד פרופיל. המשטח "ציוד: 3 · דיוק 40%" לא נחת.
**המלצה:** keep-open-and-schedule (‏Phase 9, ליד E5/E6).

## R-17 · פריטי-תהליך graphify — ALREADY-DONE חלקי → לצמצם

ארבעה תת-פריטים, שניים השתנו:
- **hook post-commit — נחת.** ‏`f5fd200` "prove incremental doc extraction is content-hash safe + wire
  post-commit trigger"; הקובץ קיים ומורשה-הרצה: `.git/hooks/post-commit` (‏9643 בתים, 31.7),
  והסקריפט `scripts/graphify-doc-trigger.sh`. ✔
- **קורפוס `methodology` עם מסמכים פרטיים של פרויקט אחר — עדיין שם.** ‏`graphify global list` מחזיר
  `methodology: 1947 nodes, added 2026-08-02`. המקור
  (`docs/analysis/program/MASTER-ONBOARDING.md:176`) מתאר אותו כ-"‏1,875 nodes, 0 keys leaked — flagged,
  not fixed"; היום 1947 — כלומר לא רק שלא הוסר, הוא גדל. ✗
- **סכנת headless-extract** (‏"documented, not hardened" באותה שורה) — לא נמצאה הקשחה. ✗
- **D12 (embedding-query) / D13 (upstream של ה-patch)** — קיימים כהחלטות ב-
  `docs/analysis/program/DECISIONS-2026-07-24.md:208` ו-`:219`; לא נמצא commit שמימש אותן. ✗
**המלצה:** keep-open-and-schedule בגרסה **מצומצמת** — למחוק את סעיף ה-hook מהשורה ולהשאיר את שלושת
הנותרים. הפריט הבולט הוא הקורפוס: זו החלטת בעלים (`graphify global remove methodology`) שכבר סומנה
"‏needs owner ok" במקור.

## R-18 · מפרט home-screen של R7 — STILL-OPEN (S)

הצ'רטר מאשר את הטענה מילולית:
`docs/analysis/2026-07-27-gap-closing-BIG-STATUS.md:69` — `| **P7** product surface | S9·S10 |
◻ **NOT STARTED** | no R7 home-screen spec; delivery shell + presentation tokens untouched |`.
בצד השני, סעיף Phase 12b ב-ROADMAP (`docs/ROADMAP-2026-07-30.md:128-133`) מפרט passes רוחביים
(‏audit → contrast → ARIA/landmarks → targets/labels → craft) ושורה נקובה אחת (סקרבר ה-occupancy) —
**ואין בו שורת-מפרט למסך-הבית**. הפער עומד.
**המלצה:** keep-open-and-schedule — אבל כמשימת-אימות בפתיחת Phase 12b (כפי שהנחיתה כבר קובעת), לא
כמשימת-בנייה. ודאות בינונית, כפי שהשורה עצמה מודה.

## R-19 · Photo Analyze בלי downscale — STILL-OPEN (S), **והמניע הופרך**

**העובדה ההנדסית נכונה.** ‏`openPhotoAnalyze()` ‏(`app.js:13808`) קורא
`rd.readAsDataURL(file)` ‏(`app.js:13819`) ושולח את ה-`dataUrl` הגולמי ל-`gemVision` ‏(`app.js:13823`).
‏`downscale(file)` ‏(`app.js:5178`) מחווט לצרכן אחד בלבד — היומן: `app.js:5172`
‏`if(f){try{e.photo=await downscale(f);}catch(err){}}`.
**אבל המניע שבשורה סותר פסק REFUTED.** השורה מצטטת את `sweep/W4-A-unit-economics.md` §5.7 כמקור, ומסגרת
המסמך היא עלות. ‏`docs/analysis/2026-07-22-ULTIMATE-knowledge-and-gaps.md:1129` פוסק על טענת W4:
*"‏'Photo analysis is an expensive feature, weight 3' (W4-B) | **Measured the cheapest feature**
($0.0014/call), weight 1."* כלומר **חיסכון-עלות אינו הצדקה** למשימה הזאת.
**המלצה:** keep-open-and-schedule — אבל **מנוסחת מחדש**: ההצדקה היא זמן-העלאה ברשת סלולרית וכשל
בקבצים גדולים, לא עלות. מי שירים את השורה כלשונה ייכתב לו יעד-הצלחה שכבר הופרך.

## R-20 · `overscroll-behavior` — STILL-OPEN (S)

`grep "overscroll"` על `app.css` **ועל** `app.js` → **0 התאמות**. לא `contain`, לא `none`, בשום מקום.
כל פאנל/sheet עדיין משרשר גלילה למסך שמאחוריו.
**המלצה:** keep-open-and-schedule (‏Phase 12b craft pass). שורה אחת של CSS.

## R-21 · bottom nav ו-safe-area — STILL-OPEN (S)

`app.css:320` ‏— `.cnav{position:fixed;bottom:0;left:50%;…}` — ‏`bottom:0` נטו, בלי
`env(safe-area-inset-bottom)`.
האידיום אכן קיים בקובץ ×3, במספרי-שורה מעודכנים (השורה נוקבת `:369/:374/:1438`):
`app.css:369` ‏(`.cactive-fab`, ‏`bottom:calc(74px + env(safe-area-inset-bottom,0px))`),
`app.css:377` ‏(`.panel-body`), `app.css:1474` ‏(`.eq-wrap`).
כלומר הפקד הקבוע והנוכח-תמיד הוא היחיד שלא מקבל את הטיפול שכל השאר מקבלים.
**המלצה:** keep-open-and-schedule (‏Phase 12b craft pass).

## R-22 · tap-highlight + `transition:all` — STILL-OPEN (S)

- `grep "tap-highlight"` על `app.css` → **0**. ההבזק הכחול של ברירת-המחדל נשאר על ממשק טרקוטה.
- `transition: all` ‏— בדיוק ×2, בדיוק בסלקטורים שהשורה נוקבת:
  `app.css:210` ‏`.cseas-item{…transition:all .12s}` · `app.css:1237` ‏`.mtoggle{…transition:all .15s}`.
**המלצה:** keep-open-and-schedule (‏Phase 12b craft pass).

## R-23 · מסך-ריק של Voice Cook — STILL-OPEN (S)

מספר-השורה (`app.js:6398`) מיושן; המחרוזת היום ב-`app.js:7752`:
```
:`<div class="shop-empty">${L('אין משימות — בנה תוכנית עבודה במתזמן ואז חזור.',
  'No tasks — build a work plan in the scheduler, then come back.')}</div>`;
```
זהו ה-fallback של הענף השני ב-`vcRender` — טקסט בלבד, בלי כפתור ובלי `data-vc` כלשהו. לשם השוואה,
בענף המלא כל פעולה היא כפתור מחווט (`app.js:7736-7739`, ‏`host.querySelectorAll('[data-vc]')` ב-`7753`).
המשתמש שמגיע למסך ריק מקבל הוראה ולא יעד.
**המלצה:** keep-open-and-schedule. הנחיתה בשורה היא "רוכב על חילוץ Voice Module" — אבל אחרי
Voice Wave 0 (‏v280-v285) התיקון הזה עצמאי לגמרי ואינו טעון חילוץ; שווה לשקול הקדמה.

## R-24 · re-extract בגרנולריות-claim של W1-* — STILL-OPEN (M), **המדד לא משוחזר**

**המהות תקפה.** מסמכי W1 מיוצגים בגרף כצומת-מסמך אחת: למשל
`{"label":"W1-A · Code Sweep — app.js, app.css, serve.js, build.py, worker/",
"file_type":"document","source_file":"analysis/sweep/W1-A-code.md","source_location":null,
"id":"analysis_sweep_w1_a_code"}`. ספירת הצמתים לפי `source_file` על `graphify-out/graph.json`
(‏6,818 צמתים סה"כ): W1-A ‏3 · W1-B ‏6 · W1-C ‏14 · W1-D ‏2 · W1-E ‏4 · W1-F ‏4 · W1-G ‏14 · W1-H ‏2 —
‏**49 צמתים סה"כ לשמונה מסמכים** שמכילים ~156 טענות. זו לא גרנולריות-claim.
**המדד שבשורה לא משוחזר.** השורה טוענת "‏133/156 קשתות ה-refutes נוחתות על מסמך שלם". בגרף של היום יש
**81** קשתות refutes בסך הכול, ומהן **1** נוחתת על צומת-מסמך חסרת `source_location`. הגרף עבר מאז שני
רענוני-deep (`b04163e`, `805999a`), ומבנה הקשתות השתנה. המספר בשורה מיושן.
**המלצה:** keep-open-and-schedule — אבל **עם המדד מתוקן**. מי שירים את זה חייב למדוד מחדש לפני שהוא
מגדיר יעד. הנחיתה (טריגר: רענון-הגרף המלא הבא) נכונה.

## R-25 · pass יישוב-מסמכים — STILL-OPEN (S)

ארבעת המסמכים היתומים קיימים כולם, ואף אחד מהם לא הוכרז superseded:
`docs/equipment-2.0-gaps-2026-07-15.md` · `docs/REVIEW-v147.md` · `docs/matkonetesh-modes-demo.html` ·
`docs/sources/agent-research-protocol.md` — ‏`grep -il "superseded"` על ארבעתם → **0 התאמות**.
המנגנון עצמו כן קיים ובשימוש במקומות אחרים (‏`docs/ai-strategy.md`, ‏`docs/analysis/2026-07-21-
requirements-conformance.md`, ‏`2026-07-22-audit-orchestrator.md`, ‏`COVERAGE-CHECK-2026-07-30.md`
ועוד) — כלומר לא חסרה קונבנציה, חסר pass.
**המלצה:** keep-open-and-schedule יחד עם R-24 (אותו טריגר).

---

## מה נדרש מהבעלים

לפי discipline §16 שלב (c), אף שורה כאן אינה מוכרעת עד שתוכרע **יחד** עם הבעלים. שלוש קבוצות:

1. **סגירה מיידית (המלצה חד-משמעית):** R-10 ‏(close-as-done) · R-5 ‏(close-as-done על שני שלישים,
   ופתיחת שורה צרה לרענון כלכלת-היחידה) · R-17 (סגירת סעיף ה-hook, צמצום השורה).
2. **הכרעת מוצר — לא ניתן להכריע הנדסית:** R-6 · R-13 (עם המלצה מפורשת ל-obsolete).
3. **ניסוח מחדש לפני תזמון:** R-11 (הארכיטקטורה הוחלפה) · R-19 (המניע הופרך) · R-24 (המדד מיושן).

השאר (13 שורות) הן keep-open-and-schedule כלשונן — כולן אומתו חיות בקוד של היום.
