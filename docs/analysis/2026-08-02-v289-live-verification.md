# אימות חי — ‏v289 · ‏`מהדורה 289 · 2.8.26`

**סטטוס:** אימות בלבד. לא שונה שום קובץ מקור, לא בוצע commit, לא הורצה סוויטת Playwright.
**תאריך:** 2026-08-02 · **סביבה:** `https://matkonetesh.pages.dev` (חי), Chromium headless, ‏390×844, locale `he-IL`.
**commit נבדק:** `249a0c4` (release v289) · **חותמת נקראה מה-DOM לפני כל ריצה:** `מהדורה 289 · 2.8.26`
(ריצה נעצרת אם החותמת אינה 289 — `lib.mjs` זורק `WRONG STAMP`).

`curl` נכשל במכונה הזו בשגיאת TLS revocation; הכול נעשה דרך דפדפן אמיתי.
‏Playwright-MCP אינו מוגדר בפרויקט (`.mcp.json` מכיל רק `serena`), ולכן הדפדפן הונע ישירות
דרך ספריית `playwright` המותקנת — **לא** דרך `npx playwright test`.

---

## שלב 0 (חוסם) — החותמת והסכימה שנשלחה בפועל

| בדיקה | תוצאה |
|---|---|
| `.foot-stamp` | `מהדורה 289 · 2.8.26` ✅ (הגיע ב-poll הראשון, ללא המתנה) |
| `typeof safetyClaimSchema` | `function` |
| `SAFETY_SUBJECT_NONE` | `"NONE"` |
| `safetyClaimSchema(true).properties.subject_category.enum[0]` | `"NONE"` ✅ |
| האם קיים איבר ריק ב-enum | **לא** (`hasEmptyMember=false`) ✅ |
| אורך ה-enum | 25 (‏`NONE` + 24 קטגוריות קטלוג) |
| ראש ה-enum | `["NONE","בקר","הודו","טלה","עוף","חזיר"]` |
| ‏`/SAFETY_SUBJECT_NONE\s*=\s*'NONE'/` ב-HTML המשוגר | נמצא ✅ |
| ‏`/enum:\s*\[''\]\.concat/` (התבנית השבורה של v288) ב-HTML המשוגר | **לא נמצא** ✅ |

מקור הפונקציה כפי שנקרא מהדף החי (`safetyClaimSchema.toString()`), verbatim:

```js
function safetyClaimSchema(withSubject){
  if(!withSubject) return SAFETY_CLAIM_SCHEMA;
  const props=Object.assign({}, SAFETY_CLAIM_SCHEMA.properties,
    { subject_category:{ type:'STRING', enum:[SAFETY_SUBJECT_NONE].concat(askAllCategories()) } });
  return Object.assign({}, SAFETY_CLAIM_SCHEMA, {properties:props});
}
```

**שלב 0 עבר.** הסנטינל המפורש נשלח, האיבר הריק איננו.

---

## שלב 1 (המכריע) — האם המסווג חי

**כן. המסווג חי ב-10 מתוך 10 הריצות. אין `api-400` אחד.**

* ‏`__vcClassCalls === 1` בכל ריצה — המסווג נקרא.
* ‏`vcClassifySafetyClaims` החזיר **Map אמיתי** בכל ריצה — `isNull=false`, `threw=null`. אף פעם לא `null`.
* גודל ה-Map: 1–3 claims לפי התשובה.
* רשת: כל הבקשות ל-Worker החזירו **200**. אפס תגובות ‏`>=400` בכל 10 הריצות
  (‏`POST /v1beta/models/gemini-3.6-flash:generateContent` → 200 — זו קריאת המסווג).
* ‏console: **ריק** מ-error/warn בכל הריצות. אין `[vcClassify]`, אין `api-4`.
* זמן המסווג: 5,522–9,618 ms.
* בנוסף — וזו העדות שהסכימה החדשה מתקבלת ולא רק "לא נדחית": השדה `subject_category` **חזר מלא**
  בכל ריצה (`בקר` / `טלה` / `עוף` / `נקניקיות`), כלומר Gemini קיבל את ה-enum ובחר ממנו ערך.

---

## שלב 2 — אסאדו ×3 · ‏**3 מתוך 3 עברו**

הקריטריון: ‏63°C של המדריך מוביל **וגם** יעדי המרקם של המודל (‏90–95°C) מופיעים כמספרים אמיתיים ולא `[…]`.

| ריצה | 63°C מוביל | יעדי המודל | `[…]` בתשובה | verdicts |
|---|---|---|---|---|
| run1 | ✅ | 90°C, 95°C | אין | `63°C→verified(63)` · `90°C→release` · `95°C→release` |
| run2 | ✅ | 90°C, 95°C | אין | `63°C→verified(63)` · `90°C→release` · `95°C→release` |
| run3 | ✅ | 93°C, 95°C | אין | `63°C→verified(63)` · `93°C→release` · `95°C→release` |

**המשפט המוביל, verbatim מה-DOM (`.vc-qa-a`):**

* **run1:** ‏`בשר בקר כמו אסאדו בטוח למאכל בטמפרטורה פנימית של 63°C לפי המדריך המאומת. לפחות.`
* **run2:** ‏`טמפרטורת הבטיחות המינימלית לנתח בקר כמו אסאדו היא 63°C לפי המדריך המאומת., עם מנוחה של שלוש דקות לאחר הבישול.`
* **run3:** ‏`טמפרטורת הבטיחות המינימלית לבשר בקר שלם כמו אסאדו היא 63°C לפי המדריך המאומת., בתוספת מנוחה של שלוש דקות מחוץ לאש.`

השוואה: ‏v287 = 7/8 · **v288 = 0/3** · **v289 = 3/3**. הרגרסיה נסגרה.

> **פגם קוסמטי שנצפה ואינו חלק מהקריטריון (מדווח, לא מתוקן):** הזרקת סמן האימות שוברת את הפיסוק —
> ‏`...המאומת., עם מנוחה` (נקודה+פסיק) ב-run2/run3, ו-`...המאומת. לפחות.` ב-run1 (שבר משפט).

---

## שלב 3 — המקרים של הבעלים

| # | שאלה | קריטריון | תוצאה |
|---|---|---|---|
| a | `מהי טמפרטורת הבטיחות לצלעות כבש` | מכבש/טלה, לעולם לא בקר | **PASS** |
| b | `מה הדרך המומלצת להכין נקניקיות מרגז על הגריל` | "אין ערך מאומת", בלי 71°C, בלי כרטיס פריט | **PASS** (×2) |
| c | `מה טמפרטורת הבטיחות בבשר בקר` | טווח + שני נציגים | **PASS** |
| d | `מה טמפרטורת הבטיחות בעוף` / `בעוף בגריל` | שניהם 74°C ומסכימים | **PASS** |

**a · צלעות כבש** — verbatim:
> `לפי המדריך, הטמפרטורה הבטוחה עבור צלעות כבש (קארה): 63°C. טמפרטורת הפנים הבטוחה לצלעות כבש היא […], עם זמן מנוחה של 3 דקות לפחות לאחר היציאה מהאש. אם אתה מעדיף את הבשר מבושל יותר, אפשר להגיע עד […]. תיהנה מהצלי! המספרים האלה אינם מאומתים — בדוק בכרטיס הפריט.`

הסולם נחת על **פריט הכבש** (`צלעות כבש (קארה)`), והמסווג בחר `subject_category="טלה"`. **אין בקר בשום מקום.**
per-token trace: `63°C→null` · `71°C→null` · `3 דקות→release`.
*הערה מדווחת:* שני מספרי המודל עצמו נמחקו ל-`[…]` — בניגוד לאסאדו, שם הם שוחררו. זה **אינו** הקריטריון
שנבדק כאן, אבל זה הבדל התנהגות אמיתי בין שני המקרים והוא מתועד ולא מוסבר.

**b · מרגז** — verbatim (ריצה 2, קצרה יותר):
> `אין לנו ערך בטיחות מאומת למרגז. לנקניקיות מרגז מומלץ לעבוד בחום בינוני ולא ישיר מדי, כדי שהמעטפת לא תתפוצץ והשומן לא יישרף. סובב אותן בעדינות כל דקה או שתיים במשך כעשר דקות. חשוב להגיע לטמפרטורה פנימית של […] כדי להבטיח שהבשר מבושל ובטוח לאכילה. מספר זה אינו מאומת.`

‏"אין לנו ערך בטיחות מאומת" ✅ · אין `71` בשום מקום בטקסט ✅ ·
**אין הפניה לכרטיס פריט** ✅ — לא הביטוי `בדוק בכרטיס הפריט` (שכן מופיע בעוף/כבש), ולא צ'יפ:
נסרקו כל ה-`button`/`[data-k]`/`.askhit` ב-`#vcBody` ואין אף אחד שמצביע על פריט
(רק כפתורי הפאנל הקבועים: `הקרא`, `הבא`, `הקודם`, `מה הטמפרטורה?` וכו').
המסווג בחר `subject_category="נקניקיות"` והסולם בכל זאת סירב לתת מספר — נכון.
*הערה:* ריצה 1 הכילה דליפת מילה בערבית מהמודל — `מבושל تماماً`. תופעת פלט של המודל, לא של ה-guard;
לא חזרה בריצה 2.

**c · בשר בקר** — verbatim:
> `לנתחי בשר בקר שלמים, כמו צלי או סטייק, טמפרטורת הבטיחות היא 63°C לפי המדריך המאומת. עם מנוחה של 3 דקות לפחות מחוץ לאש. לבשר בקר טחון, טמפרטורת המינימום הבטוחה היא […]. מספר זה אינו מאומת — בדוק בכרטיס הפריט. בקטגוריית בקר זה תלוי בנתח: בריסקט 63°C, קבב 71°C.`

טווח קטגוריה + **שני נציגים** (`בריסקט 63°C`, `קבב 71°C`) ✅.

**d · עוף** — שתי הנוסחאות מסכימות על 74°C:
> `מה טמפרטורת הבטיחות בעוף` → `לפי המדריך, הטמפרטורה הבטוחה עבור קטגוריית עוף: 74°C. טמפרטורת הבטיחות הפנימית לבישול עוף היא […] לפחות. ...`
> `בעוף בגריל` → `לפי המדריך, הטמפרטורה הבטוחה עבור קטגוריית עוף: 74°C. טמפרטורת הבטיחות הפנימית של עוף בגריל היא […] בחלק הכי עבה של הבשר, ...`

שניהם `74°C`, שניהם נוחתים על **קטגוריית עוף**, המסווג בחר `subject_category="עוף"` בשניהם. אין סתירה.

---

## שלב 4 — השכבה הסמנטית · **"כבש" חשוף**

זו הפעם הראשונה שהשכבה הזו רצה אי-פעם בייצור (תחת ה-400 של v288 היא הייתה אינרטית לחלוטין).

**שאלה:** `מה טמפרטורת הבטיחות בכבש`
**מה שהמסווג החזיר:** `subject_category = "טלה"` — קטגוריית קטלוג חוקית, בדיוק כפי שהבעלים הורה.
היא שרדה את הוולידציה ב-`vcBuildClaimMap` והופיעה כ-`map.catalogSubject="טלה"`.

**הטקסט שנרנדר, verbatim:**
> `לפי המדריך, הטמפרטורה הבטוחה עבור קטגוריית טלה: 63°C. טמפרטורת הבטיחות המינימלית לנתחי כבש שלמים היא […], בתוספת מנוחה של 3 דקות מחוץ לאש. אם מדובר בכבש טחון, טמפרטורת הבטיחות היא […]. המספרים האלה אינם מאומתים — בדוק בכרטיס הפריט.`

**‏"כבש" אכן נפתר כמו "טלה"** — הכותרת הדטרמיניסטית אומרת `קטגוריית טלה` בעוד השאלה אמרה `כבש`.
זה בדיוק היעד של תיקון R-64. per-token trace: `63°C→null` · `71°C→null` · `3 דקות→release`.

---

## שלב 5 — שני התיקונים הקטנים

**האישור `רגע, בודק` — נאמר בדיוק פעם אחת, ב-10/10 הריצות.**
בכל ריצה נפתח פאנל `מצב בישול קולי` תחילה והומתן עד ש-`gemCache` מכיל את האישור
(`warmBefore=true` בכל ריצה) — כלומר בדיוק התנאי החם שבו הוא הוכפל.
מונים: `ackWarm=1`, `ackSeg=0`, **`ackTotal=1`**. אין הכפלה.

**הפער השמע הגדול ביותר בתוך הקראת התשובה: 2.701 ‏ש'** (מרגז, `i=181`, מתוך 326 מקטעים).
בכל שאר 9 הריצות הפער המרבי **בתוך** ההקראה הוא **0.000 ‏ש'** — המקטעים משורשרים רצוף.
הפער הגדול היחיד בכל ריצה (7.57–14.07 ‏ש', תמיד ב-`i=1`) הוא ההמתנה **בין האישור לתשובה** —
זמן החשיבה של המודל, לא חור בתוך ההקראה. שני המספרים מדווחים בנפרד כדי לא לערבב ביניהם.

---

## טבלת מקרים מרוכזת

| מקרה | ריצות | מסווג null? | `subject_category` | ack | פער בתוך ההקראה | פסק |
|---|---|---|---|---|---|---|
| אסאדו | 3 | 0/3 | `בקר` ×3 | 1 ×3 | 0.000s | **PASS 3/3** |
| צלעות כבש | 1 | 0/1 | `טלה` | 1 | 0.000s | **PASS** |
| מרגז | 2 | 0/2 | `נקניקיות` ×2 | 1 ×2 | 2.701s | **PASS** |
| בשר בקר | 1 | 0/1 | `בקר` | 1 | 0.000s | **PASS** |
| עוף | 1 | 0/1 | `עוף` | 1 | 0.000s | **PASS** |
| עוף בגריל | 1 | 0/1 | `עוף` | 1 | 0.000s | **PASS** |
| כבש (חשוף) | 1 | 0/1 | `טלה` | 1 | 0.000s | **PASS** |

## צילומי מסך (390×844)

`docs/analysis/evidence/2026-08-02-v289/` — ‏`s2-asado-run1.png` · `s2-asado-run2.png` · `s2-asado-run3.png` ·
`s3a-lamb-run1.png` · `s3b-merguez-run1.png` · `s3b-merguez-run2.png` · `s3c-beef-run1.png` ·
`s3d1-chicken-run1.png` · `s3d2-chickengrill-run1.png` · `s4-kevesbare-run1.png`

## היגיינת סודות

הורצה דרך AI מנוהל. הוקצו **4 קודי גישה זמניים** ב-KV `CODES` של ה-Worker `matkonet-ai`
(‏cap ‏5,000,000 טוקנים כל אחד), הם נכתבו לקובץ בתיקיית scratch ומעולם לא הודפסו.
**כל 4 נמחקו בסיום ואומת:** ‏`central-code.mjs list` מחזיר כעת קוד אחד בלבד (`code:4Sx5e***zx`) —
זה שהיה קיים לפני העבודה, לא נגעתי בו. קובץ הקודים המקומי נמחק ואומת שאיננו.
שום קוד גישה, מפתח API או כותרת Authorization אינו מופיע במסמך הזה, ולא נראה אף אחד כזה בפלט.

## מה נשאר UNDETERMINED

1. **יציבות סימון האימות בין ריצות.** באסאדו המספרים 90/93/95 שוחררו ב-3/3, אבל בכבש/טלה
   ה-63°C ו-71°C של המודל **נמחקו** ב-2/2. לא נחקר למה שני מקרים כמעט זהים מקבלים טיפול שונה.
2. **התנהגות ל-`NONE` בפועל.** בכל 10 הריצות המודל בחר קטגוריה אמיתית; הסנטינל `NONE` עצמו
   מעולם לא הוחזר, ולכן מסלול "שום דבר לא מתאים" לא נבחן בייצור.
3. **גודל מדגם.** רק אסאדו קיבל 3 ריצות ומרגז 2; שאר המקרים ריצה אחת. הם לא הראו רעש, אבל
   ריצה אחת אינה מודדת יציבות.
4. **הפער של 2.7 ‏ש' במרגז** — נצפה פעם אחת בתשובה הארוכה ביותר (326 מקטעים); לא אופיין.
