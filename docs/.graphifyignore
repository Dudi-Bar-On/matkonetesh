# graphify — exclude non-semantic image assets from the documentation knowledge graph.
#
# Why: the doc corpus under docs/ contains 260+ screenshots (docs/analysis/shots/**)
# and program mockups. graphify has no vision pass configured here, so these images
# carry no extractable text — yet every incremental run re-stats them and, because
# most were added after the last manifest was written, flags ~200 of them as
# "changed". That manifest churn (a) makes `detect_incremental` report hundreds of
# phantom doc changes and (b) would make a `--mode deep` run try to LLM-process every
# screenshot. Excluding images makes the doc graph markdown-focused and keeps the
# incremental gate honest — it then reports only real .md changes.
#
# gitignore semantics (unanchored patterns match at any depth under docs/).
*.png
*.jpg
*.jpeg
*.gif
*.webp
*.svg

# Added 2026-07-24 (§10.12 scope-fix, see ../.graphifyignore and
# docs/process/graphify-improvements.md "Scope-fix + refresh"). Raw
# measurement/instrumentation data under docs/research/measurements/ —
# regenerated every run, not reviewed/diffed like prose (matches the repo's
# own .gitignore treatment of *.json/*.csv in this directory). RUNBOOK.md and
# any hand-written analysis .md stay tracked/graphed; only raw per-run
# artifacts are excluded. evidence/ is a nested Playwright test-results dump
# (trace.zip, error-context.md, .last-run.json, failure screenshots) —
# verified 2026-07-24: 8 of the pre-fix "51 changed documents" were
# evidence/cert-run5-test-results/*/error-context.md files, none of them real
# documentation.
research/measurements/*.json
research/measurements/*.csv
research/measurements/*.log
research/measurements/**/*.json
research/measurements/**/*.csv
research/measurements/**/*.log
research/measurements/evidence/
