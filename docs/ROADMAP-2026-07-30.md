# מפת הדרכים המאוחדת — 2026-07-30

> **משימה 6 מתוך 7 בקשת התכנון. המסמך הזה הוא הבסיס היחיד והמחייב לכל המשך הפיתוח.**
> כל מסמכי הקשת (רגיסטר ההחלטות · האינוונטר · DECISIONS-TO-PLAN · GAP-DELTA · אודיט הציות) נצרכו
> לתוכו; במקרה של סתירה — מסמך זה גובר, וסתירה מדווחת לבעלים (§4).
> **בסיס חי: v277** (מאומת ב-URL החי, `מהדורה 277`).
> **מוסכמת שמות (הוראת בעלים):** מספור ומזהים באנגלית — Phase 0..Phase 12, Language Thread, Sync Thread;
> שמות תוכניות במילים מלאות — Cooking Path (CP), Equipment Programme (E); גוף הטקסט עברית.
> **עדכון 30.7 (פסיקת בעלים):** ארבע פסיקות ביקורת-הכיסוי אושרו — (1) 18 נחיתות היתומים + Phase 9b ·
> (2) בלוק ה-a11y → **Phase 12b** חדש · (3) 9 החורים השקטים נחתו · (4) 12 הכמעט-יתומים שובצו — וכן
> **Dec-H8, כלל הנחיתה המלאה ("שום דבר באוויר")**. המרשם בסעיף 5 חושב מחדש: **0 פריטים ללא נחיתה**.
> **עדכון 30.7 ב' — Recovery Landing (פסיקת בעלים):** אודיט-כיסוי שני (שני סוכנים, כל `docs/analysis/`)
> שחזר **25 פריטים אבודים** → מרשם שחזור חדש **R-1..R-25** בסעיף **5a** (סטטוס ⚠️R נדרש-אימות, שער
> H13 — discipline §16) · **17 פריטים בלועים** נקובים עתה בשם בתוכן הפאזות · **9 טעויות-ראי** תוקנו
> במסמכי המקור · התנגשות המזהה **D6** פוצלה (gap-D6 ≠ Dec-D6, סעיף 3c) · **2 פריטי-לימבו** נרשמו
> ברגיסטר משימות-הדיון (סעיף 5b). נספח כרטיסי-משימות קריא: `docs/ROADMAP-task-cards.md`.

---

## 0 · פרוטוקול הניהול — איך עובדים עם המסמך הזה

1. **המסמך הזה הוא הבסיס.** הבעלים מנהל ממנו הדוק: שום Phase לא נפתח בלי שהוא מסומן כאן ושעריו ידועים.
2. **כל Phase נפתח בתוכנית מפורטת** לפי סקיל `writing-plans` — רק אחרי שהמפרט שלו מאושר (HARD-GATE §1).
3. **בדיקת שלמות-תוכנית מכנית** (מ-Phase 0): כל תוכנית מחוללת נבדקת — ספירת בלוקי-קוד פר משימה > 0. אין משימות-פרוזה. (הלקח מקטיעת תוכנית Cooking Path 2 השקטה.)
4. **H6 — עבודה מרובת subagents, קונטקסט ראשי דק:** הכבד רץ בסוכנים עם קונטקסט מקומי; הראשי שומר לוגיקה וניהול. **תקרת מקבילות סוכנים** נכתבת ב-Phase 0 ל-§10.5 ונאכפת (הלקח מה-fan-out wedge: ~50/25 במקביל תקעו את המכונה).
5. **משמעת בדיקות (H7, פסיקת בעלים 30.7):** DoD-12 פר-משימה = **ריצת סוויטה נקייה ×1** (`npx playwright test`, ללא overrides). **שער שילוח (release) = ×2.** מיושר ב-§3/§9 ב-Phase 0.
6. **releasable תמיד:** כל Phase מסתיים ב-vNNN חי ומאומת ב-Playwright מול ה-URL החי (§10.10) — `.foot-stamp` תואם + probe של פיצ'ר מהמהדורה.
7. **טריגרי C1 (React)** נבדקים בכל שער-Phase; כל סטייה ממפרט מאושר עולה לשיחה (§4); סיכום תלת-חלקי (§10.6) אחרי כל משימה, כולל שורת ברן-דאון מהמרשם שבסעיף 5.
8. **מדיניות מתמשכת (ללא Phase):** Dec-A3 חד-קובץ=ברירת-מחדל עם 3 חובות שימור · Dec-C1–C6 · Dec-D9 תמונות על המכשיר · Dec-E5/E6 · Dec-H3/H4/H5 · כלל הציטוטים (כל `safe` ממקור ראשוני).
9. **H8 — כלל הנחיתה המלאה ("שום דבר באוויר", פסיקת בעלים 30.7):** לכל פער/החלטה/רעיון בדיוק אחד משלושה מצבים — (א) פאזה נקובה · (ב) דחייה מנומקת **עם טריגר מוגדר** · (ג) משימת-דיון רשומה בפאזה נקובה שהחלטותיה משתלבות חזרה בתוכנית. **"נדחה בלי מועד" אסור.** נאכף מכנית: בדיקת **no-unlanded-items** ב-`check-meta.mjs` על המרשם, בכל שער-Phase ובסגירת קשת (מפרט: METHODOLOGY-2026-07-30).
10. **נספח כרטיסי-המשימות:** `docs/ROADMAP-task-cards.md` — כרטיס קריא לכל פריט-עבודה בכל Phase
   (מה התכולה · מה נדרש · איך יודעים שנגמר · מקור). הנספח נגזר מהמסמך הזה ואינו גובר עליו; בפתיחת
   Phase — התוכנית המפורטת (writing-plans) גוברת על שניהם.
11. **H13 — שער רלוונטיות לפריט משוחזר:** כל פריט ⚠️R בסעיף 5a נבדק ברגע ההרמה — (א) שחזור הקשר מלא
   מהמקור + בדיקה מה כבר קיים בקוד ובהחלטות ההווה → (ב) גיבוש **המלצה** (בצע/בטל) → (ג) **שאלת הבעלים
   והחלטה משותפת — נקודת-עצירה חובה** (§10.8: החלטה חשובה, הפרעה נדרשת) → (ד) עדכון השורה בהחלטה →
   (ה) ביצוע או ביטול (`R-cancelled` + שורת נימוק, לעולם לא מחיקה). מפרט מלא: discipline §16.
12. **H9 — טבלת סיכום-משימה מחייבת (פסיקת בעלים 30.7):** כל משימת פיתוח — ב-Main או ב-subagent — מסתיימת בטבלה קבועה בת 5 שורות: **מה היה · מה נעשה (+ראיות) · מה נשאר · איפה אנחנו (Phase X, משימה Y/Z + מרשם N/156) · הבא בתור**. אצל subagents זה חלק מחוזה הדוח; ה-Main מאמת. מיקום חי: `docs/STATUS-BOARD.md` (**H10** — מתעדכן בסיום כל משימה; **H10a** — מתוחזק בכל משימה, מוצג לבעלים רק באבני-דרך או לבקשתו). הרחבה מוּבנית של §10.6 (מפרט: METHODOLOGY-2026-07-30 §3.5–3.6).

---

## 1 · מבנה הפאזות הסופי

**יחידת גודל: "משימת CP2"** (תוכנית Cooking Path 2 = 11 משימות). כל Phase: תוכן (החלטות+פערים+אינוונטר) · כניסה/יציאה · גודל · שערים.
נחיתות היתומים (שסומנו ⚠️) **אושרו על-ידי הבעלים 30.7** (סעיף 3) — הסימונים הוסרו.

### Phase 0 · הקשחת משמעת (5 תיקוני האודיט)
- **תוכן:** N-15 — (1) L23–L27 אל §11 · (2) רענון גרף `--mode deep` (53 מסמכים) + הפקדות §10.16 לקשת v5 · (3) פסיקת H7 ×1/×2 נכתבת ל-§3/§9 · (4) אשרור תזמון החילוצים (Phase 3/5/9/12) · (5) תקרת מקבילות ל-§10.5 + בדיקת שלמות-תוכנית. **וגם:** **H-9** — תיקון טענת "Nobody owns" המופרזת (תיקון-מסמך של שורה) · אישור טבלת נחיתת 18 היתומים — **ניתן 30.7** (סעיף 3) · כתיבת H8/H9 ל-discipline (סעיף 5 ב-METHODOLOGY).
- **סוגר:** H-9, N-15.
- **כניסה:** מיד. **יציאה:** מסמכים+גרף עדכניים; אין שינוי קוד אפליקציה (releasable טריוויאלית, נשארים v277).
- **גודל:** ~3-5. **שער בעלים: כן** — H7 + תזמון חילוצים (אשרור נחיתות היתומים — **ניתן 30.7**).

### Phase 1 · חוסמים מיידיים (Dec-A1/A2/A3, D6)
- **תוכן:** A1 פיצול מילונים + תיקון `LANGNAME`/ar (N-2) · A2 P0-worker — 9 פערי B19–B22/E14/H-3 (CORS, rate-limit, fail-open, TOCTOU, timeout, stream-bypass) · N-4 חיבור מניפסט `SYNCABLE` · N-1 🔴 סתימת דליפת `mk-central-*` ב-`exportData` · D6 ייצוא/ייבוא בטוח · N-5 הצהרת חובות השימור של Dec-A3 (SW אופליין · TWA · רתמה) · **B24** escape של `</script` בהזרקת ה-build · **B25** הסרת ה-catch הריק ב-SW (כשל נרשם ומדווח) · **E15** כותרות CSP/אבטחה (אבטחה זולה, נחתה 30.7).
- **סוגר:** B19, B20, B21, B22, B24, B25, E14, E15, H-3, N-1, N-2, N-4, N-5 (E9 נפתחת חלקית — שארית ב-Phase 12).
- **כניסה:** Phase 0. **יציאה:** vNNN — bundle ~2.1MB, worker fail-closed, גיבוי לא מדליף. משחרר את ה-Language Thread מיד, ואת ה-Sync Thread כשמפרט S1 יאושר.
- **גודל:** ~6-7. **שער בעלים:** לא.

### Phase 2 · גל 0 (Dec-F1/F2a/F3, G1–G5 שכבה 1; Dec-E1: לפני Cooking Path 2)
- **תוכן:** N-7 שלושת הבאגים (עטיפה→שלב אמיתי, כולל שארית `deriveRequires` מ-Equipment 3 · חיווט `svOrderDesc` · נפילת `validCombo`) · N-6 🔴 ארבעת הכשלים המבניים (תנור בלתי-בשיל · **איברים→גריל נגד ציטוט שלנו** · sv-כמתאם · ישיר/עקיף למודל) · אוצר שיטות ~30+~10 (G1/G5-שכבה-1) · טוקני ציוד (G3) · תיוג-מחדש לשון/זנב/מח (G4) · `path_outcomes` מצוטט (F1) · **A10** תיקון נתון ה-cure של קבאנוס (זול, סתירת-ציטוט) · **B31** רענון הערת הבוטוליזם המיושנת (הערת-נתונים — שכבת הנתונים ממילא פתוחה כאן).
- **סוגר:** A10, B31, D8 (reshaped), D9, N-6, N-7; פותר את הגופה `svOrderDesc` (חלק G-7).
- **כניסה:** Phase 0 (ניתן להקבלה עם Phase 1 — קוד שונה). **יציאה:** vNNN — עטיפה=שלב, אוצר חי, `path_outcomes` קיים; DoD-10 בכל משימה.
- **גודל:** ~6-8 (+0.5 A10). **שער בעלים:** לא (F/G אושרו "כן לכולן").

### Phase 3 · חילוץ Orchestrator Module (ORCH) + הקשחות מנוע (Dec-H1/H2, F4/F5)
- **תוכן:** N-12 extractor (רשימת קבצים+acorn-jsx) → חילוץ ORCH (עם Dec-H3 codemod ל-501 בדיקות `page.evaluate` — N-11, ו-Dec-H4 חוזה סלקטורים) → **הכללת MODULES-list ב-build.py** (החילוץ השני — התפר ב-n=2) · תיקון probe-laundering (באג בטיחות חי) · F4 קולגן+`item_facts.py` · F5 `rules.js`+`thresholds.py` · **A5** 🔴 cure-scale הופך BLOCK (חוק ברגיסטר `rules.js`) · **A11** יישוב 18 ערכי המלח (עם `thresholds.py`) · **B29/B30** נורמליזציית הפרות saved/svh וסנטינל `svt=0` (בדיוק מה שרגיסטר ספים מנרמל) · **H-10** עדכון טענת "guards your cure" — כבול ל-A5: ברגע ש-cure חוסם, הטענה הופכת אמת.
- **סוגר:** A5, A11, B2, B3, B4, B8, B9, B14, B15, B16 (ליבת אשכול ה-13), B29, B30, H-10, N-12.
- **כניסה:** Phase 2. **יציאה:** vNNN — `orchestrator.js` חי; שער-Phase עצמאי מול המפרט מאשר את סגירת האשכול; באג ה-probe מתוקן; **A5 מאומת: cure מחוץ לטווח חוסם, לא מזהיר**.
- **גודל:** ~13-15 (+~2 ליתומים). **שער בעלים:** לא (שער-ה-Phase פנימי; חריגה → §4).

### Phase 4 · Cooking Path 2 (CP2) — פאנל הנתיבים (Dec-E1 ✓, Dec-E2)
- **תוכן:** 11 משימות variant B **מרועננות** ל-F2 (שורות מדברות תוצאה, `desc` נצרך) + F2a (רצף מכשירים בתוכנית העבודה) + Dec-E3 (שורת מסלול נבחר+הדגשה) · Dec-H5 פונקציות טהורות · הכנת Dec-C5 (4 משימות) בפנים · **F-30/F-31 — תיקון ניסוח ביניים** בכל משטח קיבולת שהפאנל נוגע בו (סגירה סופית ב-Phase 9b).
- **סוגר:** D4 (reshaped — Phase 2 העטיפה, Phase 4 הרינדור).
- **כניסה:** Phase 2 + Phase 3. **יציאה:** vNNN — הפאנל חי ב-390×844, עברית נקייה (DoD 8+9).
- **גודל:** 11. **שער בעלים: כן** — אישור התוכנית המרועננת (§4) + בדיקת-שלמות מכנית לפני הרצה.

### Phase 5 · ניווט (Dec-B1–B6) + חילוץ Navigation Module (NAV)
- **תוכן:** N-13 מודול state (subscribe/publish) → חילוץ NAV (עם N-11 codemod) → B6 רגיסטר פאנלים+History API → B1 אימוץ+מחיקת `openTools` → B2 back+toast (N-9 — עיצוב חדש) → B4 `?item=` → B5 `?ev=`. N-10 (לחיצה-ארוכה ✕, Dec-B3) נרשם לעתיד. **וגם: A13** — עם הפיכת `NAV.render` לכותב-ה-DOM היחיד נוצרת נקודת-החנק הטבעית ל-escaping: סגירת ה-innerHTML הלא-מוגן של `padvRender` כאן, לא כטלאי נקודתי.
  **פריטי מפרט-הניווט הנקובים (nav-audit 24.7 — היו בלועים בשער, עתה בשם):** **G-N5** — האשף תמיד יוצא ל-home במקום לנקודת-הכניסה · **G-N6** — שחזור מצב/מסך ב-reload/רילונץ' PWA (נשען על רגיסטר הדסקריפטורים) · **G-N7** — אזהרה לפני איבוד קלט-טופס שלא נשמר (✕ על טופס עם קלט). שלושתם שורות חובה במפרט הניווט המאושר בשער.
- **סוגר:** A13, E16, N-9, N-13 (וחלק G-7 — `openTools` נמחק).
- **כניסה:** Phase 4 (Dec-E2). **יציאה:** vNNN לכל פרוסה — deep-links חיים, Back צפוי.
- **גודל:** ~16-20. **שער בעלים: כן** — (א) מפרט הניווט DRAFT→מאושר; (ב) עיצוב ה-toast (§10.9).

### Phase 6 · Equipment 4 (E4) — סדר בישול
- **תוכן:** ברירת-מחדל בכרטיס (Dec-E4) + minTempC/cold-smoke + O-3 preview + אזהרת downgrade (~8).
  **acceptance-bar נקוב (order-vocabulary 25.7 — היה בלוע):** authoring אוצר-סדרים רחב + הכללת הסכימה (`orders:{key}`) עד ש"תוספת-סדר = דאטה בלבד" — נבדק בשער Phase 6 וממשיך כזנב ב-Phase 7/CP3.
- **כניסה:** Phase 5. **יציאה:** vNNN. **גודל:** ~8. **שער בעלים: כן** — מוקאפ (§10.9) אם חורג מהמאושר.

### Phase 7 · Cooking Path 3 (CP3) — מחקר מצוטט (G5 שכבה 2, N-8)
- **תוכן:** נתיבים כ-data+ציטוט, אפס JS; FSIS App. A / FDA 3-401.11(B) מילה-במילה; מתרחב 3→~30 (זנב מתמשך). סופג את A14 (רצפות `safe` של איברים מבלוגים → ציטוט ראשוני).
- **סוגר:** A14, N-8 (גרעין).
- **כניסה:** Phase 2; מומלץ אחרי Phase 6. **יציאה:** vNNN לגרעין. **גודל:** ~4 + מתמשך. **שער בעלים:** לא (כלל הציטוטים = השער).

### Phase 8 · Cooking Path 4 (CP4) — שארית E4 על נתיבים
- **תוכן:** override פר-אירוע (O-1) + O-3 על נתיבים (~6). **סוגר:** C8 (חלקית — overrides לשלב/מדף/חימום נשארים פתוחים).
- **כניסה:** Phase 6 (+Phase 7 רצוי). **יציאה:** vNNN. **גודל:** ~6. **שער בעלים:** לא.

### Phase 9 · Equipment Programme (E5/E6) + חילוץ AI Module
- **תוכן:** חילוץ AI Module (Dec-H1, עם N-11) → Equipment 5 תחליפים+AI (סולם declared→AI→approve/deny→persist, O-2) → Equipment 6 requires מוצהר (grinder/stuffer±sealer, D1-join, צורך סכימת G3). כאן — ורק כאן — זז `eqmRequiresMethodKey` (Dec-E5, בטיחות).
  **הקשחות AI Module (רוכבות על החילוץ):** **E6** ביטול ה-fallback השקט ל-BYOK (הודעה מפורשת למשתמש) · **E10** ביטול ה-retry כפול-החיוב · **E11** מעבר ל-`responseSchema` · **E12** ביטול קונקטנציית פרומפט-טקסט-חופשי · **A4** שער סירוב-אבחון.
  **משימת דיון רשומה (H8-ג): "Brainstorm: הזנת פרמטרי מתכון (טקסט חופשי → פרמטרי מנוע דרך AI)"** — המשימה היא הדיון עצמו; שאלת-הליבה: **התנגשות הבטיחות** בין מספרים שמקורם ב-AI לבין כלל הציטוטים (כל `safe` ממקור ראשוני — לעולם לא מ-AI). תוצאות הדיון משתלבות חזרה בתוכנית דרך שער-Phase.
  **שורות נקובות (היו בלועות, עתה בשם):** **`gemText()`** — איחוד 5 קוראי-הטקסט-החופשי כחלק מתוכנית חילוץ ה-AI Module (audit-product STRAT-B-A1 + model-selection §7-dec-4) · **AI-TEST-1** — בדיקת-שער: אודיט 4-שכבות-הבדיקה פר-פיצ'ר AI (F2/F4/F7 טרם אומתו) רץ בשער ה-Phase · **פירוט צריכת ה-properties** — שער ה-Phase מפרט אילו מ-14 טוקני-הציוד נצרכים ב-E6 ואילו לא (D2-reshape אמר "though not all 14"), כולל הכרעה נקובה על CONS-16 (מודל קיבולת `curechamber`) ו-CONS-9 (blower/controller-fan); CONS-6 (chimney ‏+10 דק') שייך ל-D8/Phase 2.
- **סוגר:** A4, D1, D2 (reshaped), D7-charcuterie, E5, E6, E10, E11, E12.
- **כניסה:** Phase 6 / Phase 8. **יציאה:** vNNN. **גודל:** ~18-20. **שער בעלים:** לא (תוצאות ה-Brainstorm — כן, §4).

### Phase 9b · תחיית P5b — גבולות מבניים (**אושר 30.7**)
- **תוכן:** אשכול היתומים הגדול — **פסק-קיבולת אחד** (B1, B5, B6, B7, B11: איחוד כל פסקי ה"נכנס/לא נכנס" למקור אחד) · **סכימת keyspace ורגיסטר מיגרציות** (B10, B12, B13 — משלים את G-2 מעבר ל-SYNCABLE של הסנכרון) · **אוצר-יום** `today()`/`isoDate()` (B18) · **D5** ביקוש לפי סועדים/חתיכות — מצטרף טבעית לאשכול הקיבולת (שיבוץ 30.7) · סגירה סופית של F-30/F-31 (הסתירות הן סימפטום של ריבוי פסקי-קיבולת).
- **הנימוק למיקום (מול החלופות):** לא זנב של Phase 3 — הוא כבר ~15 משימות והעמסה תסכן את שער האשכול; לא ה-Sync Thread — SYNCABLE מכסה סכימת *סנכרון*, לא את keyspace האפליקציה; **כן כאן** — הסולבר (Phase 10) הוא הצרכן האמיתי הראשון של פסק-קיבולת יחיד (rack budget, share/reassign), ולכן האיחוד חייב להיסגר מיד לפניו; וסכימת ה-keyspace נבנית אחרי שהמניפסט (Phase 1) וה-Sync Thread צברו ניסיון בפועל.
- **סוגר:** B1, B5, B6, B7, B10, B11, B12, B13, B18, D5, F-30, F-31.
- **assertion נקוב בשער (ARCH-analysis §5.4 — היה בלוע):** `FIT_SLOT_TOL` מוחל על **שלושת** הצרכנים — `schedulePlacements` עדיין בלי הטולרנס ("התיקון שנרשם לא הוחל בכל מקום"); שער ה-Phase מאמת את שלושתם.
- **כניסה:** Phase 9. **יציאה:** vNNN — פסק-קיבולת יחיד מאומת בכל המשטחים. **גודל:** ~7. **שער בעלים:** ניתן — קיום ה-Phase ומיקומו **אושרו 30.7**.

### Phase 10 · הסולבר — Solver Phase 3a (שלוש פרוסות)
- **תוכן:** פרוסה 1 `PREFS`/hub/Units + **C7** חשיפת `SCHED_PULL_MAX_MS` ב-UI (~5-6; ה-probe תוקן ב-Phase 3) — **וכולל, בשמו: `PREF_PRESETS` — בורר Simple-Balanced-Pro** (outstanding-register §D + SPEC-reconciliation §2.2; הדרישה שנשרה-בהשקטה בקובץ-תוכנית — דפוס L1 — חוזרת למפרט הפרוסה) · פרוסה 2 `orchestrate`+share/reassign+rack budget+`safetyGate` (~6) — **וכולל שורת-בדיקה נקובה:** cross-event אינו עוקף את העשרת `equipPlan` (substrate-map §1.3 — תדלוק/חימום סותרים בין תצוגות) · פרוסה 3 wood/hold+danger-zone+דירוג AI `action_id` + **D10** תיקון הקשר העשן ל-pellet (הקשר ה-wood-swap הטבעי) — **וכולל, בשמם: מחשבון המתכלים (פחם/פלט/נייר) והצידנית-כבאפר (Faux Cambro, עם מינימום-טמפ, תקציב danger-zone ואיסור עוף/דג — ו-assertion בעלות נקוב: העצה מותנית ב-`hasGear('cooler')`, audit-research-safety D11-ישן)**; המספרים שלהם מקבלים **ציטוטים שלנו — לעולם לא את מספרי ההצעה** (~7). על ORCH מחולץ; **מדרג מסלולים מצוטטים, לעולם לא מייצר** (F1); מרחב המהלכים = האוצר — **וכולל, כשם-מהלך במפרט הסולבר: `hang` כמהלך אוטומטי** (לא רק כבעלות; outstanding-register A.21).
- **סוגר:** A7 (safetyGate = הצרכן של `safetyDiff`), C1, C2, C3, C4/C5 (תיקוני perSlot/slack), C6 (batch כמהלך סולבר), C7, D10.
- **כניסה:** Phase 9b. **יציאה:** vNNN לכל פרוסה. **גודל:** ~18-19. **שער בעלים: כן** — go/no-go: אשרור שספק 17.7 עדיין תקף אחרי גידול האוצר (שינוי → §4) — **ובשער, בשמם (scheduling-architecture §7.5/§7.7 — היו בלועים):** זמן-ההעברה (transfer) הפיקטיבי מטופל במפרט, ו**המפעיל אינו משאב** (אדם אחד אינו מבצע שתי משימות בו-זמנית) מכוסה או נקוב כפער.

### Phase 11 · קופיילוט חי — Solver Phase 3b
- **תוכן:** re-solve חי, "עשה X עכשיו", autopilot; `advanceWhen` מ-Cooking Path 2 הוא הקלט. **כולל, בשמו: רכיב כשל-מכשיר → re-plan** (התאוששות מנפילת חומרה — יש גלאי התנגשות, אין התאוששות; מספריו מצוטטים בעצמנו, לא מההצעה). אין ספק → brainstorming מלא.
- **סוגר:** C12. **כניסה:** Phase 10. **יציאה:** vNNN. **גודל:** ספק + ~12. **שער בעלים: כן** — אישור מפרט לפני שורת קוד (HARD-GATE).

### Phase 12 · זנב לא-פונקציונלי + השלמת החילוצים (I18N Module, Voice Module)
> **⚠️ הערות בעלים מבדיקה חיה של v278 (31.7.2026) — ‏R-27:** ההשמעה הקולית **איטית מאוד**, **טקסטים ארוכים במקצת אינם מוקראים**, וכפתורי עברית/אנגלית מתנהגים לא עקבית. הבעלים ביקש **ריפקטור עומק** לכל נושא הקול. הפריטים הקיימים כאן (A12/E13/A15/C10 + חילוץ Voice Module) **אינם מכסים את השלושה האלה** — הם פערי איכות/ביצועים חדשים. **מיקום נוכחי: Phase 12 (סוף התוכנית) — מועמד להקדמה, טעון החלטת בעלים.**
- **תוכן:** חילוץ I18N Module + F-8 (MutationObserver 62%) + שארית E9 · חילוץ Voice Module + **A12** הזנת 5 אתרי הגארד החזותי ל-TTS + **E13** טקסטי שגיאת TTS מלוקאלים (Dec-E6 לא מכסה אותם — טקסט משתמש, לא פרומפט) + **A15** חוק %/pH בדיבור + **C10** סמן יום ב-voice-cook (שניהם רוכבים על חילוץ Voice) · band-F i18n: A8, B17, F-33, F-5 · TWA ל-Play (F-24/F-25) · מיניפיקציה F-10 (מותרת מאז Dec-A3) · CWV F-6/F-7 (+שארית B27) · אנליטיקס G-3 · ניקוי טענות אופליין G-1 · N-10 לחיצה-ארוכה ✕ (עיצוב+מימוש) · השלמת N-11 · **B28** צמצום ה-2.27MB פר-נתיב (SPA fallback) · **C9** scroll-to-now · **C11** סדר מכשירים ב-occupancy · **G-8** טיפול ב-116 ה-empty-catches · **שארית G-7** השלמת סריקת ה-dead-surface (שיבוץ-מנהל שגרתי, §10.8).
- **סוגר:** A8, A12, A15, B17, B27, B28, C9, C10, C11, E9, E13, F-5, F-6, F-7, F-8, F-10, F-24, F-25, F-33, G-1, G-3, G-7 (סופית), G-8, N-10, N-11.
- **כניסה:** גמיש; אחרי Phase 5 אפשר להקדים פריטים. **יציאה:** vNNN. **גודל:** ~15-17. **שער בעלים:** לא.

### Phase 12b · Accessibility & Presentation (חדש — **אושר 30.7**)
- **תוכן:** 22 פערי ה-a11y/presentation — **F-4, F-9, F-11–F-22** (Lighthouse/ARIA/contrast/targets/labels/landmarks), **F-26–F-29, F-32, F-34–F-36** (craft/tokens) — **וגם F-23** (auto-dark `prefers-color-scheme`; הוויתור בוטל 30.7 — נוחת כאן). העבודה ב-passes רוחביים (audit → contrast → ARIA/landmarks → targets/labels → craft), לא פער-פער.
  **שורה נקובה ב-targets-pass (הייתה קבורה בגוף F-15):** **סקרבר-הזמן של ה-occupancy** — ‏330×16px, אגודל 15px, `position:static` (נגלל החוצה עם השעון של עצמו); הפקד היחיד של "מה על המכשירים בזמן T" מקבל תיקון נקוב, לא רק ספירת-יעדים (W5-B-ux P2).
- **הנימוק למיקום (אחרי Phase 12):** עבודת הניווט של Phase 5 (NAV, רגיסטר פאנלים, History API) נוגעת בכל המשטחים — תיקוני a11y לפניה היו נכתבים פעמיים; ואחרי Phase 12 המשטחים (I18N/Voice/TWA) יציבים סופית.
- **סוגר:** F-4, F-9, F-11–F-22, F-23, F-26–F-29, F-32, F-34–F-36 (23 פערים).
- **כניסה:** Phase 12. **יציאה:** vNNN — audit נקי ברמת היעד ב-390×844 (DoD-8). **גודל:** ~10-14 (משימות-pass, לא פר-פער). **שער בעלים:** לא.

### Language Thread · תור 23 השפות (שזור; נפתח אחרי Phase 1/A1)
- **תוכן:** N-3 — מיזוג pt (staged, 1.45MB) → תיקון el (598KB+52KB כשלים) → 20 הבאות **בסדר הקבוע של הבעלים** (אין לשנות; ru #14, ar #22 — ar דורש את תיקון LANGNAME מ-Phase 1). אימות פר-שפה §10.19 (DOM מרונדר, לא מדדי-פרוקסי); GPU מושהה בזמן ריצות סוויטה (§11a).
- **תנאי-כניסה חוסם לכל מיזוג-שפה — R-1 ⚠️R (שער content-fidelity/אלרגנים, G-T1 🔴):** פסיקת הבעלים 24.7 (אופציה A: ‏recall ‏≥0.90 בטיחות / ≥0.70 כללי) קבעה שער אלרגנים/content-recall **לפני** תרגום המוני — הוא לא נבנה מעולם, וריצות ההמון (it/pt/el/es/de/fr) רצו בלעדיו. עד פסק H13 על R-1: **אף מיזוג-שפה חדש אינו נסגר בלי שורת-שער זו** (מקור: DECISIONS-2026-07-24 D1 + new-gaps G-T1).
- **חוזה-שערים קבוע (סגירה שלא נרשמה — טעות-ראי, תוקנה 30.7):** שערי **G-T2** (unit-literal) · **G-T3** (safety-lexicon: nitrite/nitrate/pink-salt) · **קיפול-½** (foldFractions) **קיימים ונאכפים** ברתמת התרגום (`scratch/translate-eval/gates.mjs`, נאכפים ב-`bulk.mjs gateCheck`) — אבל הקבצים תחת `scratch/` **לא מנוהלים ב-git**; אימוץ הרתמה כחוזה מנוהל = חלק מה-Thread. בנוסף, באימות פר-שפה: **בדיקת-דגימה של גליפי-היחידות `ס״מ²`/`ל׳`** בשפות לא-עבריות (שריד לא-מאומת מ-v267-ui-audit; כנראה נסגר ב-v269 — לא אומת-מחדש).
- **סוגר:** N-3. **גודל:** ~20-22 (~1/שפה). **שער בעלים:** לא (הסדר כבר נקבע).

### Sync Thread · סנכרון (שזור; נפתח אחרי Phase 1/A2; רץ במקביל ל-Phase 4–6 — קוד שרת, לא app.js)
- **תוכן:** מפרט S1 מ-Dec-D1–D5 (כותב-אחד · פיצול תוכנית/ביצוע · fan-out SSE/DO יחיד · LWW+הודעה · **Dec-D5: סמכות בטיחות=מאסטר עם assertion מפורש בבדיקות, DoD-10**) → בניית S1 (~8-10) → זהות: token קשור-מכשיר, קוד גישה→זכאות (~3, Dec-D8). בהמשך: D7/N-14 טלמטריה-כשסגור (הדרישה היחידה שמחייבת שרת, ~4) · Paddle כשה-band המסחרי מופשר. Dec-C6 תוחם: אין חוקים בשרת.
  **וגם, בשמו (פוצל מהתנגשות "D6" — סעיף 3c): D6-gap — תקצוב ערוצי-פרוב בתוך האפליקציה** (leave-in מול spot-check; היום ספירת-footer בלבד, בלי BLE) — נוחת לצד **G-6** (probe/log-import) אחרי S1; אינו Dec-D6 (ייצוא/ייבוא — Phase 1).
- **סוגר:** G-5 (superseded→נבנה), G-4, G-2 (חלק הסנכרון), A6 (קריאות פרוב ככתיבת-ביצוע), G-6/N-14 (אחרי S1).
- **גודל:** ~13 (+עתידי). **שער בעלים: כן** — אישור מפרט S1 לפני בנייה.

---

## 2 · גרף תלויות

```mermaid
flowchart TD
    P0["Phase 0 משמעת"] --> P1["Phase 1 חוסמים A1+A2"]
    P0 --> P2["Phase 2 גל 0"]
    P2 --> P3["Phase 3 חילוץ ORCH + מנוע"]
    P1 -.מקבילות מותרת.- P2
    P3 --> P4["Phase 4 Cooking Path 2"]
    P2 --> P4
    P4 --> P5["Phase 5 ניווט + NAV"]
    P5 --> P6["Phase 6 Equipment 4"]
    P2 --> P7["Phase 7 Cooking Path 3"]
    P6 --> P7
    P6 --> P8["Phase 8 Cooking Path 4"]
    P7 -.רצוי.- P8
    P8 --> P9["Phase 9 Equipment 5/6 + AI"]
    P9 --> P9b["Phase 9b תחיית P5b"]
    P9b --> P10["Phase 10 Solver 3a"]
    P10 --> P11["Phase 11 Copilot 3b"]
    P5 -.אפשר להקדים פריטים.- P12["Phase 12 זנב + I18N/Voice"]
    P9 --> P12
    P12 --> P12b["Phase 12b a11y & presentation"]
    P1 --> L["Language Thread · 23 שפות"]
    P1 --> S["Sync Thread"]
    S -.רץ במקביל ל-Phase 4..6.- P4
    S --> N14["D7 טלמטריה (אחרי S1)"]
```

**מקבילות:** Phase 1 ‖ Phase 2 (קוד שונה) · ה-Language Thread שזור לאורך הכול (GPU מושהה בסוויטות) · ה-Sync Thread ‖ Phase 4–6 (שרת בלבד) · פריטי Phase 12 ניתנים להקדמה אחרי Phase 5. הכול תחת תקרת המקבילות של Phase 0.

---

## 3 · נחיתת 18 היתומים — **אושר על-ידי הבעלים 30.7** (סימוני ⚠️ הוסרו מכל הפאזות)

| # | פער יתום | נחיתה מוצעת | נימוק |
|---|---|---|---|
| 1 | **A5** 🔴 cure-scale רק מזהיר, חייב לחסום (בטיחות) | **Phase 3** | חוק ברגיסטר `rules.js` (F5) — הבית הטבעי לפסק חוסם; נחיתה מוקדמת ומפורשת, מאומתת בשער ה-Phase |
| 2 | A10 cure קבאנוס סותר ציטוט | **Phase 2** | תיקון נתון זול; Phase 2 ממילא בשכבת הנתונים; סתירת-ציטוט לא מחכה ל-Cooking Path 3 |
| 3 | A11 18 ערכי מלח לא-מיושמים | **Phase 3** | יישוב מול מקורות = בדיוק עבודת `thresholds.py`/`item_facts.py` |
| 4 | A12 גארדים חזותיים לא מגיעים ל-TTS | **Phase 12** | רוכב על חילוץ Voice Module — הזנת 5 האתרים נקראת בשם בתוכנית ה-Phase |
| 5 | B29 הפרות saved/svh | **Phase 3** | רגיסטר ספים מנרמל בדיוק את זה |
| 6 | B30 סנטינל `svt=0` | **Phase 3** | כנ"ל |
| 7–11 | B1, B5, B6, B7, B11 — איחוד פסק-קיבולת | **Phase 9b** | הסולבר (Phase 10) הוא הצרכן הראשון של פסק יחיד; חייב להיסגר מיד לפניו |
| 12–14 | B10, B12, B13 — סכימת keyspace | **Phase 9b** | SYNCABLE (Phase 1) מכסה סנכרון בלבד; סכימת האפליקציה נבנית אחרי ניסיון בפועל מהמניפסט ומה-Sync Thread |
| 15 | B18 `today()`/`isoDate()` | **Phase 9b** | זעיר; רוכב על Phase הגבולות המבניים |
| 16 | E13 שגיאות TTS עברית-בלבד | **Phase 12** | Dec-E6 אינו מכסה (טקסט-משתמש, לא פרומפט); בית: חילוץ Voice Module |
| 17 | F-30 סתירת ניסוח קיבולת | **Phase 4** (ביניים) + **Phase 9b** (סופי) | תיקון טקסט זול בזמן ש-Cooking Path 2 נוגע במשטח; הסגירה האמיתית = פסק-קיבולת יחיד |
| 18 | F-31 סתירת ניסוח קיבולת | כנ"ל | כנ"ל |

### 3a · 12 הכמעט-יתומים — שובצו 30.7 (שיבוצי-מנהל שגרתיים לפי §10.8, מצוינים ככאלה)

| ID | פער | נחיתה | נימוק |
|---|---|---|---|
| A4 | שער סירוב-אבחון | **Phase 9** | עבודת ה-AI Module |
| A13 | `padvRender` innerHTML לא-מוגן | **Phase 5** | `NAV.render` הופך לכותב-ה-DOM היחיד — נקודת-החנק הטבעית ל-escaping |
| A15 | חוק %/pH בדיבור | **Phase 12** (Voice) | רוכב על חילוץ Voice Module |
| C7 | `SCHED_PULL_MAX_MS` ללא UI | **Phase 10** | שייך ל-hub/PREFS של הסולבר (פרוסה 1) |
| C9 | scroll-to-now | **Phase 12** | זנב UI קטן |
| C10 | סמן יום ב-voice-cook | **Phase 12** (Voice) | רוכב על חילוץ Voice Module |
| C11 | סדר מכשירים ב-occupancy | **Phase 12** | זנב UI קטן |
| D5 | ביקוש לפי סועדים/חתיכות | **Phase 9b** | אשכול הקיבולת — התאמה טבעית |
| D10 | pellet רואה "🪵 oak" | **Phase 10** | הקשר ה-wood-swap (פרוסה 3) |
| G-8 | 116 empty catches | **Phase 12** | ניקוי רוחבי בזנב |
| H-9 | טענת "Nobody owns" מופרזת | **Phase 0** | תיקון-מסמך של שורה — מיידי |
| H-10 | טענת "guards your cure" | **Phase 3** | כבול ל-A5 — ברגע ש-cure חוסם (BLOCK), הטענה הופכת אמת |

### 3b · 9 החורים השקטים — נחתו 30.7

**B24** (script-escape) · **B25** (SW catch ריק) · **E15** (CSP) → **Phase 1** (בטיחות/אבטחה זולה) · **B31** (הערת הבוטוליזם) → **Phase 2** (הערת-נתונים) · **E6 · E10 · E11 · E12** → **Phase 9** (עבודת ה-AI Module) · **B28** (2.27MB פר-נתיב) → **Phase 12**. **שארית G-7** (השלמת סריקת ה-dead-surface) → **Phase 12** (שיבוץ-מנהל שגרתי, §10.8).

### 3c · בלוק ה-a11y → Phase 12b · דחיות band H → עוגן-טריגר (H8-ב)

- **22 פערי ה-a11y/presentation** (F-4, F-9, F-11–F-22, F-26–F-29, F-32, F-34–F-36 — הספירה המדויקת מביקורת הכיסוי: 22, לא ~20) נוחתים ב-**Phase 12b** (סעיף 1). **F-23** (auto-dark) — הוויתור בוטל 30.7, נוחת גם הוא ב-Phase 12b.
- **דחיות band H (H-1, H-2, H-4–H-8, H-11, H-12 — 9 פריטים):** דחייה מנומקת **עם טריגר מוגדר** — band H נפתח מחדש **באבן-הדרך זהות/Paddle של ה-Sync Thread (העבודה הראשונה על שכבת-בתשלום)**. עד אז — דחוי, לא "באוויר".
- **פיצול התנגשות "D6" (תוקן 30.7, ביקורת-הכיסוי S-1):** המזהה שימש בטעות לשני פריטים שונים. מעתה —
  **‏D6-gap** (פער-המרשם: **תקצוב ערוצי-פרוב בתוך האפליקציה**, leave-in מול spot-check — היום ספירת-footer בלבד) נוחת ב-**Sync Thread לצד G-6** — עוגן-טריגר: השלמת S1; אינו נספר כסגירה בברן-דאון עד אז.
  **‏Dec-D6** (החלטת **ייצוא/ייבוא בטוח** — "כן, מיד") הוא תוכן **Phase 1** כמפורט שם, ואינו שורת-מרשם; הנוסח הקודם של שורה זו תיאר בטעות את Dec-D6 תחת המזהה D6 וגם ייחס לו "committed-later" — בסתירה ל"מיד" של ההחלטה עצמה.

---

## 4 · 15 הפערים החדשים (N-1..N-15) — שיבוץ

| N | תמצית | Phase |
|---|---|---|
| N-1 🔴 | דליפת `mk-central-*` בכל גיבוי | Phase 1 |
| N-2 | `LANGNAME` מונה ar בלי מילון | Phase 1 |
| N-3 | חוב התור: pt staged · el חלקי | Language Thread |
| N-4 | חיבור מניפסט SYNCABLE | Phase 1 |
| N-5 | חובות השימור של Dec-A3 | מדיניות מ-Phase 1 |
| N-6 🔴 | 4 הכשלים המבניים (כולל איברים→גריל נגד ציטוט) | Phase 2 |
| N-7 | שלישיית באגי גל 0 | Phase 2 |
| N-8 | נתון מצוטט פר שיטה×נתח (זנב) | Phase 7 |
| N-9 | עיצוב toast היציאה | Phase 5 |
| N-10 | לחיצה-ארוכה ✕ | Phase 12 |
| N-11 | codemod MK ל-501 בדיקות | Phase 3/5/9/12 |
| N-12 | שדרוג extractor + MODULES-list | Phase 3 |
| N-13 | מודול state | Phase 5 |
| N-14 | טלמטריה-כשסגור (שרת) | Sync Thread (אחרי S1) |
| N-15 | 5 פערי התהליך מהאודיט | Phase 0 |

---

## 5 · מרשם ברן-דאון — **חושב מחדש 30.7** (כל הנחיתות אושרו; תוקנו סטיות-הספירה שביקורת הכיסוי איתרה, כולל ה-off-by-one של Phase 10)

**סך המרשם: 181** = **156 בסיס** (141 המקוריים + 15 N, הטבלה כאן) **+ 25 משוחזרים** (R-1..R-25, סעיף **5a** — סטטוס ⚠️R נדרש-אימות). **נקודת פתיחה: 17 סגורים** (16 + D11 בשער Equipment 2). המספרים בטבלה = יעד מצטבר בסגירת כל Phase **על 156 הבסיס בלבד**; פריט R מצטרף לספירת הסגירות של הפאזה שלו רק אחרי פסק **בצע** בשער H13 (פסק **בטל** = `R-cancelled`, יורד מהמכנה של 5a). (±1 בספירות גבוליות — המרשם המחייב מתעדכן בכל שער-Phase.)

| Phase | נסגרים | Δ | מצטבר / 156 |
|---|---|---|---|
| בסיס v277 | — | — | **17** |
| Phase 0 | H-9, N-15 | 2 | 19 |
| Phase 1 | B19–B22, B24, B25, E14, E15, H-3, N-1, N-2, N-4, N-5 | 13 | 32 |
| Phase 2 | A10, B31, D8, D9, N-6, N-7 | 6 | 38 |
| Phase 3 | A5, A11, B2/B3/B4/B8/B9/B14/B15/B16, B29, B30, H-10, N-12 | 14 | 52 |
| Phase 4 | D4 | 1 | 53 |
| Phase 5 | A13, E16, N-9, N-13 | 4 | 57 |
| Phase 6 | — (מכין את C8) | 0 | 57 |
| Phase 7 | A14, N-8 (גרעין) | 2 | 59 |
| Phase 8 | C8 | 1 | 60 |
| Phase 9 | A4, D1, D2, D7, E5, E6, E10, E11, E12 | 9 | 69 |
| Phase 9b | B1/B5/B6/B7/B10/B11/B12/B13/B18, D5, F-30, F-31 | 12 | 81 |
| Phase 10 | A7, C1–C6, C7, D10 | 9 | 90 |
| Phase 11 | C12 | 1 | 91 |
| Phase 12 | A8, A12, A15, B17, B27, B28, C9, C10, C11, E9, E13, F-5/6/7/8/10/24/25/33, G-1, G-3, G-7 (סופית), G-8, N-10, N-11 | 25 | 116 |
| Phase 12b | F-4, F-9, F-11–F-22, F-23, F-26–F-29, F-32, F-34–F-36 | 23 | 139 |
| Language Thread | N-3 | 1 | 140 |
| Sync Thread | A6, G-2, G-4, G-5, G-6, N-14 | 6 | **146** |

**מצב סיום התוכנית (על 156 הבסיס): 146/156 (~94%).** **הנותרים — 10 בדיוק, כולם מעוגני-טריגר לפי H8-ב; אפס פריטים "באוויר":**
- **9 דחויי band H** (H-1, H-2, H-4–H-8, H-11, H-12) — **טריגר: אבן-הדרך זהות/Paddle של ה-Sync Thread** (העבודה הראשונה על שכבת-בתשלום).
- **D6-gap** (תקצוב ערוצי-פרוב; פוצל מ-Dec-D6 — סעיף 3c) — **טריגר: השלמת S1 ב-Sync Thread** (נוחת לצד G-6).

**יעד H8 הושג: 0 פריטים ללא נחיתה** — 129 מזהים נסגרים בפאזות נקובות (17 + 129 = 146), 10 מעוגני-טריגר; **וגם כל 25 המשוחזרים נחותים** (סעיף 5a: 20 בפאזות נקובות · 3 מעוגני-טריגר · 2 משימות-דיון רשומות בסעיף 5b). נאכף מכנית בבדיקת ה-no-unlanded-items של `check-meta.mjs` בכל שער-Phase ובסגירת קשת. מצב חי: `docs/STATUS-BOARD.md` (H10).

---

## 5a · מרשם השחזור (Recovery Ledger) — R-1..R-25 · **פסיקת בעלים 30.7**

> **מקור:** אודיט-כיסוי שני (30.7) על כל `docs/analysis/` — סוכן A (root+program, 41 מסמכים) + סוכן B
> (sweep+תתי-ספריות, 34 קבצים). דוחות המקור: `.superpowers/sdd/coverage-audit-A-report.md` /
> `coverage-audit-B-report.md` (קבצים מקומיים לפי מוסכמת הרפו; התוכן המחייב — כאן).
> **פסיקת הבעלים:** לשחזר הכול, אבל **כל פריט משוחזר עשוי להיות מיושן** — בוצע מאז, או הופרך על-ידי
> החלטות/ארכיטקטורה מאוחרות. לכן כל שורה: סטטוס **⚠️R נדרש-אימות** + **מצביע-מקור** לשחזור ההקשר,
> ובהרמה — **שער H13** (סעיף 0 פריט 11; discipline §16): שחזור→בדיקה→המלצה→**החלטה משותפת עם הבעלים**→
> בצע/בטל (`R-cancelled` + נימוק — לעולם לא מחיקה שקטה).
> **ספירה:** 25 = 18 (סוכן A) + 7 (סוכן B: M-1..M-7). נחיתות: 20 בפאזות נקובות · 3 מעוגני-טריגר ·
> 2 משימות-דיון (סעיף 5b). פריט R נספר בברן-דאון רק אחרי פסק בצע (סעיף 5).

| R | הפריט (תמצית) | מצביע-מקור | נחיתה | סטטוס |
|---|---|---|---|---|
| R-1 | 🔴 **שער content-fidelity/אלרגנים לפני תרגום המוני** — פסיקת בעלים 24.7 (אופציה A: recall ≥0.90 בטיחות / ≥0.70 כללי) כחוסם; לא נבנה (`gates.mjs` מכיל רק unit-literal+safety-lexicon), וריצות ההמון רצו בלעדיו | `program/DECISIONS-2026-07-24.md` D1 + `program/new-gaps-2026-07-24-p0-app.md` G-T1 | **Language Thread** — תנאי-כניסה חוסם לכל מיזוג-שפה | ⚠️R נדרש-אימות |
| R-2 | 🔴 **מיסוג ה-marker "לפי המדריך המאומת" למספר עצמו** — הגארד טוען על משפט שלם שלא בדק; מספר בלי יחידה/במילים עובר בלי בדיקה; גבולות Unicode (`º`, `℃`, full-width); "מעלות פרנהייט" בעברית לא מזוהה. הוחלט 24.7 (spec-change §3.1), לא מומש | `program/new-gaps-2026-07-24-p0-app.md` G-A1 (+addendum) + DECISIONS D2 | **Phase 1** (אושר 30.7) | ✅ **בצע — פסק בעלים 31.7 (H13)**: כל 5 החורים אומתו חיים בקוד של היום (הרצת הסימבולים עצמם, memo `.superpowers/sdd/task-13-gate-memo.md`). הבעלים: "בצע מלא" + דרישת-על: **רכיב המרת-יחידות גנרי** שישרת גם את הקול (ראו R-26) |
| R-3 | 🔴 **מספר "מאומת" מהשדה הלא-נכון** (63° של אמבט sv מוצג כ-safe) — הוחלט "מתקפל לתוך D2"; D2 לא מומש | `program/new-gaps-2026-07-24-p0-app.md` G-A2 + DECISIONS D3 | **Phase 1** (יחד עם R-2) | ✅ **בצע — פסק בעלים 31.7 (H13)**: כל 5 החורים אומתו חיים בקוד של היום (הרצת הסימבולים עצמם, memo `.superpowers/sdd/task-13-gate-memo.md`). הבעלים: "בצע מלא" + דרישת-על: **רכיב המרת-יחידות גנרי** שישרת גם את הקול (ראו R-26) |
| R-4 | **scoping דליפת ה-heap** — ‏~2.4-2.8MB לכל reload בלי plateau; הוחלט 24.7 "scope first" — ה-scoping מעולם לא בוצע | DECISIONS D15 + `program/MASTER-ONBOARDING.md` §4 | **Phase 5** (אושר 30.7) | ⚠️R נדרש-אימות ⚠️ **סקר H13: משימת ה-scoping מעולם לא בוצעה (אין commit), אך קיום הדליפה עצמה לא אומת** — דורש מדידה חיה בדפדפן. **הצעד הראשון הוא מדידה-מחדש, לא תיקון** |
| R-5 | **בר-ההשוואה של PRE-4 מעולם לא הורץ** (הוחלט 24.7 "run now") + בדיקת עלות thinking של 3.6-flash על payload אמיתי + רענון כלכלת-היחידה | DECISIONS D17 + `program/PRE-4-baseline-runbook.md` + `program/gemini-3.6-thinking-research.md` | **משימה עצמאית מוקדמת** — חוסמת את פתיחת Phase 9 (עוגן: פתיחת Phase 9) | 🟡 **חלקית — נסגר בסקר H13 (2.8.26) על 2 מ-3 חלקיו.** בר PRE-4 הורץ (`18e23ba`, ריצה 30117127600, 13/13) · עלות החשיבה נמדדה (`39a20a0`/`bcf9d81`, 204 קריאות חיות). **נותר רק רענון כלכלת-היחידה** → נפתחת שורה צרה חדשה במקום להשאיר את זו פתוחה |
| R-6 | **מודל plan-depth — 47 הדרישות הלא-בטיחותיות** (pref עומק, טוגל 3 רמות, buffer משתנה-רמה, stall-reassurance, משימות-תלייה…) — פסיקת close-or-commit; 2 הבטיחותיות כבר נחתו (E3 ✅, A5→Phase 3) | `2026-07-22-audit-orchestrator.md` §H (DEPTH-1..14) + `2026-07-21-outstanding-register.md` §A.24 | **משימת-דיון H8-ג → סעיף 5b** (החלטת בעלים נדרשת) | ✅ **פסיקת בעלים 2.8.26 — commit ל-Phase 10.** נסגר מלימבו: 47 הדרישות הלא-בטיחותיות נבנות כחלק מתכנון הסולבר, לא לפניו. הנימוק: הסולבר צורך את רוב דרישות-העומק, ובנייה מוקדמת היא בנייה נגד ממשק שטרם קיים. שתי הבטיחותיות כבר נחתו בנפרד (E3 ✅, A5→Phase 3) |
| R-7 | **שארית C8 (C8b)** — ‏overrides למשך-שלב/מדף/חימום "נשארים פתוחים" (לשון Phase 8 עצמה) ללא נחיתה | `COVERAGE-CHECK-2026-07-30.md` (C8) + ROADMAP Phase 8 | **Phase 12** | ⚠️R נדרש-אימות |
| R-8 | **ברירת-מחדל sv+smoke לנתחי גריל** (פיקנייה/קבב) — הבאג השורשי של C2; אומת חי: `app.js:999` ‏`def:['sv','smoke']` לכל doneness | `2026-07-21-walkthrough-defects.md` M1 + `2026-07-22-status-and-gaps.md` §D | **Phase 2** (עבודת methodRules/האוצר) | 🟠 **פסיקת בעלים 2.8.26 — משובץ להחלפת המנוע בגל 0.** אובחן במלואו (`docs/analysis/2026-08-02-R8-R15-safety-diagnosis.md`): **אינו קריטי-בטיחות** — ‏bcheck אינו תלוי-קומבינציה (`app.js:4752` קורא `meta.obj.safe` מחוץ לענף; פיקנייה 63°, קבב 71° בכל מסלול). **אך משך הבישול כן זז** (קבב 3.33 שע' מול 0.3 שע'), וזה המנגנון של באג C2 — ולכן אינו פטור מ-DoD-10. ‏`grillable` אינו מבחין: כל 56 נתחי ה-doneness `grillable:True`. **הגדרת ההיקף נעשית עם החלפת המנוע**, לא לפניה |
| R-9 | **צ'יפ קטגוריה באשף לא ניתן לביטול בהקשה שנייה** — אומת חי: `app.js:9067` ללא toggle-off | `2026-07-21-walkthrough-defects.md` M2 (במספור הרגיסטר M3) | **Phase 12** (זנב UI) | ⚠️R נדרש-אימות |
| R-10 | **אימות-קליק שטופס גריל-פחם אינו משמיט zones/area** — תוקן רק לשדות property (`cf9458a`); שדה הקיבולת לא אומת מעולם בקליק אמיתי | `2026-07-21-walkthrough-defects.md` M3 + `2026-07-22-audit-research-safety.md` | **Phase 2** (משימת-אימות קצרה) | ✅ **כבר בוצע — נסגר בסקר H13 (2.8.26).** ‏`tests/equipment-walkthrough.spec.ts:139` מריץ קליקים אמיתיים על כל 8 הקטגוריות ומאמת שדה-תפוסה ל-`grill`; ‏area מכוסה ב-`tests/e1-area-field.spec.ts:24-33` |
| R-11 | **חבילת הפוליש של תצוגת-המכשירים** — סרט-צפיפות-זמן + ghosting ‏±20 דק' (`deviceTimeline`) · אריחים כ-`<button>` פותחי-שיוך · reconciliation במקום `innerHTML=` פר-scrub · גובה אריח מודע-`--fscale` · ‏`zoneMix`/מדפים לא-אחידים/רוטיסרי ("listed here so they don't silently disappear" — ונעלמו) | `2026-07-21-outstanding-register.md` §E + `2026-07-21-device-visualization.md` §2.6 | **Phase 12b** (שורת-אשכול ב-presentation pass) | ⚠️R נדרש-אימות ⚠️ **סקר H13: לנסח מחדש לפני שיבוץ** — `deviceTimeline` כבר לא קיים (הוחלף ב-`deviceOccupancy`/`occupancyViewHtml`). השארית עדיין נכונה: `body.innerHTML=` בכל scrub (`app.js:1090`), אין `zoneMix`, האריחים divים (`:942`) |
| R-12 | **AI response cache/dedup + in-flight guard** בפתיחה-חוזרת של פאנל | `2026-07-22-audit-product.md` STRAT-B-A5 + `2026-07-22-status-and-gaps.md` §E.14 | **Phase 9** (הקשחות AI Module, ליד E10) | ⚠️R נדרש-אימות |
| R-13 | **מחליף 3 המצבים (beginner/home/pro)** — "deferred" בזיכרון-שיחה בלבד; המסמך דורש close-or-commit מפורש | `2026-07-22-audit-product.md` UXR-1 | **משימת-דיון H8-ג → סעיף 5b** (החלטת בעלים נדרשת) | ✅ **כבר בוצע — נסגר בסקר H13 (2.8.26).** מחליף שלוש הרמות חי ומשוחרר: `openUiLevel()` בתפריט ⚙️ ("רמת ממשק — מתחיל/בינוני/מתקדם"), `uiLevel()`/`setUiLevel()` ואונבורדינג — `app.js:11176-11198`, `:11235`, `:13929`. המקור עצמו כבר סימן "SUPERSEDED-in-spirit" (`audit-product.md:154`). **השורה הוצגה לבעלים כלא-בנויה — זו הייתה טעות הצגה, לא החלטה.** ‏R-38 נותר פתוח כשאלה עסקית נפרדת |
| R-14 | **החלפת אייקון 🧫→⚗️** (זעיר; פתוח מאז fire-guide-prompt) | `2026-07-22-audit-product.md` UXR-3 | **Phase 12b** (craft pass) | ⚠️R נדרש-אימות |
| R-15 | **בדיקה-מחדש של 3 ציטוטי `cold:True` בכבש** (n-35/36/60 — cold-smoke בטמפ' 55-60°, מעל כל תקרה מקובלת) — פסיקת בעלים 25.7, מסלול-נתונים | `program/registered-2026-07-25-order-vocabulary.md` addendum | **Phase 7 / CP3** (עבודת הציטוטים) | 🟡 **אובחן, ממתין לביצוע** (`docs/analysis/2026-08-02-R8-R15-safety-diagnosis.md`): **אינו קריטי-בטיחות** — ל-`cold` בדיוק שני צרכנים, שניהם תווית בלבד (`app.js:3027`, `:4698`); המספרים נקראים מ-`osm.t`/`osm.h` בנפרד, ואין מסלול להוראה לא-בטוחה. אך התיוג שגוי לפי הגדרת האפליקציה עצמה — המילון `data.py:201` מלמד "עישון קר … ≤30°" בעוד מסך אחר אומר "עישון 60° (עישון קר)". **התיקון: 3 × `cold:True`→`False` ב-`sources.py:1094/1132/1241`, אפס שינוי מספרי, נקי ל-DoD-10.** מבוצע אחרי סגירת באג המספרים |
| R-16 | **משטח שלמות פרופיל-ציוד** ("ציוד: 3 · דיוק 40%") — לא נחת | `2026-07-22-audit-orchestrator.md` PROP-14 | **Phase 9** (E5/E6) | ⚠️R נדרש-אימות |
| R-17 | **פריטי-תהליך graphify** — קורפוס `methodology` מכיל מסמכים פרטיים של פרויקט אחר · hook post-commit לא מותקן · סכנת headless-extract · D12 ‏embedding-query · D13 ‏upstream של ה-patch (install דורס) | `program/MASTER-ONBOARDING.md` §4 Tier-3 + DECISIONS D12/D13 | **טריגר: רענון-הגרף המלא הבא** (§10.12; ‏Phase 0 כבר נסגר) | ⚠️R נדרש-אימות 🟡 **סקר H13: תת-הפריט post-commit-hook נסגר** (`f5fd200` + `.git/hooks/post-commit` קיים); שלושת האחרים נותרו |
| R-18 | **מפרט home-screen של R7** — הצ'רטר פתח את P7 עם "no R7 home-screen spec"; ב-Phase 12b יש passes רוחביים אך אין שורת-מפרט (MISSING-lead, ודאות בינונית) | `2026-07-27-gap-closing-BIG-STATUS.md` §3.1 (P7) | **Phase 12b** — אימות מול הצ'רטר בפתיחת ה-Phase | ⚠️R נדרש-אימות |
| R-19 | **Photo Analyze שולח תמונה בלי downscale** — ‏`openPhotoAnalyze` שולח base64 גולמי ~4-8MB ל-Worker; ‏`downscale()` (‏`app.js:4553`) מחווט רק ליומן | `sweep/W4-A-unit-economics.md` §5.7 (M-1) | **Phase 9** (אושר 30.7; משפחת הקשחות עלות/עומס ליד E10/E12) | ⚠️R נדרש-אימות ⚠️ **סקר H13: הנימוק שגוי, העובדה נכונה** — היעדר ה-downscale אמיתי (`app.js:13819`), אך מניע-העלות **סותר REFUTED**: ניתוח תמונה נמדד כיכולת הזולה ביותר, $0.0014 לקריאה (`ULTIMATE:1129`). לנמק על **זמן העלאה**, לא על עלות |
| R-20 | **`overscroll-behavior: contain` נעדר לחלוטין** — גלילה לסוף פאנל/sheet משתרשרת למסך שמאחוריו (`grep app.css` ‏→ 0) | `sweep/W5-B-ux.md` runners-up (M-2) | **Phase 12b** (craft pass) | ⚠️R נדרש-אימות |
| R-21 | **ה-bottom nav מתעלם מ-safe-area** — ‏`.cnav{bottom:0}` בלי `env(safe-area-inset-bottom)` בעוד האידיום קיים בקובץ ×3 (`app.css:320` מול `:369/:374/:1438`) | `sweep/W5-B-ux.md` runners-up (M-3) | **Phase 12b** (craft pass) | ⚠️R נדרש-אימות |
| R-22 | **אין `-webkit-tap-highlight-color`** (הבזק כחול על ממשק טרקוטה) + **`transition: all` ×2** (`.cseas-item`, `.mtoggle`) | `sweep/W5-B-ux.md` runners-up (M-4) | **Phase 12b** (craft pass) | ⚠️R נדרש-אימות |
| R-23 | **מסך-ריק של Voice Cook הוא משפט מת** — "בנה תוכנית עבודה במתזמן ואז חזור" בלי כפתור ליעד (`app.js:6398`) | `sweep/W5-B-ux.md` runners-up (M-5) | **Phase 12** (רוכב על חילוץ Voice Module, לצד C10/A12/A15/E13) | ⚠️R נדרש-אימות |
| R-24 | **re-extract בגרנולריות-claim של 8 קבצי W1-*** — התחייבות מפורשת ("worth doing, and not done here"); בלעדיה 133/156 קשתות ה-refutes נוחתות על מסמך שלם ולא על הטענה | `sweep/W1-GRAPH-docs.md` §Q2 | **טריגר: רענון-הגרף המלא הבא** | ⚠️R נדרש-אימות ⚠️ **סקר H13: המהות נכונה, המדד לא משוחזר** — 49 צמתי-גרף על פני 8 מסמכי W1 (~156 טענות), אך "133/156" אינו ניתן לשחזור היום (81 קשתות refutes, אחת נוחתת על צומת-מסמך-שלם) |
| R-25 | **pass יישוב-מסמכים** — מסמכים יתומים על-תוכן (`equipment-2.0-gaps`, `REVIEW-v147`, `matkonetesh-modes-demo`, `agent-research-protocol`) · תאומי EN/HE שיכולים לסחוף · ציר מסך-הבית "הוחלט 3 פעמים ב-3 מסמכים שאינם מצטטים זה את זה" — לסמן superseded/למחוק/לקשר (חלקו כנראה DONE-בפועל אך אף מסמך לא הוכרז superseded) | `sweep/W1-GRAPH-docs.md` §Q2/Q4 (M-7) | **טריגר: רענון-הגרף המלא הבא** (יחד עם R-24) | ⚠️R נדרש-אימות |
| R-26 | 🔴 **רכיב המרת-יחידות גנרי + יחידות קנוניות בבסיס** (פסק בעלים 31.7, נולד משער H13 של R-2/R-3) — דרישה: (1) הגדרת יחידות למשתמש ב-Settings — לבדוק אם ממומשת נכון; (2) **רכיב ממיר גנרי אחד** שכל צרכן עובר דרכו — ממשק קולי, מתכונים, בטיחות, ציוד — כך שתמיד מוצג/נאמר הערך הנכון עם היחידה הנכונה; (3) **ייבוא ערך חיצוני מייבא גם את יחידתו** וממיר לקנוני; (4) **הבסיס נשמר מטרי/צלזיוס** בדיוק כפי שהעברית היא בסיס המילון וכל השאר נגזר ממנה. | פסק בעלים 31.7 בשער H13 (`.superpowers/sdd/task-13-gate-memo.md`) | **חקירה בוצעה 31.7** (`docs/analysis/2026-07-31-units-architecture-investigation.md`) → 6 משימות U-1..U-6: U-1..U-3 ‏**Phase 1** (מסווג-יחידות אחד במקום 5 כפילויות · פורמטר `fmtUnit` · Guard B נגזר מאותה טבלה) · U-4/U-5 **Phase 3** (הצהרת קנון מפורשת + שדה `cited:{value,unit}` — תנאי מוקדם ל-CP3) · U-6 **Phase 10** (המתג) | ✅ **פסקי בעלים 31.7:** (א) **תצוגה אימפריאלית מלאה** למשתמש שבוחר בה — ‏~150 אתרי פורמט מוסבים לפורמטר המרכזי; (ב) אופציית "אימפריאלי" ב-Settings **מתוקנת** ולא מוסרת; (ג) שדה `cited` מובנה **ב-Phase 3**. נותר לתכנון: מפרט U-1..U-6 |
| R-27 | 🔴 **הממשק הקולי וההשמעה הרוחבית — ריפקטור עומק (הערות הבעלים מבדיקת v278 החיה, 31.7)**: (א) **ההשמעה איטית מאוד** — זמן תגובה מרגע הבקשה עד תחילת הדיבור; (ב) **טקסטים ארוכים במקצת אינם מוקראים** (נחתכים/לא מושמעים כלל — חשד: מגבלת אורך/חלוקה לקטעים ב-TTS או timeout); (ג) **כפתורי עברית/אנגלית** — התנהגות לא עקבית; (ד) הבעלים: "באופן כללי וגורף נדרש לבצע refactoring מעמיק לנושא". | בדיקת בעלים חיה על v278 (31.7.2026) | ✅ **הוקדם — "Voice Wave 0", פסק בעלים 31.7** (מיד אחרי שילוח v279). חקירה: `docs/analysis/2026-07-31-voice-investigation.md` — שלוש התקלות הן תיקונים ממוקדים, ללא תלות ב-Phase 12 | ✅ **בוצע ושוחרר** — שלושת התסמינים: איטיות (v280 timeout→v282 R-51 ~1.1 שנ') · טקסטים ארוכים (v280 chunking) · כפתורי HE/EN (v280, R-29) |
| R-28 | 🔴 **Phonikud — פתרון ההגייה העברית (פסק בעלים 31.7: "רוצה מאד שתשתמש ב-Phonikud, זה פותר לחלוטין את בעיות ההגייה בעברית")** — ניקוד אוטומטי של הטקסט לפני הסינתזה; נשקל כבר במחקר Wave 4 ומעולם לא נחת. טעון ספק: איך משתלב בצינור ה-TTS, עלות/גודל, מקומי מול שרת, והשפעה על הגארד הדבור. **פסק בעלים 31.7: הספק נשאר Google TTS — שכבת פרימיום יקרה (ElevenLabs) בוטלה כלא-כלכלית**, ו**Phonikud מיושם עבור עברית בלבד** (שאר השפות אינן מנוקדות ואינן זקוקות לכך — הפעלתו עליהן היא באג), ולכן Phonikud הוא *מסלול השיפור היחיד* של ההגייה ולא אחת משתי חלופות. | `docs/research/03-tts.md` (15.7) + פסק בעלים 31.7 | ❌ **בוטל — פסק בעלים 31.7 אחרי ניסוי A/B עיוור** (24 קבצי אודיו, אותו מנוע): "כמעט ואין הבדל, מקרים מעטים וניואנסים קטנים — לא מצדיק את המאמץ". הניסוי המקומי הוכיח היתכנות טכנית (26ms/משפט, אפס פגיעה בספרות/יחידות) אבל **הרווח הנשמע אינו קיים** — ולכן הכיוון נסגר, לא נדחה. **נסגר סופית 31.7 גם עבור הניקוד של Google עצמה** — ניסוי שני (Gemini מנקד ב-~1.2-1.4 שנ' ואז Gemini מקריא): פסק בעלים "הניקוד של גוגל לא הוסיף איכות — לא נשתמש בו". כלומר **כל מסלול הניקוד סגור**, לא רק Phonikud: לא חיצוני, לא מקומי, ולא עצמי. `R-cancelled` · תיעוד: `docs/analysis/2026-07-31-phonikud-local-trial.md` + `docs/research/2026-07-31-runtime-hebrew-diacritization.md` (נשמר — יש בו פתרון חינמי מוכן אם השאלה תחזור) | 🚫 בוטל (פסק בעלים 1.8: Gemini נבחר, ניקוד נסגר) |
| R-29 | 🟠 **כפתורי עברית/אנגלית — מועמדים להסרה** (פסק בעלים 31.7): הם נוספו כפתרון-עוקף לאיכות עברית ירודה. **אם ההגייה העברית תהיה טובה (R-28) — אפשר להסיר אותם לגמרי.** ההחלטה תלויה בתוצאת Phonikud, לא בעיצוב. **פסק בעלים משלים 31.7 (R-31): הממשק הקולי מדבר בשפת האפליקציה הנוכחית** — ומכאן ששני הכפתורים מאבדים את תפקידם ממילא: שפת התשובה נגזרת מהממשק ואינה בחירה נפרדת. | פסק בעלים 31.7 (חקירת R-27) | **Voice Wave 0** — הכרעה אחרי מדידת איכות ההגייה | ✅ **בוצע ב-v280** — הכפתורים הוסרו; הקול נגזר משפת ה-UI (R-31) |
| R-30 | 🟠 **זיהוי דיבור (STT) בעברית — לא עבד מספיק טוב ודורש שיפור** (פסק בעלים 31.7). **פחות דחוף, אפשר בשלב מאוחר — אך חובה**: זהו התנאי לשימוש האמיתי במוצר, "ליד המעשנה כשהידיים תפוסות". | פסק בעלים 31.7 | **Phase 12** (עם חילוץ Voice Module) — נקוב בשמו, לא בשורת-סל | ⚠️R · טרם נחקר |
| R-31 | 🔴 **הממשק הקולי מדבר בשפת האפליקציה** (פסק בעלים 31.7): רואים צרפתית → השיחה בצרפתית; רואים עברית → עברית. שפת התשובה, מבטא ה-TTS ושפת הזיהוי נגזרים **ממצב אחד** — שפת הממשק — ולא משני מצבים נפרדים כפי שהיום (`vcLang`/`vcAnsLang`, שמתנתקים לצמיתות אחרי הלחיצה הראשונה — ראו החקירה). נגזרות: (א) **Phonikud רק לעברית**; (ב) הכפתורים (R-29) מתייתרים; (ג) 7 השפות החיות נבדקות בקול, לא רק he/en. | פסק בעלים 31.7 | **Voice Wave 0** | ✅ **בוצע ב-v280** — מקור-שפה אחד; דליפות שנותרו נסגרו ב-v283 (R-55) |
| R-32 | 🔴 **ההקראה אך ורק דרך Google — ביטול מוחלט של קול הדפדפן** (פסק בעלים 31.7): "אני כנראה שומע רוב הזמן את ברירת המחדל של הדפדפן — רוצה לבטל את זה לחלוטין, אך ורק דרך גוגל". היום `vcSpeak` נופל ל-`sysSpeak` (‏`speechSynthesis`) בכל כשל — בשקט — וזה מה שהבעלים שומע בפועל. נגזרות: הסרת `sysSpeak`/בורר-הקולות מהמסלול, כשל TTS מוצג כשגיאה גלויה ולא מוחלף בקול נחות, והחלטה מפורשת למשתמש **ללא מפתח AI** (הקראה אינה זמינה + הכוונה להגדרה). | פסק בעלים 31.7 | **Voice Wave 0** | ✅ **בוצע ב-v280** — fallback לקול הדפדפן נמחק; Google בלבד |
| R-33 | 🔴 **להפסיק לשכתב טקסט שנשלח ל-Gemini TTS** (ממצא מדיד 31.7): `hebSpeechText` נכתבה עבור מנוע הדפדפן והופכת `63°C`→"63 מעלות", סוגריים→", … ,", `·`→פסיק, `8-10`→"8 עד 10" — ומזריקה פסיקים שקוטעים את ההקראה (נצפה: `מתייצב , כ-70 מעלות פנימי,.`). ‏A/B באותו מנוע ואותו קול הראה שהטקסט הגולמי נשמע טבעי בהרבה. העיבוד יישאר **רק** למסלול שלמענו נכתב (אם יישאר כזה), ו-Gemini יקבל טקסט נקי. | ‏A/B 31.7 (`scratchpad/phonikud/preproc-audio`) + `app.js hebSpeechText` | **Voice Wave 0** | ✅ **בוצע ב-v280** — `ttsText` מחליף את שכתובי הטקסט |
| R-34 | 🟠 **מדיניות 8192 טוקנים לא יושמה בקריאת ה-TTS** — `gemTtsGen` אינו מגדיר `maxOutputTokens` כלל, ולכן טקסט ארוך נקטע בצד המודל ומוחזר כ-`no-audio` → נפילה לקול הדפדפן. סותר את פסק הבעלים "כל קריאת AI עם 8192". | `app.js gemTtsGen` + מדיניות הבעלים | **Voice Wave 0** | ✅ **בוצע ב-v280** — `maxOutputTokens: 8192` על קריאת ה-TTS |
| R-35 | 🔴 **אין משתמש חינם ללא מפתח AI — פסק בעלים שלא תועד מעולם** (הובהר 31.7: "חד משמעית אין משתמש חינם ללא מפתח AI, הורדתי את זה כבר מזמן — חבל שלא מתועד בתוכנית"). **כל משתמש מגיע עם מפתח אישי (BYOK) או עם גישה מנוהלת.** המשמעות: מסלולי ה-`!aiAvail()` בקוד (‏**28 מופעים** ב-app.js) מניחים מצב-מוצר שאינו קיים — ובראשם נפילת ההקראה ל-`sysSpeak` (‏6516) ו"אנגלית בלי מפתח קוראת עברית" (‏6652). נדרש: (א) תיעוד ההחלטה כאינווריאנטת-מוצר; (ב) סקירת כל 28 המופעים — מה מהם מת, מה הופך להודעת-שגיאה כנה, ומה באמת נחוץ (למשל כשל רשת אמיתי, שאינו "אין מפתח"). | פסק בעלים 31.7 (מבדיקת v278 החיה) | **Voice Wave 0** (מסלול ההקראה) + **סקירה רחבה ב-Phase 9** (AI Module) לשאר המופעים | ✅ **מתועד ומאומת** — אין משתמש חינם ללא מפתח; 28 ענפי `!aiAvail()` נסקרו |
| R-36 | 🔴 **זמן תגובה של שאלות-ותשובות חופשיות — "קרוב לזמן אמת"** (דרישת בעלים 31.7). מדוד (`docs/analysis/2026-07-31-qa-latency-measured.md`): היום **5.7 שנ'** לתשובה של **1,488 תווים = ~106 שניות דיבור**. הגורם הדומיננטי **אינו** רמת החשיבה אלא **אורך התשובה**: הנחיית "ענה בקצרה, אתה עוזר קולי" הורידה ל-**1.29 שנ' ו-147 תווים** (פי 4.4 מהיר, פי 10 קצר). ‏**streaming** מביא משפט ראשון ל-**1.24 שנ'**. נדרש: (א) פרומפט-אורך ייעודי למסלול הקולי; (ב) streaming עם התחלת סינתזה על המשפט הראשון; (ג) קיצור זמן הסינתזה עצמה (~4 שנ' למקטע ראשון — הצוואר הבא). | מדידה 31.7 | **Voice Wave 0**: (א) הנחיית-אורך — ✅ **אושר 31.7**, נכנס למסלול הקולי · (ב) streaming — ✅ אושר, תלוי ב-R-37 (מסלול מתומחר) · (ג) קיצור הסינתזה — הצוואר הבא | ✅ **הושג ב-v282** — R-51 (מפריד SSE): שאלה רגילה ~1.4 שנ'; שאלת בטיחות ~4 שנ' **במכוון** (שער הספרות) |
| R-37 | 🔴 **‏streaming למשתמש מנוהל — התנגשות עם תיקון אבטחה** (‏§4): ‏`streamGenerateContent` **הוסר במכוון מה-Worker ב-Phase 1** (‏B19 — הוא עקף את תקרת הטוקנים ואיפשר שימוש לא-מתומחר). לכן: משתמש **BYOK** יקבל streaming מיד; משתמש **מנוהל** לא יקבל, עד שייבנה **מסלול streaming מתומחר** ב-Worker (מדידת טוקנים בזרימה + debit-first). **אין להחזיר את המסלול בלי מדידה — אחרת הפער שנסגר חוזר.** החלטת בעלים נדרשת. | מדידה 31.7 + `worker/index.js` (Phase 1 Task 6) | ✅ **GO — פסק בעלים 31.7:** "אפשר לטפל בהגנה בתוך הפרוקסי, וגם לכל משתמש יש טוקן כך שאפשר לחסום ברמת המשתמש ולא באופן גורף". כלומר: ‏**מסלול streaming מתומחר ב-Worker** — מדידת טוקנים תוך כדי הזרימה, חיוב פר-קוד, ו**אכיפה פר-משתמש** (ניתוק/חסימה של החורג) במקום הסרת המסלול לכולם. **קשת נפרדת אחרי Voice Wave 0.** | ✅ **בוצע ב-v282** — מסלול streaming ממודד ב-Worker (חיוב-לפני-שימוש, תקרה, התאמת-חשבון בניתוק) |
| R-38 | 🟠 **מדרג משתמשים (tiers) כמנגנון — **בולע גם את מחליף 3 המצבים beginner/home/pro (R-13, פסיקת בעלים 2.8.26): מנגנון-מצב אחד, לא שניים** — תשתית עכשיו, מדיניות עסקית בהמשך** (פסק בעלים 31.7: "כדאי לחשוב על מדרג משתמש כמנגנון… לכל מדרג הקצאה שונה כפונקציה של גודל המנוי"). **היקף עכשיו = תשתית בלבד:** (א) רשומת קוד-הגישה נושאת שדה `tier`; (ב) המכסות (`cap`, ‏rate-limit, ובהמשך הרשאת streaming) **נגזרות מטבלת מדרגים** ולא מקודדות פר-קוד; (ג) רשומה בלי `tier` נופלת למדרג ברירת-מחדל — **אפס שינוי התנהגות** למשתמשים קיימים; (ד) `central-code.mjs` יודע להנפיק/לשנות מדרג, ו-`audit` מציג אותו. **מחוץ להיקף עכשיו:** תמחור, שמות מסחריים, ‏UI, חיוב — אלה band-H/Paddle (‏D8) ויוכרעו עסקית. | פסק בעלים 31.7 (בהמשך ל-R-37) | **קשת ה-streaming המתומחר** (התשתית נבנית שם, כי שם ממילא נוגעים במדידה) · המדיניות → band-H | ⚠️R |
| R-39 | 🟢 **הזרמת אודיו עובדת כבר היום ב-endpoint הרגיל — נמדד 31.7** (פסק בעלים: "אין משתמשים קיימים, אנחנו בפיתוח; הכי חשוב הביצועים המקסימליים — רוצה את ה-API הייעודי של גוגל להזרמת אודיו הכי מהר שניתן"). **מדידה:** `streamGenerateContent` עם `responseModalities:[AUDIO]` על `gemini-3.1-flash-tts-preview` החזיר **185 פריימים, פריים ראשון ב-1,101ms**, סה"כ 4.36 שנ' ל-7.4 שנ' אודיו — מול **7,643ms עד הצליל הראשון** בקריאה החוסמת של היום. **לא נדרש ה-Interactions API** שהמחקר הצביע עליו; ה-endpoint הקיים מספיק. **המצטבר הצפוי: ~2.3 שנ' עד הצליל הראשון** (משפט ראשון בזרימת טקסט 1.24 שנ' + פריים אודיו ראשון 1.1 שנ'). | מדידה `scratchpad/phonikud/tts-stream-probe.mjs` | **v281 — עדיפות ראשונה בקשת ה-streaming** | ✅ **מומש ב-v282** — היה מוכח אך מושבת בשקט עד R-51 |
| R-40 | 🟢 **אין משתמשים קיימים — פיתוח בלבד** (פסק בעלים 31.7). נגזרות: (א) **תאימות-לאחור ומיגרציית אחסון אינן שיקול** — מותר לשנות מבני נתונים חופשית; (ב) הבעיה האלגוריתמית של חריגה באמצע זרימה (‏F5) **פחות קריטית** — ההכרעה "לא לקטוע" נשארת כי היא נכונה לגופה, אך אינה חוסמת; (ג) **המדידה ב-Worker עדיין נדרשת** — לא להגנה על משתמשים אלא על **המפתח והחשבון של הבעלים**; (ד) תשתית המדרגים (‏R-38) יכולה להישאר מינימלית. **הסדר החדש: ביצועים תחילה, תחכום המדידה אחר כך.** | פסק בעלים 31.7 | **משפיע על תעדוף כל הקשת** | ✅ **פסק מתועד** — אין משתמשים קיימים; שימש בהחלטות הגירה |
| R-41 | ✅ **מכסת ה-TTS שוחררה — Tier 1 החל בפועל** (‏1.8.2026). ההיסטוריה: ‏31.7 המודל `gemini-3.1-flash-tts-preview` היה חסום ב-`GenerateRequestsPerDayPerProjectPerModel=100` גם עם יתרה משולמת — כי המכסה **פר-פרויקט**, והפרויקט טרם שויך ל-Tier 1. אחרי שהבעלים הקים פרויקט `matkonetesh`, קישר חיוב והפעיל APIs — **נמדד: 6/6 בקשות רצופות עברו, ~2.2-3.1 שנ' כל אחת, אפס 429**. **מסקנה לתיעוד:** "יש לי חיוב" אינו מספיק — צריך לוודא ש**המפתח שבשימוש שייך לפרויקט שבו החיוב פעיל**, וזו הייתה הסיבה האמיתית לתקלה. | מדידה 31.7 + 1.8 | סגור | ✅ |
| R-42 | 🟢 **Google Cloud TTS כחלופה — נבדק חלקית, ממתין להפעלת API** (ליד של הבעלים 31.7): מוצר **GA** (לא preview) עם **‏38 קולות he-IL כולל 30 Chirp3-HD** (אותם שמות קולות כמו Gemini), **ללא תקרה יומית** (‏200 בקשות/דקה), חיוב **לפי תווים** (‏$4.50 ל-1,000 תשובות · מיליון תווים חינם בחודש ≈ 6,600 תשובות), ו**השומר הדבור ממשיך לעבוד ללא שינוי** (טקסט→אודיו, בניגוד ל-Live API שנפסל). דורש **חשבון שירות** — מפתחות API נדחים קטגורית (‏401 CREDENTIALS_MISSING). **מצב:** חשבון השירות הוקם ואומת (אסימון נוצר בהצלחה), **חסרה הפעלת `texttospeech.googleapis.com` בפרויקט** (‏403). | `docs/research/2026-07-31-cloud-tts-evaluation.md` | **החלטת בעלים אחרי מבחן האזנה** — כרגע לא דחוף (R-41 שחרר את החסם) | ✅ **מומש ב-v282** — Cloud TTS חי כמנוע משני (R-45); אומת: 200, 1,672ms, קול אמיתי |
| R-43 | ❌ **הניקוד נסגר סופית — נבדק על ארבעה צירופים, כולם נפסלו** (פסקי בעלים 31.7-1.8). המסלולים שנבדקו אמפירית עם אודיו אמיתי להאזנה: (1) **Phonikud + Gemini 3.1** → "כמעט ואין הבדל, לא מצדיק את המאמץ"; (2) **ניקוד עצמי של Gemini** → "לא הוסיף איכות"; (3) **Phonikud גולמי + Chirp3-HD** → **קטעי איות** (אובחן: 10 סימני הטעמה/מטג לא-תקניים שנועדו למנוע-פונמות ולא למנוע-טקסט; הקובץ היה הארוך ביותר — 64KB מול 55KB ללא ניקוד); (4) **ניקוד תקני בלבד + Chirp3-HD** (הטעמים סוננו, 66 סימנים, 0 טעמים) → **"עדיין לא מספיק טוב"**. **מסקנה: איכות ההגייה העברית אינה נפתרת בניקוד, בשום מנוע ובשום וריאנט.** לא לפתוח מחדש בלי מנוע חדש מהותית. | ארבעה מבחני האזנה עיוורים, 31.7-1.8 | סגור — `R-cancelled` | ❌ |
| R-44 | ✅ **החלטת מנוע ההקראה: נשארים על `gemini-3.1-flash-tts-preview`** (פסק בעלים 1.8 מהשוואת האזנה). הנימוק: **ההגייה העברית שלו הטובה ביותר** מכל המועמדים שנבדקו — טוב יותר מ-Chirp3-HD, מ-2.5-flash ומ-2.5-pro. הטרייד-אוף המודע: **פי-9 עד פי-13 איטי יותר** מ-Chirp3 (~10 שנ' מול 757ms), **ותקרת preview** — שכבר אינה חוסמת אחרי R-41. **המסקנה המעשית: המהירות נפתרת ב-v281 (זרימה), לא בהחלפת מנוע.** ‏Cloud TTS/Chirp3 נשאר מתועד כחלופה מוכנה (R-42) אם המכסה תחזור להציק או אם המהירות תוכיח את עצמה כחשובה יותר מהניואנס בהגייה. | מבחני האזנה 31.7-1.8 | סגור | ✅ |
| R-45 | 🔴 **שכבת ספקי-הקראה: Gemini ראשי · Cloud TTS/Chirp3 משני** (פסק בעלים 1.8: "בוא נשתמש ב-Gemini כאופציה ראשונה ו-Chirp כאופציה שנייה — או למצב שהראשונה לא עובדת, או לשימוש במקרים שונים בתוך המערכת שתכיל הרבה ממשק קולי"). **שתי תכליות נפרדות:** (א) **נפילה אוטומטית** — כשל/‏429/‏timeout ב-Gemini → Chirp3 (‏757ms, ללא תקרה יומית). זה סוגר בדיוק את מחלקת התקלה של 31.7, שבה ההקראה מתה לגמרי; (ב) **ניתוב לפי מקרה-שימוש** — אישורים/הודעות קצרות → Chirp3 (מהיר וזול); תשובות בטיחות ארוכות → Gemini (ההגייה הטובה ביותר, R-44). **נדרש:** רגיסטר ספקים במתכונת רגיסטר-המודלים הקיים (יכולות + נתיב + אישור), חשבון-שירות ב-Worker עבור Cloud TTS, ומדיניות ניתוב מוצהרת ובדיקה. **אילוץ:** השומר הדבור והאינווריאנטה INV-T חלים על **כל** ספק — אין ספק פטור. | פסק בעלים 1.8 (אחרי מבחני ההאזנה R-42/R-44) | **v281 — נכנס לקשת ה-streaming** | ✅ **בוצע ושוחרר ב-v282** — Gemini ראשי + Cloud משני; נפילה = 2 בקשות בדיוק, נמדד |
| R-46 | 🟠 **הגיית ספרות אינה מספקת** (בעלים, 1.8: "הביטוי של ספרות גם לא מספיק טוב"). **קריטי לבטיחות** — מספר שנשמע שגוי הוא ערך בטיחות שגוי, לא פגם אסתטי. נבדקו שלוש צורות על gemini-3.1: `95°C` (‏v280 היום) · `95 מעלות` · `תשעים וחמש מעלות` (מילים). **סימן מדיד:** צורת המילים הייתה **המהירה ביותר לייצור בכל שלושת המקרים** (‏5.7 מול 6.7 שנ' · 2.7 מול 4.8 · 5.4 מול 6.1) — כלומר המנוע מתאמץ על ספרות וסמלים. **ממתין להכרעת האזנה של הבעלים.** אם המילים מנצחות: ממיר מספרים→עברית במסלול הקולי עם **התאמת מגדר** ("שישים ושלוש מעלות", לא "שלושה"), טווחים, שברים ויחידות — ובדיקה שכל ערך בטיחות נאמר במלואו. **אינו סותר את R-33** (שם נפסלה הזרקת פסיקים; כאן מדובר בהמרת מספרים, שכתוב אחר לגמרי). | מבחן האזנה 1.8 | סגור — **ללא ממיר מספרים** | ✅ **פסק בעלים 1.8:** הצורה המאויתת-במילים של הטווח («שמונה עד עשר שעות במאה ועשר מעלות») **נשמעה פחות טוב**; שלוש צורות הספרות («95°C» · «95 מעלות» · «תשעים וחמש מעלות») היו כולן **"סביר +"**, ומתוכן **הבעלים בחר ב-A** (‏1.8): *"‏A הכי מדויק, כולל יחידות צלזיוס"* — כלומר המנוע **מבטא את סימן היחידה** ולא בולע אותו, וזו בדיוק התכונה הבטיחותית (צלזיוס מול פרנהייט נשמע באוזן). **מסקנה: לא בונים ממיר מספרים→מילים** — הוא מזיק דווקא במספרים הגדולים. **נשארים על הספרות כפי שהן (התנהגות v280)**, ובמיוחד עם **סימן היחידה המפורש `°C`** — כי הוא מבחין צלזיוס מפרנהייט באוזן, וזו הבחנה בטיחותית. שיפור עתידי בהגיית ספרות ייבחן רק מול מנוע/ספק אחר, לא בשכתוב טקסט. |
| R-49 | 🟡 **גיבוי לסיכון השיורי של R-48** — הנחיית "ספרות בלבד" **מקטינה** את הסיכון ואינה מאפסת אותו: מודל עלול לא לציית. הגיבוי המוצע: כשתשובה נוגעת לבטיחות **ואין בה ולו ספרה אחת**, לצרף הסתייגות כללית (בלי גלאי למספרים מאויתים בעברית — ראו למה ב-R-48). זול, אך דורש הגדרה זהירה של "נוגעת לבטיחות" כדי לא לתייג פרוזה תמימה. **נמדד 1.8 אחרי R-48 (6 שאלות בטיחות חיות): ציות 6/6 בטמפרטורות — כל מספר בספרות.** **אבל הממצא המדויק צר יותר ממה שהונח:** ה**זמנים** עדיין נכתבים במילים — "בתוספת **שלוש דקות** מנוחה", "**שעה וחצי עד שעתיים**". שלוש דקות המנוחה הן **ערך בטיחות לפי USDA**, לא קישוט, ולכן החור לא נסגר לגמרי אלא הצטמצם לפינה אחת: **ערכי זמן**. הגיבוי מתמקד מחדש — או להרחיב את סעיף ההנחיה כך שיכלול במפורש זמני-בטיחות (מנוחה, זמן/טמפ' בסו-ויד), או ההסתייגות הכללית. | נגזר מ-R-48, נמדד 1.8 | הרחבת סעיף ההנחיה (זול) לפני ההסתייגות הכללית | 🟡 ממוקד-מחדש |
| R-50 | 🔴 **הצוואר האמיתי בזמן התגובה הוא רצפת החשיבה, לא הסטרימינג** — נמדד על האפליקציה החיה אחרי ש-Task 4 נחת: `firstSound` חציון **4,840ms** מול רף 3,000. השורש: ב-`thinkingLevel:'low'` המודל פולט את התו הראשון רק אחרי **4,177ms**; ב-`minimal` — אחרי **1,028ms**. הסטרימינג תקין ורץ (שתי הקריאות הן `streamGenerateContent`), פשוט **אין מה לשדר** עד שהחשיבה נגמרת. ההפרש טקסט-ראשון→תשובה-מלאה הוא ~400-650ms בלבד, כלומר הנחת היסוד של הקשת נכונה רק חלקית. **איכות נמדדה ולא הונחה:** 4 שאלות בטיחות עם תשובה ידועה מ-USDA — **4/4 נכונות בשתי הרמות**, ‏`minimal` מהיר פי 2-3. חשבון לרף: 1,028+1,100 ≈ **2.1 שנ'**. **אזהרה מוצהרת: מדגם 4 שאלות בלבד** — ראיה להמלצה, לא הוכחה; הרצפה קיימת מטעמי בטיחות ומגיע לה מבחן הוגן עם שאלות מטעות. `docs/analysis/2026-08-01-thinking-latency-is-the-bottleneck.md` | מדידת D1 חיה, 1.8 | **נסגר 1.8: להשאיר את `ask` על `low`.** ‏204 קריאות חיות, 17 שאלות מכשילות, 3 שפות, 4 רמות. `low` נכון ובטוח ב-100% — כולל שלוש השאלות המטעות. `minimal` נסדק (פתח ב"כן, אבל..." במקום "לא"). **`medium` הכניס פגם חדש** — טקסט חשיבה גולמי דלף לתשובה הגלויה ב-2/3 ריצות. **ותיקון-אמת:** הנתון "פי 2-3" שלי נמדד **בלי `google_search`** ואינו משחזר בייצור (4,955 מול 4,872ms) — מדדתי מסלול שאינו זה שרץ אצל המשתמש. | מדידת D1 חיה + מחקר R-50 | **המצב הקיים נכון — אין שינוי** | 🟢 סגור |
| R-51 | 🔴 **כל ארכיטקטורת הסטרימינג הייתה מושבתת בשקט — מפריד מקטעי SSE** — Google מפריד מקטעים ב-` `; הקוד מחפש ` ` ולעולם לא מוצא. **הוכח על התיל החי:** `CRLFCRLF=1, LFLF=0`. **המדידה באפליקציה:** כל הקראה מנסה סטרימינג, נכשלת ב-`no-audio` אחרי ~1.8 שנ', ואז משלמת שוב על המסלול החוסם — משפט בן 28 תווים עלה **~4.5 שנ'** במקום ~2.1. אינסטרומנטציה הראתה `gemB64Bytes` נקרא **0 פעמים**. משפיע על R-39 (אודיו, v281) **וגם** על Tasks 2/4 (טקסט). **למה הבדיקות פספסו:** הן מזינות ` ` משלהן — אישרו את הנחת הקוד ולא את פורמט השרת (בדיוק `verify-against-the-runtime-path`). | מדידת D1 חיה, 1.8 | **תוקן מיד** | 🔄 בתיקון |
| R-52 | 🔵 **סקר-רוחב: היכן המערכת צריכה לדבר, ומי שולט בזה** (הוראת בעלים, 1.8) — פסיקה אסטרטגית: **רוב השימוש הקולי הוא הקראה יזומת-מערכת** שהטריגרים שלה נגזרים מזמן בישול וממצבי מערכת; אינטראקציית שאלות-ותשובות היא **קטנה עד זניחה** בהשוואה. נגזר: (א) **מיפוי מעמיק של כל האפליקציה** — כל מקום שבו נדרש או מומלץ פלט קולי **בנוסף** לחיווי הטקסטואלי, עם הטריגר, הדחיפות והסיכון בהחמצה; (ב) **שכבת קונפיגורציה** שמאפשרת למשתמש להחליט **מתי והיכן** יופעל פלט קולי. **טרם עוצב — נדרש brainstorming ואישור בעלים לפני קוד** (§1 HARD-GATE). מחליף את ההנחה הקודמת שהקול משרת בעיקר Q&A, ומשנה סדרי עדיפויות: זמן ההקראה (~1.1 שנ' אחרי R-51) חשוב יותר מזמן תשובת ה-AI. | הוראת בעלים 1.8 | **קשת חדשה — לתכנון** | ✅ **בוצע ושוחרר ב-v285** — 5 קטגוריות + בטיחות מחוץ למנגנון, כל דיבור עם עקבה חזותית ויומן |
| R-53 | 🔴 **השומר מוחק מספר שמאומת מול המקור שלנו — כי הופיע לצד מספרים אחרים** (דיווח בעלים 1.8, שוחזר) — לשאלה "אני מבשל אסאדו, מה הטמפרטורה הבטוחה?" המודל ענה **נכון**: 63°C מינימום, 71°C מלא, ~93°C לרכות. **כל שלושת המספרים נמחקו ל-`[…]`.** האפליקציה **מזהה** אסאדו (`cut-2`, tiers.t2 מלא) ו-`data.py:7` נותן **`safe=63`** — כלומר **63°C הוא בדיוק הערך המצוטט שלנו**, והוא נמחק. הסיבה: כלל הכשירות דורש שהתשובה תכיל **מספר אחד בלבד** (נועד לחסום טענות מורכבות כמו טווחים). התוצאה: השאלה המרכזית ביותר של המוצר חוזרת **ריקה**. **המלצה: לא להרפות את השומר** (הכלל נולד אחרי ששלושה תיקונים תלויי-תחביר נכשלו על "63°C-74°C") **אלא להוסיף החלפה**: כשהכול נמחק ויש לנו ערך מצוטט לפריט המזוהה — לצרף אותו בשם המדריך ("לפי המדריך, הטמפרטורה הבטוחה לאסאדו: 63°C"). לא מרחיב את מה שמותר לומר; מוסיף מקור מאומת במקום שקט. | דיווח בעלים 1.8, שוחזר עם מפתח חי | **החלטת בעלים (§4/H13)** | ✅ **בוצע ושוחרר ב-v283** — החלפה בערך המצוטט, מוגבלת לפריט נקוב בשאלה |
| R-54 | 🟢 **תקרת הבקשות ל-TTS כבר אינה אילוץ תכנוני** (פסק בעלים + אימות שלי, 1.8) — מפולת ה-429 מ-v281 לא הייתה במכסה היומית אלא ב**מגבלה לדקה** של ה-free tier: הבקשה השנייה באותה דקה כבר נחסמה. אחרי המעבר ל-**tier 1** (ו-Cloud TTS עם מכסת חינם נדיבה) — **נמדד: 10 בקשות רצופות ב-22 שנ', כולן 200, אפס 429** (~27/דקה). **משמעות ל-R-52:** דיבור יזום (~48 הכרזות פרושות על שעות) **אינו מוגבל בבקשות**, ואין צורך לעצב את הארכיטקטורה סביב תקרה. **מה שכן נשאר:** איחוד המקטעים מ-`0bee32f` לא מבוטל — הוא כבר לא הכרחי אך עדיין חוסך עלות ונקודות-כשל. | פסק בעלים 1.8 + מדידת burst | **האילוץ הוסר מהתכנון** | 🟢 סגור |
| R-55 | 🔴 **דליפת עברית בדיבור לכל שפה שאינה אנגלית** (דיווח בעלים 1.8, v282, ברוסית) — שכבת הקול בונה מחרוזות מדוברות בתנאי **בינארי** `vcVoiceLang()==='en' ? EN : HE` במקום דרך `L()`. עבור ru/fr/de/es/it זה נופל **לעברית**. המקרה שדווח: כפתור "מתי הבא?" (`qwhen`, app.js ~7402) — `המשימה הבאה בשעה ...` בעברית, בעוד **תווית המשימה עצמה מתורגמת** — ולכן נשמע משפט עברי עם מילים רוסיות בתוכו. **מחלקה מוכרת שלא נסרקה:** אותה צורה בדיוק תוקנה ב-R-36a/T9 לשורת השאלה, בלי סריקה של השאר. | דיווח בעלים 1.8 | **בתיקון — סריקה מלאה של המחלקה** | ✅ **בוצע ושוחרר ב-v283** — 7 אתרים + באג לוקאל TTS |
| R-56 | ✅ **התראת ה-bcheck קופצת מחדש בכל כניסה לאירוע — האישור אינו נזכר** (דיווח בעלים 1.8, רגרסיה ב-D2 שנשלח ב-v282) — `scheduleBcheckDue` נעל מפני ירי כפול עם `if(_bcheckDue()[key]) return;`, אבל `ackBcheck` **מחק** את המפתח, כך ש"מעולם לא הוצג" ו"הוצג ואושר" נראו זהים. **תוקן:** המפתח כעת `s.tid+'@'+s.start.getTime()` — צמוד למופע (חותמת הזמן של השלב), לא רק לזהות השלב; `ackBcheck` מסמן `acked:true` במקום למחוק, כך שה-guard ממשיך לזהות "כבר הוצג" בלי לאבד את ההבחנה מ"אף פעם לא הוצג" — ובישול **חדש** של אותו פריט (מועד `start` חדש מ-`planSchedule`) מקבל מפתח חדש ומתריע שוב, כנדרש משער בטיחות. **תסמין שני** (הכרטיס בלי הקשר-אירוע): תוקן — `renderBcheckAlarm` מוסיף כעת את שם האירוע לכל שורה דרך `timerEventName(w.tid)`, באותו תבנית כמו כרטיס הטיימרים; ההצפה הגלובלית לא שונתה. בדיקות: `tests/d2-bcheck-alert.spec.ts` — 7/7 ירוק (RED→GREEN מתועד, מחזור רגרסיה תוקן/נפרד/שוחזר, מקרה שלילי לבישול חדש, התמדה אחרי reload, שם אירוע ב-DOM). צילום מסך 390×844: `mockups/r56-bcheck-with-event-name-390x844.png`. | דיווח בעלים 1.8 על v282 | **נסגר** | ✅ תוקן |
| R-57 | 🟠 **אירוע תקוע בעבר הוא מצב לא-חוקי — נדרש מחזור-חיים** (הצעת בעלים 1.8) — אירועים נשארים במצב "בישול" זמן רב אחרי שמועד ההגשה חלף. כל חישוב מבוסס-"כמה נשאר" מייצר ערכים חסרי משמעות, והמערכת מתחילה להתריע על העבר. **קשר ישיר ל-R-56:** `scheduleBcheckDue` יורה כש-`ms<=0`, ולכן באירוע שחלף **כל בדיקות הטמפ׳ של כל הפריטים פגות בבת אחת** — זה מקור "רשימת הפריטים לאישור" שהבעלים ראה בכניסה, ולא מקרה. **התכנון (הצעת הבעלים, אומצה):** מועד עבר ⟵ האירוע עובר למצב **"דורש עדכון"**; החזרה לפעילות מחייבת מועד הגשה חדש והפעלה מחדש; **ובינתיים לא יורות ממנו התראות מבוססות-זמן** — התראה על מועד שחלף אינה מידע. **טרם עוצב:** נדרשת הכרעה על סף ה"פג" (מיד? שעות? יממה?), על מה קורה לסימונים ולטיימרים הקיימים, ועל האם סגירת אירוע היא מצב נפרד מ"דורש עדכון". **הוכרע 1.8 (בעלים):** ‏(1) סף פגות = **12 שעות** אחרי מועד ההגשה — בישול אמיתי חורג בשעה-שעתיים ולפעמים אוכלים מאוחר; 12 שעות תופס אירוע שנשכח עד למחרת בלי לסמן אירוע חי · (2) בעדכון מועד — **לשאול את המשתמש** אם להמשיך מאיפה שהפסיק או להתחיל מחדש; המערכת אינה יכולה לדעת לבד · (3) **שני מצבים נפרדים** — "הסתיים" אינו "דורש עדכון"; אירוע שבושל והוגש בהצלחה אינו אירוע שנשכח, והיסטוריית בישולים היא נכס · (4) **מתוכנן יחד עם R-52** ב-spec אחד. | הצעת בעלים 1.8 + 4 הכרעות | **תכנון משותף עם R-52** | ✅ **בוצע ושוחרר ב-v285** — מחזור-חיים לאירוע: 12 שעות, "הסתיים"≠"דורש עדכון", bcheck רק אם הבישול התחיל |
| R-58 | 🔴 **שאלה על קטגוריה (עוף) אינה מקבלת החלפה** (דיווח בעלים 1.8, v283) — "טמפרטורת בטיחות עוף" החזירה 3 מופעי `[…]` בלי מספר. **הסיבה:** `askFindEntity` מחזיר **מערך ריק** — "עוף" אינה פריט בקטלוג אלא **קטגוריה** (`cat`), ולכן ההחלפה של R-53 (שדורשת פריט נקוב) לא פעלה. **הנתונים נותנים כלל נקי:** עוף 6 פריטים כולם `safe=74` · הודו/אווז/ברווז 74 · חזיר/טלה/דג/פירות-ים 63 — **אחידים**. אבל **בקר 63 ו-71** ו**איברים פנימיים 63/65/72/74** — **מעורבים**. **הכלל: להשיב ברמת קטגוריה רק כשה-`safe` אחיד בכולה; אחרת לשתוק** — ההבדל 63/71 בבקר הוא בדיוק נתח-שלם מול טחון, וקביעה גורפת שם מסוכנת. גם `safe=0` (ירקות/פירות) אינו טמפרטורת בטיחות ויש להחריגו. | דיווח בעלים 1.8 | **תיקון מיידי** | ✅ **בוצע ושוחרר ב-v284** — קטגוריה אחידה בלבד; בקר/איברים שותקים |
| R-59 | 🟠 **תשובת AI קודמת נשארת על המסך אחרי החלפת שפה** (דיווח בעלים 1.8) — תשובה שנשאלה בעברית ממשיכה להופיע אחרי מעבר לרוסית. **פסק הבעלים: למחוק, לא לתרגם** — תמלול הוא רשומה של שיחה שהתקיימה בשפה מסוימת, ותרגום בדיעבד יוצר טקסט שאיש לא אמר. | דיווח בעלים 1.8 | תיקון | ✅ **בוצע ושוחרר ב-v284** — תמלול נמחק בהחלפת שפה |
| R-60 | 🔴 **"הדלקת מעשנת (חימום מוקדם)" — שגיאת הקראה** (דיווח בעלים 1.8, v283) — הקראת המשימה הזו נכשלת עם "שגיאת הקראה נסה שוב". טרם אובחן; חשד ראשוני לתוכן הטקסט (סוגריים? משימה ללא טמפ׳/זמן?) או למסלול שלב-ללא-נתונים. **לא שוחזר — נבדק בשלוש שכבות (1.8):** ‏(א) סוכן עקב אחר צינור הלקוח המלא (`vcCurrentText`→`ttsText`→`vcChunkText`→`vcCoalesceTtsChunks`→בקשה) על כל וריאנטי `PREHEAT` — אפס חריגות, אפס מקטעים ריקים · (ב) **בקשה חוסמת חיה** עם התווית המדויקת: 200, ‏151KB אודיו, `finishReason:STOP` — וגם בלי סוגריים, וגם סוגריים לבד · (ג) **בקשת סטרימינג חיה**: 78 מקטעי אודיו, צליל ראשון 1,267ms. **מסקנה: כשל רגעי (רשת/API)** שהוצג דרך הודעת השגיאה הגנרית. **הפעולה שכן נדרשת אינה ניחוש תיקון אלא הפיכת המקרה הבא לניתן לאבחון:** סיבת השגיאה האמיתית נרשמת ב-`console.warn` ואינה מוצגת למשתמש לעולם — ולכן אין דרך לדעת מה נכשל. | דיווח בעלים 1.8 | **להחליף ניחוש-תיקון בחשיפת סיבת השגיאה** | 🟡 לא שוחזר |
| R-61 | 🟠 **המספר המאומת נגרר בסוף במקום להוביל** (דיווח בעלים 1.8 על R-53) — ההחלפה עובדת, אבל הבעלים ציפה שהמספר **ישובץ בגוף המשפט**. **שיבוץ במקום אינו אפשרי בבטחה:** התשובה על אסאדו הכילה 3 מספרים (63 בטיחות · 71 בישול-מלא · 93-95 מרקם) והמערכת **אינה יודעת איזה `[…]` הוא הבטיחותי** — זו בדיוק בעיית סיווג-הטענה (G-A2) שהרישום מצהיר עליה כקשה-מהותית. שיבוץ שגוי ייצר "המשך לבשל עד 63°C כדי שהשומן יימס" — משפט סמכותי ושגוי, **גרוע מהמצב היום**. **ההצעה החלופית: כשכל המספרים נמחקו, גוף ההודעה כמעט חסר מידע ("עד […] עד […]") — להוביל במשפט המאומת** ("הטמפרטורה הבטוחה לאסאדו היא 63°C לפי המדריך") ורק אז את ההסבר. עונה על הצורך בלי לגעת בסיווג הטענה, ונשמע נכון גם בקול שבו אי-אפשר לחזור אחורה. | דיווח בעלים 1.8 | **אושר 1.8 בתנאי R-62** — לא ניתן לשילוח לבדו | ✅ **בוצע ושוחרר ב-v285** — הערך המצוטט מוביל כשהכול נמחק |
| R-62 | 🔴 **הפרדת "מספר בטיחות" מ"מידע מספרי רגיל" — פסק בעלים 1.8, שינוי מדיניות בשומר** — הבעלים אישר את R-61 **בתנאי שהשומר יפסיק למחוק מספרים שאינם בטיחותיים**: *"כמעט כל שאלה שיש בה אלמנטים של זמן וטמפרטורה לא תענה — מה שאני לא מוכן שיקרה"*. **העיקרון שנקבע:** מספרי **בטיחות** מגיעים **מטבלאות הבטיחות שלנו בלבד**; **כל מידע מספרי אחר** (זמני עישון, טמפ׳ תא, משקלים, מרווחים) חופשי מכל מקור, כולל האינטרנט. **האסימטריה שמכתיבה את התכנון:** סיווג-יתר = מספר נמחק מיותר (מציק); סיווג-חסר = **טמפרטורת בטיחות לא-מאומתת נאמרת כעובדה**. לכן הסיווג חייב **להיכשל לכיוון הבטיחות** — ספק ⟵ בטיחות. **מלכודת מזוהה מראש:** תיוג לפי מילות-מפתח ("בטוח"/"פנימי"/"ליבה") הוא **בדיוק הגישה תלוית-התחביר ששלושה תיקונים קודמים נכשלו בה**, כל אחד בניסוח אחר (הרישום ב-`vcGuardSpoken` מתעד זאת). נדרש תכנון שאינו נשען על ניסוח בלבד. **תלות: R-61 אינו ניתן לשילוח לבדו** — הבעלים התנה אותו בזה. **שינוי ספק למנגנון P0 — עובר דרך spec, לא טלאי.** **כיוון הפתרון (הצעת בעלים 1.8, אומצה):** ‏**AI במצב חשיבה מחלץ מהתשובה את הנתונים הבטיחותיים**, ואז **הקוד שלנו משווה מול הטבלאות** — התאמה ⟵ השומר מאשר. **למה זה עוקף את המלכודת:** הסיווג נעשה לפי **משמעות** ולא לפי ניסוח, ולכן אינו תלוי-תחביר כמו שלושת התיקונים שנכשלו. **מקור האמת נשאר אצלנו** — ה-AI מסווג בלבד. **הבהרה מחייבת (הבקר):** זו שכבת **אישור**, לא שכבת **החלטה** — היא יכולה רק להוסיף אישור, לעולם לא להסיר הגנה. כשל API, JSON פגום, סכימה לא תקינה או ביטחון נמוך ⟵ **חוזרים להתנהגות השמרנית של היום**. **שתי שאלות פתוחות לתכנון:** ‏(1) מעבר **נפרד** (ביקורת עצמאית תופסת שגיאות שהכותב מפספס, במחיר 1-2 שנ' — בדיוק מה שקשת R-51 חסכה) מול **תיוג באותה קריאה** (אפס עלות זמן, תיוג-עצמי חלש יותר); ‏(2) האם להתנות בערוץ — בקול הזמן קריטי, במסך פחות. | פסק בעלים 1.8 + כיוון פתרון | **ל-spec המשותף עם R-52/R-57** | ✅ **בוצע ושוחרר ב-v285** — מסווג AI + טבלת החלטה; שכבת אישור בלבד, כשל מחזיר להתנהגות היום |
| R-63 | 🟢 **פסק אסטרטגי: שאלות AI הן המסלול הנדיר — מוכנים לשלם עליהן זמן** (בעלים, 1.8) — רוב השימוש הקולי הוא הקראה יזומת-מערכת; **שאלה היא המקרה הפחות שכיח**. לכן: **תקציב הזמן לתשובה נדיב במכוון**, והאישור הקולי המיידי ("רגע, בודק") הוא מה שקונה אותו — המשתמש יודע שנשמע ומחכה לתשובה **טובה**, לא **מהירה**. **שלוש נגזרות מיידיות:** ‏(1) **R-50 מתהפך** — השאלה "להוריד את רצפת החשיבה כדי לחסוך 2-3 שנ'" מיותרת; הורדת רצפת בטיחות כדי לקנות מהירות שאיננו צריכים היא עסקה גרועה. המחקר הופנה לשאלה ההפוכה: **האם רמה גבוהה יותר מייצרת תשובות טובות/בטוחות יותר** (minimal/low/medium/high, בטיחות כציר ראשי). (2) **R-62 משתחרר** — מעבר אימות **נפרד** במצב חשיבה כבר אינו בעייתי מבחינת latency, וזו הייתה השאלה הפתוחה העיקרית בתכנון שלו. (3) **latency נמדד ומדווח, אך אינו הציר שמכריע** בנתיב ה-Q&A. **‏זמן ההקראה נשאר קריטי** (~1.1 שנ' אחרי R-51) — ההבחנה היא בין שני המסלולים, לא ויתור גורף. | פסק בעלים 1.8 | **מנחה את R-50/R-62 ואת ה-spec המשותף** | 🟢 סגור |
| R-64 | 🔴 **‏n=35 מעשן ב-60°C, מעל יעד הסו-ויד שלו 57°C — ובלי מקור מצוטט לטמפרטורת העישון** (פסיקת בעלים 2.8.26: שורה נפרדת, לא נוגעים במספר) — חריג מול n=36/n=60 (55° < 57°). ה-`ref` הוא Baldwin Table 5.1, טבלת זמני-פסטור שאינה מכילה טמפרטורת עישון כלל. **אסור לנחש מספר בטיחות**: ההכרעה מחייבת קריאה במקור ראשוני — USDA/FSIS *Smoking Meat and Poultry* ו/או 9 CFR §318.7/§381.147 | `2026-08-02-R8-R15-safety-diagnosis.md` | **גל 0 — לפני החלפת המנוע** | 🔴 פתוח |
| R-65 | 🔴 **סריקת קטלוג מלאה מול ה-AI — כל 279 הפריטים, שאלה פשוטה אחת, מה באמת חוזר** (הוראת בעלים 2.8.26). שלושת הדיווחים מהשטח (אסאדו/כבש/מרגז) נמצאו **ביד** ולימדו יותר מכל בדיקה שנכתבה — הסריקה הופכת את זה משיטתי במקום מקרי. פלט: טבלה פר-פריט — האם נפתר, לאיזה פריט, איזה ערך הוצג, והאם הוא הנכון. **חייבת לרוץ אחרי סולם-הישויות**, אחרת היא מודדת קוד שעומד להשתנות | הוראת בעלים 2.8.26 | **גל 0 — מיד אחרי סולם הישויות** | 🔴 פתוח |
| R-66 | 🟠 **הרחבת הקטלוג לצורת החיתוך של הנתח** (הוראת בעלים 2.8.26, "מתישהו — הצורה עדיין לא ברורה לי"). אותו נתח בצורות חיתוך שונות הוא היום פריטים נפרדים או חסר לגמרי: **אנטריקוט מול סטייק אנטריקוט** · **פילה בקר מול מדליוני פילה** · ובעוף — **שלם, כרעיים, שוקיים, כנפיים, פרגיות, חזה**. נוגע ישירות בפתירת-נושא (שאלה על צורת חיתוך צריכה להגיע לנתח הנכון) ובזמני הבישול, שכן משתנים לפי הצורה. **דורש brainstorming ופסיקת בעלים לפני תכנון — צורת המודל לא הוכרעה** | הוראת בעלים 2.8.26 | **משימת-דיון → brainstorming** (טרם משובץ) | 🟠 מאושר-לדיון |
| R-67 | 🟠 **"שאלות שאינן בטיחות — בלי הגבלה"** (פסיקת בעלים 2.8.26). כלל "בדיוק מספר אחד" (`app.js:8472-8487`) נותן ל**מספר** המספרים במשפט להכריע את גורלם של מספרים שאינם טענות בטיחות כלל — בתשובת הכבש היו חמישה, ולכן **כולם** נמחקו. לצמצם כך שיחול על טוקנים נושאי-בטיחות בלבד. **גבול קשיח: אסור להחליש שום מחיקת בטיחות, ו-`claims===null` נשאר בייט מול בייט** | פסיקת בעלים 2.8.26 | **בטיפול בסולם הישויות; אם לא ניתן שם בבטחה — שורה עצמאית** | ✅ **הופרך כסיבה (נמדד 2.8.26)** — עם מפת-טענות בצורה החיה, מספרים שאינם בטיחות **עוברים מילולית** (`3 דקות`, `200°C`, `10 דקות`). מה שמחק את מספרי הכבש היה **כשל קשירת-נושא**, שתוקן ב-R-63. צמצום כלל-הספירה ללא `claims` הוא בדיוק ההיוריסטיקה הלקסיקלית ששלושה תיקונים קודמים מתו עליה, ושובר את `claims===null` בהגדרה — **לא נעשה, במכוון** |
| R-68 | 🟠 **שורת "מספר זה אינו מאומת — בדוק בכרטיס הפריט" סותרת ערך מאומת** — נצפתה ב-**14/14** ריצות חיות, כולל תשובות שבהן המשפט המוביל כבר סיפק ערך מצוטט; רק מסלול "אין לנו נתון" משמיט אותה נכון. **פסיקת בעלים 2.8.26: מאושר** — השורה תופיע רק אם באמת נותר בתשובה משהו לא-מאומת | `2026-08-02-v288-live-verification.md` | **מיידי** | 🟠 בביצוע |
| R-69 | 🔴 **המודל חולק על ערך בטיחות מצוטט שלנו — ומי צודק אינה שאלת תצוגה** (שקדי עגל: המודל 74°C, אצלנו 65°C). ההצעה להחליף במספר שלנו נדחתה כלא-מספיקה: איברים פנימיים נושאים הנחיות נפרדות ולרוב מחמירות, ולכן **ייתכן שהציטוט שלנו הוא השגוי**. **פסיקת בעלים 2.8.26: פתוח וממתין לתוצאות R-65** — אם הסריקה תראה קבוצה שלמה שבה המודל גבוה מאיתנו, זו בעיה בציטוטים ולא בתצוגה, וחמורה מכל מה שתוקן ב-2.8. **אסור לנחש מספר בטיחות; הכרעה רק מול מקור ראשוני** | `2026-08-02-v289-live-verification.md` §5 | **החלטה אחרי R-65** | 🔴 ממתין-לסריקה |
| R-70 | 🔴 **‏48 טענות בטיחות שגויות או לא-מגובות — נמצא בסריקת R-65** (‏`docs/analysis/2026-08-02-catalog-ai-sweep.md`, כיסוי 279/279). שלוש צורות חדשות, כולן דטרמיניסטיות ושלנו: **F** ‏`0°C` מוצג כטמפרטורת בטיחות ב-26 ירקות/פירות (הנתון `safe=0` הוא קידוד "לא רלוונטי", והקוד קורא אותו כמספר) · **G** חותמת "לפי המדריך המאומת" מוחלת שלא כדין — 15 פריטים. **⚠️ תיקון לניסוח הראשון של השורה הזו:** נכתב כאן ובדיווח לבעלים ש-85°C לשום "אינו ה-`safe` של אף שורה" ושזו הפרת `baldwin-backbone.md`. **זה לא נכון** — ‏85°C הוא `cut-102.tgt`, טמפרטורת יעד-מרקם שלנו, ולא מספר מומצא. שני פגמים אמיתיים ואחרים: ‏(א) החותמת נקשרה לנושא ה**טענה** ואיש לא שאל אם זה נושא ה**שאלה** (דרוורס קיבל 74°C מ-`duck`, רוקפור קיבל 63°C של בקר); ‏(ב) החותמת הוחלה לפי **שוויון מספרי** בלי לשאול מה המשפט טוען, כך שטמפרטורת מרקם קיבלה חותמת בתוך משפט בטיחות · **I** נושא התשובה מוחלף במאכל אחר (אגס⟵אווז, קלבריה⟵ברי, רוקפור⟵63°C של בקר מסומן כמאומת). **פסיקת בעלים 2.8.26: קודם לכל עבודת מוצר** | `2026-08-02-catalog-ai-sweep.md` | **מיידי** | 🔴 בביצוע |
| R-71 | 🟠 **בטיחות צ'רקוטרי אינה טמפרטורה — הערת בעלים 2.8.26** — לגבי 14 פריטי נקניק מיובש/מעושן, ג'רקי ובייקון: *"להבנתי הבטיחות שם לא עוסקת בטמפרטורה אלא ב-cure#1, cure#2"*. כלומר `safe` (שדה טמפרטורה) הוא כנראה **השדה הלא נכון** לפריטים אלה, ו"חסר" מתאר את המצב באופן שגוי. השומר **כבר מכיר** `cure_ppm` כסוג-טענה אך אין לו מול מה לאמת אותו. מחקר מקורות ראשוניים רץ: מה באמת שולט (‏nitrite/nitrate ppm, ‏a_w, ‏pH, degree-hours, ‏9 CFR §318.7/§319, FSIS Appendix A/B), אילו מה-14 בכל זאת נושאים טמפרטורת קטלניות אמיתית, ומהי צורת-הנתונים המינימלית. **אסור להמציא מספר; פער נקוב עדיף על מספר סמכותי-למראה** | הערת בעלים 2.8.26 + `2026-08-02-catalog-ai-sweep.md` | **החלטה אחרי המחקר** | 🟠 במחקר |
| R-72 | 🟡 **‏33 גבינות מעושנות ללא ערך בטיחות — לבדוק, לא להניח** (פסיקת בעלים 2.8.26). האם קיימת דרישה שראוי שנציין (פסטור החלב, בקרת ליסטריה, תקרת טמפרטורה לעישון קר, מגבלות החזקה) או ש"טמפרטורת בטיחות פנימית" פשוט אינה מושג שחל כאן. מקורות: ‏FDA ולא USDA. **תשובה כנה ש"אין ערך מספרי" היא תוצאה תקפה** | פסיקת בעלים 2.8.26 | **החלטה אחרי המחקר** | 🟡 במחקר  ‏🟢 **8.8.26 — נחקר, והתשובה היא שאין ערך.** ‏**0 מתוך 33** מצריכות ערך טמפרטורה פנימית, ו-33 מתוך 33 אין להן — לא כפער אלא כעובדה. **"טמפרטורת בטיחות פנימית" אינה מושג שחל על גבינה:** מה שמסדיר בטיחות הוא הרכב — פסטור החלב או יישון 60 יום לחלב גולמי, בייצור — ולצדו pH ופעילות-מים, ולא טמפרטורת בישול. אומת מול טקסט ראשוני שכבר בריפו: ‏21 CFR §133.3/§133.113, ‏FDA Food Code 2022 §3-501.17(G), הנחיית הליסטריה 2017. ‏⚠️ **ממצא לוואי שדורש הכרעה בנפרד:** תקרת ≤28–30°C לעישון קר שכבר יושבת ב-`data.py`/`sources.py` **אינה מתחקה לשום מקור רגולטורי** — רק לבלוגים של BBQ, והיא עוסקת בהמסה ומרקם ולא בפתוגנים. ‏`docs/analysis/2026-08-08-R72-smoked-cheeses-safety.md` | ‏🟢 **נסגר 8.8.26 — הכרעת בעלים.** ‏`safe` נשאר ריק ל-33. **ותקרת העישון הקר אינה עניין בטיחות** (הבעלים: "כבר בדקנו"), ולכן ההסתייגות שהעליתי מתבטלת — **ואומתה מול הנתונים ולא רק מול ההכרעה:** הערך יושב ב-`smt` (טמפרטורת עישון — טכניקה) בעוד `tgt` (טמפרטורה פנימית) ריק, וההערה כבר מנוסחת "עישון קר חובה (≤30°C) **למניעת המסה**". הוא אינו מוצג כבטיחות בשום מקום. אין מה לתקן |
| R-73 | 🟠 **‏`central-code.mjs revoke` נכשל בשקט** — דיווח הצלחה בעוד קוד הגישה נשאר חי; נתפס בסריקת R-65, הסוכן מחק ידנית לפי מפתח ואימת. **כלי אבטחה שמשקר על הצלחה גרוע מכלי שנכשל ברעש** | `2026-08-02-catalog-ai-sweep.md` | **גל 0** | 🟠 פתוח |
| R-74 | 🔵 **⏸️ שחרור תיקון 48 טענות הבטיחות — מוקפא בהוראת בעלים (2.8.26), לא מבוטל.** הקוד ב-main, סוויטה **1,140 ×2 exit 0**. הנימוק: ייתכן שרפקטור מודל-הנתונים יחליף את הקוד שהוא נוגע בו. **אם ייזרק — ייזרק במודע, לא ייעלם בדיעבד** | `2026-08-02-catalog-ai-sweep.md` | **הופשר בהוראת בעלים 3.8.26 — שוחרר ב-v290** | 🟢 סגור |
| R-75 | 🔴 **רפקטור מודל הנתונים והמקורות — הקשת הבאה** (הוראת בעלים 2.8.26). **האבחנה של הבעלים:** נעשו יותר מדי שינויים על שינויים, טבלת המצבים אינה סופית, ולכן תמיד יימצא מקרה שאינו נתמך. **הראיה מאשרת אותו:** כל תיקון היום נבע מאותו שורש — צורת-שורה שטוחה אחת לפריטים שהבטיחות שלהם נשלטת במנגנונים שונים (‏`safe=0` כסנטינל · ‏`safe` ריק שמציין 3 דברים · ‏`cure_ppm` בלי נתון · ‏`tgt` שקיבל חותמת בטיחות · צורת-נתח בלי מקום לגור). **וגיליון המקור של הבעלים כבר עשה את זה נכון**: שני גיליונות עם עמודות שונות (נתחים · מוצרים מיוחדים עם `כבישה/ריפוי`, `ייבוש/בשלה`, `"אופ' Cure #1"`) — **האפליקציה שיטחה אותם והמידע שלא התאים נשר**. **סדר מחייב: מודל ומקורות תחילה, שערים אחריהם.** ‏R-66 (צורת הנתח) ו-R-69/R-71/R-72 נבלעים בקשת הזו | הוראת בעלים 2.8.26 + `BBQ_Recipes_RTL.xlsx` | **קשת חדשה — brainstorming לפני תכנון** | 🔴 בבירור |
| R-76 | 🟠 **⚠️ תזכורת בעלים: "אחרי זה אני רוצה שנדבר על המבנה, תזכיר לי"** (2.8.26). הדיון על **מבנה הנתונים** מתקיים **אחרי** שמפת המקורות והצמצום יחזרו ולפני כל תכנון. **חייב להיות brainstorming עם החלטת בעלים — אין קוד לפני עיצוב מאושר** | הוראת בעלים 2.8.26 | **מיד אחרי מפת המקורות** | 🔴 **הגיע זמנו — 3.8.26.** מפת המקורות והקורפוס נחתו (R-77); זו התזכורת. הדיון נושא איתו את **R-79** (94°C), **R-80** (טריגרים למסלול) ו-**R-81** (תזונה) |
| R-77 | 🟢 **קורפוס המקורות ירד — הושלם 3.8.26.** ‏19 מקורות, **113 קבצים** (94 ארטיפקטים + 19 `PROVENANCE.md`), 26MB. כיסוי: **12 🟢 טקסט-מקור מלא · 6 🟠 חלקי/מראה · 1 🔴 שחזור-בלבד** (היה 7/7/5 בתום סבב 1). כולל **פיצול העוף לפי מין** (Appendix A טבלאות 3/4, 91 שורות OCR דו-מעברי + אימות מונוטוניות של 782 תאים ע"י ה-controller), חמש טבלאות הפסטור של Baldwin, ‏5 נוסחאות הבייקון, ומגבלות ה-PAH. **‏eCFR XML הסכים עם ה-CSV שגורדו מ-PDF על כל ערך — אפס סתירות.** ‏`docs/sources/corpus/00-INDEX.md` · `00-DOWNLOAD-REPORT.md` | הוראת בעלים 2.8.26, שלב 2 | **נחת — הקורפוס בריפו** | 🟢 סגור |
| R-78 | 🔴 **‏#17 Modernist Cuisine — המקור היחיד שנשאר בלתי-מאוחזר.** יש בידינו **2 שורות בלבד** מטבלת ה-Best Bets, שתיהן משחזור משני (בריסקט 57°C/48h · צלעות קצרות 60°C/72h). זה **המקור היחיד בעולם עם מספר לכל נתח קשה**, והוא ספר מודפס. **מה שנדרש לסגירה: רכישת הספר או גישה למהדורה דיגיטלית** — אין עקיפה טכנית | `00-INDEX.md` §"מה אין כאן" | **החלטת בעלים — רכישה או ויתור מוצהר** | 🔴 פתוח |
| R-79 | 🟠 **‏94°C לבריסקט — להכרעת בעלים.** ‏**אין לו מקור ראשוני, אפילו לא בקורפוס בן 19 המקורות.** מנגד: ‏Modernist אומר 57°C/48h (שחזור משני, מתודולוגיה `UNDETERMINED`) · ‏Tornberg 2005 (peer-reviewed) מספק מנגנון קולגן 53–63°C **שאינו מגיע עד טענת הג'לטיניזציה ב-90°C+** שהכתיבה המסורתית נשענת עליה. **זו אינה מחלוקת בין שני מקורות — זה צד אחד עם מנגנון מול צד אחד בלי מקור.** אותה תבנית חלה על שקדי עגל. **לא הוכרע בשקט** | הערת בעלים 2.8.26 + `00-SOURCE-MAP.md` §5 (D-8/G-B) | **דיון המבנה** | 🟠 ממתין להכרעת בעלים |
| R-80 | 🟠 **‏`mid`/`somid`/`wrap`/`rest` — קיימים במלואם בקטלוג, חסרה להם צורה.** תיקון בעלים 3.8.26: *"טעיתי כשאמרתי שאבדו"*. **הדרישה: שיקבלו צורה של שלב במסלול עם טריגר** — *"עטיפה ב-70°C" היא פעולה שמופעלת בטמפרטורה, לא הערה.* **זו דרישה למנוע המסלולים**, ונכנסת לדיון המבנה כדרישה ולא כשאלה | הערת בעלים 3.8.26 | **דיון המבנה (‏R-76)** | 🟠 פתוח |
| R-81 | 🔵 **ערכים תזונתיים לפריטים — נרשם, לא מבוצע.** שאלת בעלים 3.8.26 הותנתה ב*"אם כבר קיים המידע אצלנו, אחרת לא"* — **ונבדק: אין.** ‏0 שדות תזונתיים ב-1,012 שורות `data.py`, ואף לא באחד מ-19 המקורות. **המקור קיים ונגיש:** ‏USDA FoodData Central (‏`api.nal.usda.gov/fdc` — נמדד 200; גם הורדת תפזורת ללא מפתח), מכסה בשר/דג/גבינה/ירק/מותסס. **הסט הנכון הוא `SR Legacy` + `Foundation` (~7,800 פריטי ייחוס), לא `Branded` (400k+ מוצרי מדף = רעש).** שתי אזהרות: ערך תזונתי משתנה עם הבישול, ומידע לא-בטיחותי חייב הפרדה ויזואלית מ-`safe`. **ההתאמה לפריטים היא העבודה, לא ההורדה** (`lamb shoulder raw` החזיר `Lamb, ground, raw`) | שאלת בעלים 3.8.26 | **אחרי דיון המבנה — הסכימה החדשה תקבע איפה בלוק תזונה יושב** | 🔵 רשום |
| R-82 | 🟢 **‏R-69 צורה F, נתיב שני — מנוע השאלות המקומי המציא 63°C. תוקן 3.8.26, ‏main, טרם שוחרר.** נמצא בזמן עיצוב הרפקטור, כששאלתי את **serena** מי צורך את `.safe` ב-`app.js` — ‏~20 אתרים, ושניים מהם נשאו `${c.safe\|\|63}`. ‏**27 שורות ירקות/פירות** (`safe=0` = "לא רלוונטי") קיבלו את המשפט *"טמפ׳ בטיחות 63°C"* — **בתג ⚡מקומי, כלומר כערך משלנו**, ולא כניחוש של מודל. **שוחזר ב-UI ב-390×844 לפני התיקון.** למה הסריקה פספסה: הענף מוגן רק אם השאלה מכילה "טמפ׳"; שאלה בטיחותית בלי מילת-טמפרטורה ("האם תירס בטוח") נופלת לענף הלא-מוגן. **התיקון: `citedSafeC()` — קריאה אחת של `safe` לכל האפליקציה**, ו-`vcIdentifiedSafeItem` (שתוקן ב-R-69) אוחד אליה כדי שהנתיבים לא יוכלו להתפצל שוב. ‏4 בדיקות חדשות ב-`catalog-sweep-safety.spec.ts`, ‏**RED נצפה על 27 פריטים**, סוויטה **1144 ×2 exit 0**. ‏**מדגים את R-75:** במודל שטוח כל צרכן גוזר בעצמו מה `safe` אומר — ולכן גוזר אחרת | `app.js` (‏citedSafeC · askFire · askContextFor) · serena | **שוחרר ב-v290, מאומת חי 3.8.26** (`2026-08-03-v290-live-verification.md`) | 🟢 סגור |
| R-83 | 🟢 **ארבע הכרעות בעלים על מודל הנתונים — 3.8.26.** **Q-0 ✅ ספק v2 אושר** (מסלולים כאזרחים מהמעלה הראשונה, על מזהי `itemPaths`; מחליף את v1 שנכתב לפני שהגיליון נמצא). **Q-1 ✅ כן, מותנה** — הבעלים: *"בעקרון כן, רק צריך לבדוק שלא מתנגש עם המנוע שהינו גנרי פיסיקאלי"*. **נבדק ואין התנגשות:** ‏`itemStages` כבר מחזיק טמפ' תהליך פר-method, והדוקטרינה בקוד היא *"נתון מצוטט כשקיים — לעולם לא נוסחה לטמפ'/זמן רלוונטיים-בטיחות"*. **התקדים שולט: 133 עקיפות `order_smokesv`/`order_svsmoke` כבר ב-`sources.py`** (0 ב-`data.py`), ולשם מיועד גם `path_outcomes`. ⇒ **היעדים פר-מסלול נכנסים ל-`sources.py` כ-`path_targets`**, אחות ל-`order_*`. **חיווט `donenessTarget` (9 אתרי קריאה) הוא שינוי מנוע ⇒ בגל 0 עם החלפת המנוע, ובאותה משימה כמו הייבוא** — `no-inert-shipment` אוסר לייבא נתון שאיש אינו קורא. **Q-2 ✅ לא לבנות שדה** — נמדד: 63/68 מ-`תערובות` זהים ל-`צ'אנקים`, 2 ניסוח, **3 בלבד מידע חדש** (אסאדו·צוואר בקר·כתף טלה) ⇒ היסטוריית-מוצא. **Q-3 ✅ `data.py` מנצח** ב-8 סחיפות טמפ' העישון, הגיליון כהיסטוריה; **עצמות מח עצם 120→150 מסומן לבדיקה נפרדת** (הכיוון ההפוך לשבע האחרות) | הכרעות בעלים 3.8.26 · `2026-08-03-sheet-vs-app-reconciliation.md` | **גל 0 — עם החלפת המנוע** | 🟢 הוכרע |
| R-84 | 🟠 **חילוץ המנוע מ-`app.js` — בתוך גל 0, לא לפניו.** ‏`app.js` הוא **14,651 שורות · 906 פונקציות ברמה העליונה**, ו-**60% מהן לא נכנסות לאף קטגוריה לפי שם** — אין מוסכמת שמות שמאפשרת לדעת למה פונקציה שייכת. ‏EQM (544 שורות) הוא **המודול היחיד שחולץ אי-פעם**. **ההכרעה (בעלים, 3.8.26): אל תחלץ מנוע שעומדים להחליף — תחליף אותו לתוך מודול.** גל 0 ממילא כותב מנוע חדש; אם ייכתב כמודול נפרד מלכתחילה, החילוץ מתקבל בחינם ובלי סיכון רגרסיה בקוד שנמחק ממילא | נמדד 3.8.26 | **גל 0 — עם החלפת המנוע** | 🟠 פתוח |
| R-85 | 🔵 **מודולריזציה של `app.js` — קשת נפרדת.** אחרי חילוץ המנוע (R-84) נשארים ~870 פונקציות: render/UI 98 · AI/ask 84 · voice/TTS 62 · equipment 52 · timers/plan 18 · units 10 · i18n 7 · other 540. **‏906 פונקציות אינן משימה, הן קשת.** **אילוץ שחייב להישמר: `build.py` מטמיע הכל לקובץ יחיד — זו דרישת מוצר** (`docs/process/single-file-preservation.md`), ולכן **מודולריזציה של המקור אינה מחייבת פיצול בריצה**; שני צירים נפרדים | נמדד 3.8.26 | **קשת נפרדת — אחרי גל 0** | 🔵 רשום |
| R-86 | 🟢 **פיצול תשלובת הריצה — `items` יוצא לטעינה על-פי-דרישה (Task B).** נחסמנו על **6.7KB** בלבד מתקרת A1 (2,593,292 מתוך 2,600,000): ‏MAKES-כפריטים נבנו והוכחו ב-1b אך נחסמו על 2,607,978, ו-1c/1d/1e יוסיפו עוד. **ההכרעה: להוציא ולא להעלות תקרה** — זו PWA בקובץ יחיד שנטענת בשטח על רשת גרועה, והמודל נועד לגדול (v3 מוסיף `cooking`/`outcomes`, ואחריהם תזונה). **תקדים: Dec-A1** — מילוני השפות כבר יצאו ל-`dist/lang-<code>.json` (~5.5MB) ונטענים על-פי-דרישה | הכרעת בעלים 3.8.26 | **נחת `df84324`** — index.html 2,593,292→2,457,469 · items.json 172,311 · מרווח A1 פי 21 (6,708→142,531) · MAKES משוחררים · 279 פריטים = הקטלוג המלא | 🟢 סגור |
| R-87 | 🟠 **אכיפת סדר שלבים בתוך אירוע — נופלת בין Phase 6 ל-11 ואינה שייכת לאף אחד מהם.** שאלת בעלים 3.8.26. **מה שקיים:** ‏Phase 6/E4 = אוצר סדרים (*מאפשר* סדרים) · Phase 10 = הסולבר (*מייצר* סדר) · Phase 11 = קופיילוט (*מגיב* לסטייה; הקלט שלו `advanceWhen` מ-CP2 הוא הקרוב ביותר). **מה שאין:** שער שאומר *"אתה מנסה לעטוף לפני שהגעת ל-70°C"* — כלומר **אכיפה, לא תזמון והמלצה**. **חצי מהתשתית כבר קיימת:** משימה 3r בנתה טריגרים (`at_core_temp`, `when_safe_met`, `at_stage`) — חסר הצד שאומר "לא עכשיו". **צביעת/הדגשת מסלול לעומת זאת כן משובצת** — ‏Dec-E3 ("שורת מסלול נבחר + הדגשה") ו-F2a (רצף מכשירים בתוכנית העבודה) ב-CP2/Phase 4 | שאלת בעלים 3.8.26 | **להכרעת בעלים — Phase 6 או Phase 11** | 🟠 פתוח |
| R-88 | 🟢 **הכרעת בעלים 3.8.26 — `SQLite` כמאגר הכתיבה (authoring store) של הקטלוג.** *"שיהיה ניהול מקומי מהיר לנתונים, שיהיו מאורגנים, שיכילו מזהה ייחודי — מבחינתי זה הפתרון."* **בלי שרת, בלי חשבון, בלי ספק** — קובץ אחד ברפו, ב-git, נפתח בכל כלי. **ואותו מנוע הוא D1**, שכבר נבחר לסנכרון (בקשת ה-Sync), כך שכתיבה וסנכרון חולקים טכנולוגיה אחת. **ההבחנה שפתחה את זה:** ‏"בסיס נתונים" הוא שלוש שאלות שונות — הגשה למשתמש (סגורה: PWA אופליין, שום DB לא מנצח קובץ שכבר במכשיר) · סנכרון נתוני משתמש (נבחר D1) · **כתיבה ואוצרות — לא נדונה מעולם, ושם הכאב.** **‏Supabase נדחה** (‏`DECISION-HE.md` §5: הסכימה המוצעת לא מפעילה RLS ⇒ כל האירועים והציוד של כל המשתמשים קריאים דרך מפתח ה-anon; ‏$25/חודש; השהיה אחרי 7 ימי חוסר-פעילות). **‏MongoDB נשקל ונדחה** — המודל אכן מסמכי והסכימה אכן משתנה מהר (שני טיעונים אמיתיים), אבל הריצה שלנו **אינה שואלת שאילתות** אלא טוענת הכל, ‏279 פריטים אינם קנה-מידה שדורש אותו, והוא מוסיף ספק ומשטח-הרשאות באותה מחלקת סיכון שפסלה את Supabase. **ארבעה ממצאי הסקר של 3.8 הופכים בלתי-אפשריים מבנית:** ‏`{every:{min:45}}` — 45 דקות שאיש לא כתב, על 17 שלבים (⟵ `NOT NULL source_id`) · **47 התנגשויות מזהים** בין `CUTS` ל-`SPECIALS` (⟵ `PRIMARY KEY`) · `a_w` שהודבק ל-5 מוצרים כי מילת "ייבוש" הגיעה **מתבנית קוד** ולא מהמתכון (⟵ המנגנון כשדה, לא טקסט חופשי) · `n-salchichon` כפול שמחק רשומה בשקט (⟵ `UNIQUE`). **⚠️ מה שסכימה לא תפתור, ונאמר מראש: 84 מתוך 103 הציטוטים התרמיים מפנים למסמך שהמספר אינו נמצא בו.** ‏`FOREIGN KEY` אוכף שהמקור **קיים**, לא שהוא **אומר זאת** — אימות תוכן יישאר עבודה נפרדת לתמיד | הכרעת בעלים 3.8.26 · `04a-architecture.md` · `DECISION-HE.md` §5 · סקר 3.8 סוקרים 1-2 | **אחרי סקר הבדיקות — נכנס לתוכנית הפעולה** | 🟢 הוכרע, טרם מתוכנן |
| R-93 | 🔴 **‏`tnode()` מחליף יחידות בסדר שרירותי — צירוף `ק״ג/דק׳` נשבר בכל 22 השפות הלא-עבריות.** מוצג בפועל **`5 kg/דק׳`** במקום `5 kg/min` — **עברית דולפת למסך צרפתי**, בדיוק המחלקה שהזיכרון "verify rendered, not the metric" נכתב עליה. **השורש, מאומת בבידוד:** ‏`interp()` רץ `for(var u in U)` — סדר הכנסה לאובייקט — והביטוי `(\d+)\s*<unit>` דורש **ספרה לפני היחידה**. עבור `5 ק״ג/דק׳`: ‏`דק׳` נכשל (לפניו `/`, לא ספרה) · `ק״ג` מצליח ⇒ `5 kg/דק׳` · והמפתח הארוך `ק״ג/דק׳` כבר לא מתאים למחרוזת שהשתנתה. **התיקון הוא כלל אחד — longest-match-first:** למיין את מפתחות `__units__` לפי אורך יורד לפני הלולאה. נמדד: `as-is → 5 kg/דק׳` · `longest-key-first → 5 kg/min`. **המפה תקינה בכל השפות** — הצירוף קיים ב-`__units__` של כולן; הבאג הוא בסדר ההפעלה בלבד. **⚠️ הבדיקה `i18n-toast-attr.spec.ts:67` אדומה, ולכן job ה-`playwright` ב-CI אדום ב-25 ריצות רצופות לפחות (מ-4.8.26)** — לא נכלל ב-`check-meta`, ולכן דווח "META GATE OK" לאורך כל התקופה. **‏R-94 נפתח על הפער הזה עצמו.** נמצא אגב עבודת התשתית 5.8.26; **הכרעת בעלים: לרשום ולהמשיך ברצף התשתית** | `app.js` ‏`tnode`/`interp` · `tests/i18n-toast-attr.spec.ts:67` · ריצת CI 30995288822 | **בתום שלב 8 של התשתית — לפני השחרור הבא. סוויטה אדומה חוסמת כל שחרור, ולכן זה ננעל לפני `release(v`** | 🔴 פתוח, מאובחן במלואו |
| R-94 | 🟠 **`check-meta` ירוק בזמן ש-CI אדום — השער אינו רואה את job ה-`playwright`.** ‏25 ריצות רצופות אדומות (4.8-5.8.26) שלא הופיעו באף דיווח, כי `check-meta` מריץ שערי משמעת ואת סוויטת Python — **לא את סוויטת ה-UI, ולא את מצב ה-CI**. התוצאה היא בדיוק הכשל ש-L40 נכתב עליו: פסק ירוק שאינו אומר מה **לא** כוסה. **⚠️ אזהרה מ-L54/§11a לפני שמתקנים: אסור להריץ את הסוויטה בתוך `pre-commit`** — היא ארוכה, ורצה במקביל לעצמה היא מקור ל-127 כשלי-רפאים מתועדים. **הכיוון הסביר: שער שקורא את מסקנת ה-CI האחרונה ל-`HEAD`, ומדווח "לא נבדק" כשאין ריצה** — ולא הרצה נוספת | ריצות CI 4.8-5.8.26 · L40 · §11a | **עם R-93 — אותה נעילה לפני השחרור הבא** | 🟢 סגור — `check-ci.mjs` קורא את מסקנת ה-CI ל-HEAD, נכשל על job אדום, ומדווח **"לא נבדק"** כשאין ריצה. קורא ולא מריץ: §11a אוסר ריצת-סוויטה מקבילה שנייה, ו-hook שהיה מפעיל אותה היה מייצר בדיוק את הכשלים שהוא נועד לתפוס |
| R-95 | 🟠 **כשל חולף יחיד ב-`timeline-enhancements.spec.ts:114` ("doneness scale is English"), שלא הצלחתי לשחזר ולא לאבחן.** נראה **פעם אחת** בריצה מקבילה מלאה אחרי תיקון R-93. עובר בבידוד · עובר בקובץ שלו · עובר בשתי ריצות מלאות רצופות (1,186 ✓ ×2) · **ועבר ב-CI לפני השינוי** (`✓ 806` בריצה 30995288822), כלומר אינו רגרסיה ידועה. **⚠️ איבדתי את האסמכתה** — ‏Playwright מנקה את `test-results/` בתחילת כל ריצה ואני הרצתי שוב לפני שלכדתי את `error-context.md`. **‏DoD-12: כשל חולף הוא באג, ולכן זה נרשם ולא נסגר כ"נפתר מעצמו".** ההשערה הסבירה: תלות-סדר סביב מצב השפה בין בדיקות מקבילות — הבדיקה מאמתת שסולם מידת-העשייה באנגלית, ובדיקה שכנה שמשנה שפה עלולה לדלוף. **המסלול: `flakiness-forensics` על הקובץ, ולכידת האסמכתה מיד בכשל הבא** | ריצת סוויטה 5.8.26 | **מיד כשייראה שוב — הפעם האסמכתה תישמר** | 🟠 **פתוח, ושתי השערות הופרכו במדידה.** ‏(1) *מרוץ עם מילון האנגלית* — **הופרכה:** פרובה הראתה `dictLoaded=true` ב-3 מ-3 בדיוק ברגע שהבדיקה הישנה המשיכה. ‏(2) *הנתיב הא-סינכרוני של `data-mt`* — **הופרכה:** ‏`mtNodes=0` ב-4 מ-4; אין בכלל צמתים כאלה במסך הזה. **לא המצאתי תיקון שלישי,** וההמתנה שהוספתי להשערה הראשונה **הוסרה** — תיקון שלא מתקן דבר הוא רעש, ולהשאיר אותו מתויג כתיקון היה הופך את המרשם לשקר. **הסיבה שלא אובחן: איבדתי את האסמכתה** — הרצתי שוב מקומית לפני שלכדתי את `error-context.md`, ו-Playwright מנקה את `test-results/` בתחילת כל ריצה. ‏**‏CI כן שומר `test-results/`, ומעכשיו גם מעלה את `test-intel.db`** — ההישנות הבאה תגיע עם ראיות מהמכונה שבאמת מרצדת. ‏60 מעברים רצופים אחרי תיקון R-96, כלומר גם לא רגרסיה חדשה |
| R-99 | 🔴 **שישה ערכי בטיחות שהציטוט שלהם אינו נוקב בהם — נחשף על-ידי קשירת ערך↔ציטוט (§5.3).** ‏`cuts:72` כבד בקר · `cuts:73` כבד טלה · `cuts:78` כליות בקר · `cuts:79` כליות טלה — **שולחים 72°C** בעוד הציטוט אומר ‏`USDA — variety/organ meats 160°F/71°C`. ‏`cuts:114` חסילון טייגר · `cuts:115` גמברי — **שולחים 63°C** בעוד הציטוט אומר ‏`FDA shellfish / Baldwin fish 55–60°C`. **הערכים לא שונו ולא ישונו על-ידי** — או שהמספר נכון והציטוט שגוי, או להפך, ושינוי מספר בטיחות בגלל שהציטוט חולק עליו הוא שינוי בטיחות בתחפושת של תיקון ייחוס (DoD-10). המסווג הישן **לא היה מסוגל להבחין בזה**, כי מעולם לא השווה בין השניים | ‏`model.py` §5.3 · ‏`docs/analysis/model-conversion-report.md` תחת `safe-value-not-stated-by-citation` | **להכרעת בעלים — לתקן את המספר או את הציטוט, פר-פריט** | 🔴 פתוח, ‏6 פריטים נקובים בשמם |
| R-100 | 🟠 **‏`tests/test_worker.py` מדלג-או-נופל לפי מרוץ check-then-act, ולא לפי מצב אמיתי.** ה-fixture ‏`_not_while_a_real_worker_runs` (‏`autouse=True`, שורה 55 — חל על **כל** הבדיקות בקובץ) בודק `pg_try_advisory_lock`, **משחרר מיד** (שורה 76), ואז גוף הבדיקה מנסה לקחת את אותו מנעול דרך `SingleWriter()`. תהליך חילוץ רקע שתופס את המנעול **בחלון שבין השחרור לנטילה** מפיל את הבדיקה במקום שהיא תדולג — וזה בדיוק מה שקרה: ‏`check-pytest` נפל בזמן שהחילוץ רץ, והרצה חוזרת עברה. **הבדיקה גם מחמירה את המרוץ שהיא אמורה למדוד** — היא עצמה נוטלת ומשחררת את המנעול הסינגלטוני. ‏**⚠️ הסוכן שאבחן זאת המציא עובדה** ("הבדיקה היחידה שחסר לה השומר") — אין רשימת opt-in, ה-fixture הוא `autouse`. המסקנה שלו הייתה נכונה, ההסבר שגוי; **‏DoD-12: כשל חולף הוא באג** | ‏`tests/test_worker.py:55-80` · ריצת `check-meta` במשימה 9 · אומת ידנית בקריאת הקובץ | **בשלב 2 של תוכנית מסד-החוקים, יחד עם השומר (משימה 21)** — הוא הרכיב שמנטר תהליכי רקע ולכן הבעלים הטבעי של "מי מחזיק את המנעול" | 🟠 פתוח, מנגנון מאובחן ומאומת |
| R-101 | 🟠 **לתור החילוץ אין צרכן — מסמך שנערך באמצע ריצה נשאר ממתין עד שתתחיל ריצה חדשה.** הזיהוי תקין: ‏`ingest.py` משווה hash-תוכן, כותב גרסה חדשה, וה-view ‏`revisions_pending_extraction` הוא תצוגה חיה ונכונה. **אבל `extract_graph.py` קורא אותה פעם אחת** (‏`fetch_chunks`, שורה 189) ובונה ‏`by_doc` מאותו snapshot; הלולאה בשורה 237 אינה שואלת שוב לעולם. לכן גם **עדיפות לא הייתה עוזרת** — הדירוג ‏`project→tests→sources→vendor` הוא `ORDER BY` **בתוך** אותו snapshot. **והמפעיל האוטומטי היחיד הוא `MSFT_TaskLogonTrigger`** ו-`NextRunTime` ריק, בעוד הבעלים עובד remote ואינו מאתחל. מדוד: ‏17 מסמכי פרויקט ממתינים כך ב-6.8.26, אחרי ש-295 חולצו במלואם | ‏`scripts/extract_graph.py:189,237` · ‏`Get-ScheduledTaskInfo MatkonetGenizaExtraction` · פילוח ה-view לפי שכבה | **הכרעת בעלים 6.8.26 — אחרי משימה 11 של תוכנית מסד-החוקים, כמשימה נפרדת.** הכיוון שהבעלים קבע: **בדיקה חוזרת אחרי כל מסמך** ולא רק בסוף המעבר, כדי שמסמך פרויקט שנערך יקפוץ לראש התור במעבר הבא במקום להמתין מאחורי 283 מסמכי vendor. **‏`select count(*)` על ה-view נמדד ‏0.7ms חם / 3.5ms קר, על מכונה שהחילוץ עמוס עליה** — מול ‏~2.5 דקות למסמך, זו תקורה של ‏0.0005%, ולכן אין סיבה לדגום בדילוגים. **הערה שלי לתכנון: לא לייצר טבלת-משימות שנייה** — ה-view כבר גזור ממקור-האמת ולכן אינו יכול להיפרד ממנו, וטבלה או דגל נפרדים היו מכניסים בדיוק את בעיית-הסנכרון שמשימות 8-9 נבנו כדי לפתור. ‏`LISTEN/NOTIFY` של Postgres הוא התוספת הא-סינכרונית הנכונה להשלמה, מעל בדיקת ה-count ולא במקומה | 🟠 פתוח, מעוגן בטריגר |
| R-102 | 🔴 **השחתת אינדקס ה-HNSW חזרה — פעם שנייה, על אותו אינדקס בדיוק, והפעם בלי אתחול.** ‏`data_chunk_vectors_embedding_idx` נשבר שוב ב-6.8.26 בערב וחסם את שער ההפקדה, בדיוק כמו במשימה 4. **‏L62 ייחס את הראשונה לאתחול הבלתי-מתוכנן ב-02:27 — ההסבר הזה כבר לא מספיק**, כי לא היה אתחול היום. הגורם המשותף שנותר: **ריצת חילוץ שכותבת embeddings ברציפות במשך שעות** תוך כדי שקריאות אחרות רצות מולה. זו השערה, לא ממצא — לא מדדתי אותה. **תוקן פעמיים באותה דרך:** `REINDEX INDEX CONCURRENTLY` עם `mk_admin`. **אומת אחרי התיקון:** ‏`data_chunk_vectors_embedding_idx` ו-`document_chunks_embedding_idx` שניהם `indisvalid=true`, **ואין אינדקסי `_ccnew` שרידיים** — כלומר בנייה מקבילה שנכשלה לא השאירה זנב. ‏⚠️ **המשמעות רחבה מהתסמין:** הגניזה היא כלי-הראיות של הפרויקט, ואינדקס וקטורי מושחת הופך `semantic_search` לשקט-ושגוי במקום לרועש-ונופל | ‏`pg_index.indisvalid` נמדד 6.8.26 20:30 · שתי התרחשויות: משימה 4 ומשימה 11 · ‏L62 | **עם השומר (משימה 21)** — ניטור תקינות אינדקס הוא בדיוק תפקידו, וזו כבר תקלה חוזרת ולא חד-פעמית. **לפני זה: למדוד את הקורלציה לחילוץ** במקום להמשיך לנחש אותה | 🔴 **פתוח — שלוש התרחשויות.** השלישית ב-7.8.26 בזמן משימה 21, אותו אינדקס, ושוב בלי אתחול. ‏**‏L62 ייחס את הראשונה לאתחול; ההסבר הזה מת סופית.** מה שמשותף לשלוש: עבודה כבדה ומקבילה מול המסד — חילוץ שכותב embeddings, סוויטת בדיקות, וסוכנים במקביל. **עדיין השערה שלא נמדדה.** התיקון זהה בכל פעם (`REINDEX INDEX CONCURRENTLY` כ-`mk_admin`), ובכל פעם הוא נמצא רק כי שער הפקדה נכשל — **כלומר אין ניטור, יש מזל.** ‏🟡 **7.8.26 — הופעה רביעית, ועכשיו יש ניטור.** הפעם החתימה הייתה שונה ומטרידה יותר: ‏`data_chunk_vectors_embedding_idx` החזיר **אפס שורות לשאילתה בלי `WHERE` בכלל** על טבלה בת 17,126 שורות, בעוד `indisvalid=true`, ‏`indisready=true`, ‏`count(*)` תקין, והמתכנן בחר בו ברצון. נמצא שוב **במקרה** — בדיקת pytest שבמקרה עברה במסלול הצפוף. **הרדיוס נמדד:** ‏`retrieval.semantic_search` קורא `document_chunks` ולא את ההיטל, ולכן חיפוש הסוכנים לא נפגע הפעם. **נסגר הפער:** ‏`scripts/check-hnsw-health.py` — גלאי דטרמיניסטי שאינו תלוי במודל embedding (שואל בוקטור של שורה קיימת; שכנה הקרובה של וקטור היא עצמו, במרחק 0), על **שני** האינדקסים, ‏`scripts/repair-hnsw-index.py` שמבצע ‏`REINDEX CONCURRENTLY` ומאמת אחריו, ורכיב `hnsw-index` בשומר (אזהרה, 0.25 שנ׳). ‏**מה שעדיין לא נתפס:** השחתה חלקית — אינדקס שמוצא זהות נכון ומאבד recall בשאר הקורפוס יעבור. ‏recall@10 אחרי בנייה נקייה נמדד 94%. ‏🔴 **8.8.26 — הופעה חמישית, תשע שעות אחרי הרביעית, והפעם הגלאי פספס אותה.** ‏`check-hnsw-health.py` דיווח ‏`RESULT=ok` בעוד שאילתה אמיתית החזירה **0 שורות** מול 5 בסריקה סדרתית — כי הגלאי בדק רק בווקטור של שורה **שמורה**, ואינדקס פגוע עדיין מוצא אותה. **בדיוק הפער שנרשם כאן כ"לא נתפס" — והוערך בחסר.** תוקן: הגלאי שואל עכשיו גם בווקטור **שאינו שורה שמורה** (embedding קיים שהוזז ברכיב אחד) ודורש שספירת התוצאות דרך האינדקס תשווה לסריקה סדרתית. ‏⚠️ **חמש התרחשויות מחייבות שאלה אחרת:** לפי §10.15, רכיב שחוזר ומכשיל דורש בחינת חלופה ‏(ivfflat, גרסת pgvector אחרת, או בנייה-מחדש מתוזמנת) ולא עוד תיקון. **החלטת בעלים נדרשת** |
| R-103 | 🟢 **מקור-האמת מעולם לא שמר את `bucket` — גבול תהליך↔תוכן, ההכרעה המרכזית של הבעלים — ו"הריפוי העצמי" דרס את העותק היחיד שכן ידע.** ‏`sync_rule` השמיט את `bucket`, `severity` ו-`mechanism` מרשימת העמודות ב-INSERT (‏`builder.py:164`), ולכן **‏132 מתוך 132 שורות היו NULL**. הכתיבה למראה כן העבירה את הערך, ולכן `rules.sqlite` המופקדת החזיקה `'process'` אמיתי — ו-`rebuild_mirror_from_postgres` קרא NULL בנאמנות ודרס אותו. **הסיבה שזה לא נתפס במשך עשר משימות:** ה-checksum של המראה כיסה רק `(rule_id, source_hash)`, ולכן שער-המראה **דיווח תקין על מראה שגויה**. נמצא רק כשה-digest הורחב ל-`statement`/`severity`/`bucket` בסבב תיקון 1 של משימה 13 — ותפס דריפט אמיתי תוך דקות. **ובדרך התגלתה סתירה במפרט המאושר:** ‏§4.3 קבע `CHECK (bucket IN ('A','B','C'))` בעוד §1 דורש `bucket = 'content'`. אלה שני צירים — ‏A/B/C הוא מנגנון האכיפה (§5-§7), ו-process/content הוא גבול התוכן (§1). ‏`_classify_bucket` מחזיר מחרוזות בלבד, ולכן **שום שורה אמיתית לא יכלה לעמוד באילוץ הישן** | ‏`builder.py:164` · מדידת NULL לפי עמודה · השוואת `HEAD:rules.sqlite` מול המתוקנת, עמודה-עמודה · ‏`0004_bucket_process_content.sql` | **הכרעת בעלים 6.8.26:** האילוץ מתוקן ל-process/content, בלוק הסכימה ב-§4.3 תוקן עם הסבר, **וציר A/B/C נדחה לשלב 5 עם השופט — הרכיב שגם יסווג.** לא נפתחת עמודה שאיש אינו כותב אליה, כי זו בדיוק המחלה שהשורה הזו מתעדת | 🟢 **סגור** — ‏`process` ב-131, ‏`content` ב-1 (‏DoD-10, בדיוק כדרישת §1), המראה תואמת ומופקדת מתוקנת |
| R-104 | 🟠 **כלל שנעלם מהמסמך ומעולם לא נפרש — אף שער אינו תופס אותו. חור בין שערים, לא בתוך אחד מהם.** ‏`check-rules-complete` מדווח `extra` (במסד ולא בדיסק) כ**אינפורמטיבי בלבד**, וזה נכון: פרישה אסור שתוסק ממצב-דיסק שעלול להיות חולף — בדיוק הכשל שבגללו השער נכתב מחדש לקריאה-בלבד. אבל ההערה בכותרת טוענת שזו "עבודתו של `check-rules-fresh`", **וזה לא נכון לגבי הקוד**: ‏`check-rules-fresh` מחשב `stale` (אי-התאמת hash בחיתוך) ו-`missing` (דיסק פחות מסד) ולעולם לא `stored − disk`. ‏`check-rules-mirror` נמצא על ציר אחר לגמרי (‏Postgres מול המראה). **התוצאה: כלל שהבעלים מחק מהמסמך ממשיך להיאכף** עד שמישהו יריץ `sync_document` ידנית — וזה בדיוק הכיוון המסוכן שה-CHECK `current_requires_mirror` נבנה למנוע | הסקירה הממוקדת של משימה 14, מאומת בקריאת שלושת השערים | **בשלב 6 (חיווט) או קודם** — הפתרון אינו להפוך את `extra` לחוסם, אלא שער-פרישה ייעודי שמבחין בין היעלמות יציבה לחולפת | 🟠 פתוח, מנגנון מאובחן |
| R-105 | 🟡 **שלושה שערים נושאים שלושה עותקים מועתקים-ביד של אותו regex לסיווג תקלות.** ‏`ABSENT` זהה בית-בבית ב-`check-rules-fresh.mjs`, ‏`check-rules-mirror.mjs` ו-`check-rules-complete.mjs` — **לא מודול משותף, לא import**. הם מסכימים היום ואין מה שיאכוף שיוסיפו להסכים. ‏**וזה כבר קרה פעם אחת:** משימה 14 נשלחה עם הגרסה הישנה של ה-regex, שסיווגה סיסמה שגויה כ-SKIP, אחרי ששני אחיה כבר תיקנו זאת באותו יום | ‏`grep "const ABSENT"` בשלושת השערים | **עם החיווט בשלב 6** — לחלץ למודול אחד ששלושתם מייבאים | 🟡 פתוח, נזק פוטנציאלי בלבד |
| R-109 | 🔵 **קשת מאושרת: יציאה מלאה מ-Docker.** הכרעת בעלים 7.8.26 — **"אני מתכוון להפסיק לעבוד עם Docker לחלוטין."** אחרי הוצאת ה-PostgreSQL המוחלף (R-108) נשאר **‏Neo4j בלבד** בקונטיינר, ‏17,412 צמתים. **מדוד: ‏24 קריאות `docker` ב-10 קבצים** — ‏`run-extraction.ps1` (5) · ‏`watchman.ps1` (2) · ‏`test_acceptance_infra.py` (7) · ‏`test_pg_schema.py` (2) · ‏`test_retrieval.py` (2) · ‏`check-geniza-fresh.mjs` · ‏`check-geniza-reader.py` · ‏`test_graph_schema.py` · ‏`test_acceptance.py` · ‏`test_worker.py`. **העובדה שמכריעה את הסיכון:** ‏`worker.py:548 project_revision()` בונה את Neo4j **בהיטל מ-Postgres**, ולכן הגרף אינו מחזיק דבר שמקור-האמת לא יכול לייצר מחדש; מה שחסר הוא רק מנוע שמריץ את ההיטל על הכול. **⚠️ ההנחה הזו הייתה שגויה ותוקנה 7.8.26 לפני שנבנה עליה משהו.** ‏Neo4j הוא היטל טהור רק ב-43% ממנו: ‏`Document` 857 + `DocumentRevision` 927 + `Section` 5,754 = ‏7,538 צמתים נבנים מ-`project_revision()`; **‏`Module` 9,877 צמתים ו-~14,000 קשרים** (‏`CALLS` 7,223 · `DEPENDS_ON` 4,704 · `PRODUCES` 1,186 · `DESCRIBES` 1,095) **מיוצרים על-ידי `write_proposed` ב-`extract_graph.py` — נתיב כתיבה שני ועצמאי — ואינם קיימים בשום מקום ב-Postgres** (שבע טבלאות, אף אחת מהן אינה שומרת עובדה מחולצת). בנייה-מחדש מ-Postgres הייתה מוחקת בשקט את רוב הגרף. **הכרעת בעלים מתוקנת: ‏`dump/load` עכשיו, ומנוע ההיטל נבנה בנפרד** — הסיבה שבחר בו בהתחלה נכונה ולא נפגעה (אין היום דרך לשחזר היטל מושחת), הוא פשוט מפסיק להיות תנאי ליציאה מ-Docker — כי המנוע הזה הוא **יכולת ההתאוששות שחסרה היום** (היטל מושחת = אין דרך חזרה), והבנייה שלו הופכת הגירה מסוכנת להרצת פונקציה שממילא הייתה צריכה להתקיים | סקר תלויות 7.8.26 · ‏`worker.py:548` · ‏CLAUDE.md §infra | **אחרי סגירת משימה 21** — קשת נפרדת עם מפרט ותוכנית משלה. **‏⚠️ שימו לב לשערים, לא רק לקוד:** ‏`A6` (חשיפה מעבר ל-loopback) ו-`A7` (תגי `latest`) הן **בדיקות בצורת Docker** — בלי Docker הן לא נשברות אלא **נעלמות**, וזה גרוע יותר: הכלל שהן שומרות תקף לחלוטין גם לשירותי Windows וצריך להישאל מול `Get-NetTCPConnection` במקום מול `docker inspect`. **ובנוסף שער שחוסם חזרה** — סריקה שנכשלת על קריאת `docker` חדשה, אחרת המצב יחזור בדיוק כפי שחזר עם `mk-postgres` | 🟢 **סגור 7.8.26 — ‏12/12.** ‏Neo4j הועבר לשירות Windows ב-`dump`/`load` ואומת בהשוואה מכנית מול קו-בסיס שנלקח על גרף **מוכח-שקט** (שתי הרצות בהפרש 5 שניות, זהות בבתים): ‏17,858 צמתים ו-25,359 קשרים, `diff` ריק. **‏Docker ריק לחלוטין** — אפס קונטיינרים, אפס נפחים, ‏`compose.yaml` נמחק. **‏A6 ו-A7 שואלות את מערכת ההפעלה**, ו-A6 מצאה ברגע הראשון חשיפה אמיתית: ‏`postgresql-x64-18` האזין על `0.0.0.0` (‏`listen_addresses='*'`) — תוקן, גובה, ואומת שהסוקט חזר ל-loopback ושהנתונים זהים (921/16,902 לפני ואחרי). **‏`check-no-docker` חוסם חזרה** ומבחין הפעלה מאזכור (‏34 שורות מכילות `docker`, ‏12 היו קריאות). השומר עלה לשבעה רכיבים. הסוויטה: **1,197 Playwright + 223 pytest, אפס כשלים**. ‏**‏R-110 נשאר פתוח ובמכוון** — עמידות העובדות המחולצות היא קשת נפרדת |
| R-111 | 🟠 **גיבוי שאיש אינו בודק את טריותו הוא הנחה — נמדד: הגיבוי היה בן יומיים ו-71 מסמכים מאחור.** ‏7.8.26 הגיבוי החדש ביותר של הגניזה החזיק **853 מסמכים** בעוד המסד החי החזיק **924 · 1,004 גרסאות · 17,083 chunks** — כלומר **כל 61 מסמכי Neo4j שנקלטו וחולצו באותו יום, ‏L65–L67, ושלושת המפרטים והתוכניות** לא היו בשום גיבוי. **התגלה מהערת בעלים** שכלי עם גיבוי ראוי לאמון רב יותר (‏PostgreSQL מול SQLite), ומהחלת אותו היגיון על הגניזה עצמה: **מסד החוקים מחזיק שלושה ייצוגים בלתי-תלויים** (‏`mk_rules` · המראה · המסמך) **ושלושה שערים שמשווים ביניהם זוגית — ולגניזה יש עותק אחד.** ‏**‏⚠️ ולזה מצטרף R-110:** ‏9,877 צמתי `Module` ו-16,456 קשרים ב-Neo4j אינם קיימים ב-Postgres כלל, כך שגם גיבוי מלא של Postgres לא היה מציל אותם | מדידה ישירה מול המסד החי מול `ls backups/` | **הפער הפעיל נסגר מיד 7.8.26:** ‏`pg_dump -Fc` טרי (‏199,393,736 בתים) + `pg_dumpall --globals-only`, **אומת בר-שחזור לפני שנרשם** — ‏`pg_restore --list` החזיר 114 רשומות ואת ארבע טבלאות הליבה כ-TABLE DATA. **הנותר כמשימה: שער `check-backup-fresh`** שנכשל כשהגיבוי ישן מדי או מפגר במספר גרסאות — ואם יש שער, יש גם סיבה לגבות אוטומטית. ‏§9: *מנגנון שמעולם לא נבדק הוא הנחה* — וזה חל על גיבוי בדיוק כמו על התאוששות | 🟠 פתוח — הגיבוי עודכן, השער טרם נבנה |
| R-112 | 🟡 **‏§10.5a אומר "1 בזמן סוויטה/GPU" — חצי הסוויטה מומש, חצי ה-GPU לא.** כלל תקרת-הסוכנים מוריד את התקרה ל-1 כשפורט הסוויטה תפוס (נמדד: שיגור שני נדחה), אבל **אין שום אות חי לעומס GPU** — חיפוש אחר פורט, נעילה או מוסכמת-תהליך לא מצא דבר. ‏**לא מומש, ולא מוסתר:** מוצהר בכותרת הכלל עצמו ובדו"ח. המשמעות המעשית: ריצת embeddings כבדה מול ollama לא תוריד את התקרה, והמגן היחיד הוא התקרה הרכה של 3 | נמדד 8.8.26 · משימה 5, שלב 3 | לזהות אות-חיים ל-GPU/ollama שאפשר לבדוק בזול — או להכריע שהתקרה הרכה מספיקה ולסגור את הסעיף במפורש | 🟡 **פתוח — הכרעת בעלים.** אני נוטה לבדיקת עומס על ollama עצמו (יש לו כבר רכיב שומר), אבל זו הרחבה ולא תיקון, ולכן היא לא נעשתה מעצמי |
| R-113 | 🟡 **‏§5.1 מזהיר על `renderWorkplan` ושותק על `function renderWorkplan` — האם זה נכון?** המיישם הרחיב מעצמו את התנאי לצורות-הגדרה, וההרחבה הוסרה: **‏§4 אוסר להרחיב מפרט מאושר בקוד.** אבל הרעיון עצמו סביר — חיפוש הגדרה הוא בדיוק מה שserena עושה טוב יותר מ-grep. ההצעה המדודה: שלושה דפוסים מעוגנים — `^def\s+NAME`, `^(async\s+)?function\s+NAME`, `^class\s+NAME`. **שתי שאלות פתוחות שהתגלו מהאסימטריה:** האם תחילית (`export`/`public`/`static`) נספרת גם היא, ובכל מקרה יש לגזום רווחים בקצוות — כרגע `renderWorkplan` מזהיר ו-`renderWorkplan ` שותק | נמדד 8.8.26 · משימה 6, שלב 3 | הכרעת בעלים: להרחיב את §5.1 בצורות-הגדרה, או להשאיר מזהה-חשוף בלבד | 🟡 **פתוח — שאלת מפרט לבעלים.** אין נזק במצב הנוכחי: פחות אזהרות, לא יותר |
| R-114 | 🟡 **כשל מזדמן בהוכחת-המקביליות של מחסן האכיפה — נצפה פעמיים, לא שוחזר.** ‏Section 2 של ‏`test-hooks-groupb.mjs` מריצה 8 תהליכי-כתיבה במקביל מול אותו SQLite; בריצה אחת ב-8.8.26 שתי בדיקות נפלו. **המיישם הריץ שוב עד ירוק — וזו בדיוק הפעולה ש-§3.12 אוסר**; בסבב השני הוא כבר לכד את הפלט לקובץ לפני ההרצה החוזרת. **לא שוחזר אצלי ב-8 ריצות, מהן 3 תחת שישה תהליכי-עומס מכוונים.** ‏⚠️ המשמעות אינה תיאורטית: אם פתיחה מקבילה של המחסן נכשלת באמת, כל hook נופל-פתוח ו**כל קבוצה B מושבתת בשקט** — הצורה השקטה ביותר שבה שכבת אכיפה מפסיקה להתקיים | נמדד 8.8.26 · משימה 4, שלב 4 · הפלט שנלכד בדו"ח task-4-report | להריץ את Section 2 בלולאה ארוכה תחת עומס משתנה עד שחזור, ואז לתקן — לא להריץ שוב עד ירוק | 🟡 **פתוח.** אינו חוסם: בכל 8 הריצות שלי ה-8/8 כתבו 320/320 אירועים |
| R-115 | 🟠 **בטיחות בגל 0 אינה טמפרטורה — היא תלוית סוג-מזון, וזו דרישת עיצוב שהתקבלה מהבעלים 8.8.26.** *"כשנממש את גל 0 שהוא המנוע החדש של המתכונים נצטרך להגדיר בטיחות במקרים שונים ובאופנים שונים בהתאם לסוג המזון, ולא רק טמפרטורה שנכונה בדרך כלל לנתחים, דגים וכדומה."* ‏**‏R-72 הוא ההוכחה שזה נכון ולא תיאורטי:** ל-33 גבינות **אין** ערך טמפרטורה פנימית ולא בגלל פער — כי מה שמסדיר את בטיחותן הוא הרכב (פסטור/יישון 60 יום, ‏pH, פעילות-מים) שנקבע בייצור. מודל שמניח "בטיחות = טמפרטורה פנימית" **אינו יכול לייצג אותן**, וידחוף למלא שדה שאסור למלא | הוראת בעלים 8.8.26 · ‏R-72 | **נכנס ל-R-76 (דיון המבנה) — לפני המשימה הראשונה של גל 0**, כי זו החלטת מודל-נתונים ולא פרט מימוש | 🟠 פתוח — ממתין לדיון המבנה |
| R-116 | 🔴 **שני כללי הידע שומרים על דלת שאיש אינו עובר בה — ולכן לא נורו אף פעם.** ‏`symbolic-grep-use-serena` ו-`geniza-fallback-declaration` בודקים `tool_name === "Grep"`, כלומר רק את **כלי ה-Grep הייעודי**. ‏`grep` שרץ בתוך פקודת Bash אינו נראה להם כלל. **נמדד על עצמי מאז ה-compact האחרון: 105 קריאות grep דרך Bash · 0 דרך כלי Grep · 0 קריאות serena · 9 שאילתות גניזה.** כלומר האכיפה דיווחה ירוק לאורך משמרת שלמה — לא בגלל ציות, אלא כי המדד היה עיוור. ‏⚠️ **זו בדיוק צורת הכשל ששכבת האכיפה נבנתה נגדה, והפעם היא בתוכה.** ‏**התיקון אינו מדיניות חדשה אלא הרחבת משטח-ההתאמה:** אותם שלושת תנאי §5.1 בדיוק, מוחלים גם על פקודת Bash שהמילה המובילה שלה היא `grep`/`rg`/`findstr`/`Select-String` — כך שחיפוש ממוקד בקובץ ידוע, ביטוי עם רווחים או חיפוש עברית ימשיכו לעבור בשקט | נמדד 8.8.26 על התמליל של הסשן עצמו | **משימה 13 בשלב 4** — נוספה לתוכנית | 🔴 פתוח |
| R-117 | 🔴 **מצב קבוצה B ממופתח ב-`session_id` בלבד — וסוכנים משוגרים חולקים אותו.** נצפה חי במשימה 10: הסוכן נחסם על-ידי `debugging-before-fix-edit` בגלל אירועי `bash_failure` תחת ה-`session_id` שלו **שהוא מעולם לא ייצר** — הם נוצרו בסוכן מקביל. כלומר כשל של שחקן אחד חוסם שחקן אחר, וזו בדיוק צורת ה"חסימה בלי סיבה" שכל השלב נבנה נגדה. ‏**הפתרון קיים ונמדד כבר:** ‏`agent_id` נוכח במטען ה-hook (אומת 8.8.26 במשימה 8), ולכן מוני-שחקן צריכים להיות ממופתחים ב-(`session_id`, `agent_id`) ולא ב-`session_id` לבדו. ‏⚠️ **הבחנה שחייבת להישמר:** ‏§10.16 (לקחים ב-commit) הוא מונה **ברמת ה-session** ונכון לו להישאר משותף; ‏§5 (מחזורי תיקון) הוא מונה **ברמת השחקן**. הפרדה גורפת תשבור אחד מהם | נצפה חי 8.8.26 · משימה 10, שלב 4 | **משימה 14 בשלב 4** | 🔴 פתוח |
| R-110 | 🔴 **‏17–23 שעות עבודת-מודל חיות במקום יחיד בלי גיבוי לוגי.** ‏`write_proposed` כותב את העובדות המחולצות **רק ל-Neo4j**; ‏Postgres שומר מונים וחותמות-זמן בלבד. כלומר ‏9,877 צמתי `Module` ועשרה סוגי קשרים — **רוב נפח הגרף** — קיימים בעותק אחד, ושחזורם דורש הרצת חילוץ מלאה. **התגלה 7.8.26 בזמן שבדקתי 'מי עוד כותב ל-Neo4j'**, שאלה ששאלתי רק כי היא הייתה תנאי לאישור תכנון אחר — כלומר הפער הזה היה יכול להישאר סמוי עוד זמן רב | מדידה ישירה מול הגרף החי · ‏`information_schema.tables` — שבע טבלאות, אף אחת אינה שומרת עובדות | **הכרעת בעלים 7.8.26: לרשום ולטפל בנפרד** — לא לערבב עם היציאה מ-Docker. שתי אפשרויות לכשיטופל: להתמיד את העובדות גם ב-Postgres מכאן והלאה (מתקן את השורש, אינו רטרואקטיבי), או גיבוי `neo4j-admin dump` מתוזמן (מגן על הקיים, אינו מתקן את הארכיטקטורה) | 🔴 פתוח, מתועד ומכומת |
| R-106 | 🟡 **‏`known_entities` הוא snapshot שני שאיש לא הבחין בו — אותה מחלקת התיישנות של R-101, במטמון אחר.** ‏`main()` שולף `known = extract.known_entities(args.namespace)` **פעם אחת לפני הניקוז** ולא מרענן. מסמך שנקלט באמצע הריצה — בדיוק המקרה ש-R-101 נבנה לתפוס — מכניס ישויות חדשות ש**מסמכים מאוחרים יותר באותה ריצה לא יראו** בבדיקות ההיטמעות. תיקנו snapshot אחד וגילינו שיש שניים | ‏`scripts/extract_graph.py` — הסקירה של R-101 | **כשהניקוז יוכח בריצה אמיתית** (‏19 גרסאות ממתינות כרגע) — קודם למדוד אם זה משפיע בפועל על איכות החילוץ, ורק אז לתקן | 🟡 פתוח, לא נמדד |
| R-107 | 🟡 **ל-`extract_graph.py` אין מנעול כלל, בניגוד ל-`worker.py`.** שתי ריצות מקבילות ישלפו את אותו מסמך ממתין ושתיהן יעבדו אותו: **בזבוז זמן מודל, לא השחתה** — הכתיבות אידמפוטנטיות (‏`MERGE` על `(source,type,target,revision)` ו-`ON CONFLICT` ב-`_mark_extracted`). **קיים מלפני הניקוז ולא הוחמר על-ידו** — אבל הניקוז מחזיק תהליך חי מול תצוגה חיה למשך שעות במקום שליפה אחת, וזה מגדיל את חלון החשיפה מהותית | הסקירה של R-101, מאומת מול `worker.py:SINGLETON_LOCK_KEY` | **עם R-100** — שניהם על אותו נושא בדיוק: מי מחזיק מה בזמן שהחילוץ רץ | 🟡 פתוח |
| R-108 | 🔴 **מסד נתונים מוחלף עדיין רץ לצד החי, וכל `docker compose up -d` מחייה אותו.** ‏PostgreSQL של הגניזה עבר ב-6.8.26 ל**שירות Windows מקומי** (`postgresql-x64-18`), ו-CLAUDE.md קובע ש**רק Neo4j** נשאר בקונטיינר. אבל `infra/compose.yaml` **עדיין מכריז על שירות `postgres`** עם `container_name: mk-postgres`, והקונטיינר **רץ ברגע זה** (`docker ps` מראה `mk-postgres 127.0.0.1:5432->5432/tcp`). ‏`scripts/run-extraction.ps1` מריץ `docker compose up -d` כדי להעלות את Neo4j — **ובכך מעלה גם את ה-Postgres המוחלף בכל ריצת חילוץ.** מדוד: השירות המקומי מחזיק **855 מסמכים** והוא מה שכל הקוד מדבר איתו; הקונטיינר הוא עותק נפרד. **⚠️ הסכנה אינה תיאורטית:** כל מסמך, סקריפט או סוכן שינקוב ב-`docker exec mk-postgres` מדבר עם **מסד אחר** ומקבל אמת מיושנת — וזה בדיוק מה שקרה כאן: רכיב השומר של משימה 19 נכתב לגשש את הקונטיינר ודיווח על הגניזה לפי עותק מוחלף | ‏`docker ps` · ‏`infra/compose.yaml:16-19` · השוואת ספירת מסמכים בין השניים · ‏`scripts/run-extraction.ps1` | **להכרעת בעלים — יש כאן volume עם נתונים** (`pgdata`), והכלל הוא לא למחוק ולא לדרוס. שלוש אפשרויות: להסיר את `postgres` מ-compose ולהשאיר את ה-volume · להשאיר ולתעד במפורש שהוא מוחלף · או לגבות ולהסיר. **לא נגעתי בקונטיינר ולא בעוצמה** | 🟢 **סגור 7.8.26 — גובה, אומת, הוסר.** ‏`pg_dump -Fc` טרי מהקונטיירר: ‏`backups/mk_knowledge-container-retired-2026-08-07.dump`, ‏174,172,529 בתים — **זהה בגודלו בדיוק** לגיבוי הסופי של ההגירה, כלומר הקונטיינר לא נכתב מאז. ‏`pg_dumpall --globals-only` לצדו. **אומת בר-שחזור לפני המחיקה:** ‏`pg_restore --list` החזיר 114 רשומות, אפס שגיאות, ושלוש טבלאות הליבה נוכחות כ-TABLE DATA. ואז: הקונטיינר נעצר והוסר, ‏`mk-pgdata` הוסר, ושירות `postgres` הוסר מ-`infra/compose.yaml` עם הערת-כותרת שמסבירה למה ואוסרת להחזיר. **אומת אחרי:** ‏`docker ps` מראה רק `mk-neo4j` (‏healthy, ‏17,412 צמתים), הגניזה החיה עונה ‏856/923/16,559, ו-`docker compose config` תקין. ‏Neo4j נשאר בקונטיינר בכוונה — אין לו נתיב בנייה-מחדש מוני |
| R-98 | 🔴 **שלושה מקו-הבסיס הוויזואלי של שלב C היו אותו קובץ בדיוק — ‏`md5 db136374…` ל-`home-he-empty-390`, ל-`home-he-populated-390` ול-`home-he-populated-1280`.** שני שורשים, שניהם תוקנו חלקית: ‏(1) **‏viewport השולחן מעולם לא הוחל** — ‏`page` הוא כינוי ל-fixture החם, עמוד אחד לכל worker מהקשר **worker-scoped**, ו-`test.use({viewport})` הוא **test-scoped** ולא יכול להגיע אליו. הצילום שנשא את הכיתוב "העדות הראשונה איך המוצר נראה על מסך רחב" היה **צילום טלפון**, והבדיקה הנגדית "שום דבר לא גולש ב-1280" הוכיחה שהטלפון אינו גולש. **תוקן** ל-`isolatedPage` (בנוי על ה-context המובנה, שקורא `use` ברמת הבדיקה) — הצילום כעת אמיתי ב-1280 ונבדל (`5373403…`). ‏(2) **האירוע המוזרע אינו מרונדר בבית** — ‏`mk-events` חסר `date` היה השערה ראשונה, תאריך עתידי שנייה, תאריך של היום שלישית; **גשוש הוכיח `evList()===1` ו-`store.get` מחזיר את האירוע, אך `bodyMentionsBBQ=false` בכל שלושת המצבים.** ‏**עצרתי בכלל שלושת-התיקונים במקום לנסות רביעי.** לכן ‏`home-he-populated-390` עדיין זהה ל-`home-he-empty-390` ו**אינו טוען דבר מעבר אליו** | ‏`md5sum` על שבעת הבסיסים, ‏5.8.26 ו-HEAD כאחד · גשוש שנמחק באותה משימה | **להכרעת בעלים — איזה מצב אפליקציה באמת מייצר "מצב בישול חי" בבית** | 🔴 **פתוח בחציו:** השולחן תוקן ומאומת; "הבית עם אירוע" נשאר כפילות מוצהרת ולא נסגר בשקט |
| R-97 | 🟠 **הסריקה הראשונה אי-פעם ל-`axe` מצאה ליקויי נגישות אמיתיים — ולא תיקנתי אותם בשקט.** ‏`color-contrast` בחומרה serious (‏8-34 צמתים, משתנה לפי מצב המסך) ו-`nested-interactive` (צומת אחד). **תיקון ניגודיות הוא שינוי עיצוב, ו-§10.9 דורש להציג לבעלים ולקבל אישור לפני בנייה** — לכן זה נרשם ולא נצבע מחדש. ‏`tests/a11y-smoke.spec.ts` חוסם **סוג הפרה חדש** ומדפיס את הספירה בכל ריצה, כך שהחוב לא יגדל בלי שיראו. **קו-בסיס לפי מספר נוסה ונכשל מיד** — אותו מסך דיווח 25 ו-34 צמתים בשתי ריצות, ושער שנותן כשלי-שווא הוא שער שמשתיקים. ‏`minor`/`moderate` מדווחים ואינם חוסמים | ‏`axe` 5.8.26, שלב C §4.4 | **להכרעת בעלים — עיצוב** | 🟠 פתוח, מתועד |
| R-96 | 🔴 **כשל חולף שני — `vg-voice-panel.spec.ts:132` ("first-run card guard — never stomps an open pane"), ‏`Expected: true · Received: false`.** נפל ב-**CI** (ריצה 31039326448) אחרי ש**עבר מקומית פעמיים ברצף** באותו עץ. **יחד עם R-95 זה כבר דפוס ולא צירוף מקרים** — שני כשלים חולפים בשני קבצים שונים בתוך יום. **⚠️ והכלי שבניתי היום לא יעזור כמו שהוא:** ‏`test-intel.db` רושם flake-rate, אבל הוא **מקומי ו-gitignored** — כלומר הכשלים קורים ב-CI והמדידה נשארת על המכונה שלי. ‏**שתי הריצות המקומיות רשומות, ריצת ה-CI שנפלה לא.** זה הופך את שאלת ה-flakiness לבלתי-ניתנת-למדידה בדיוק במקום שבו היא מתרחשת. **המסלול: (1) לגרום ל-CI להעלות את `test-intel.db` כ-artifact ולמזג · (2) `flakiness-forensics` על שני הקבצים · (3) רק אז לשפוט.** ‏CI איטי ועמוס יותר מהמכונה המקומית, וזה מסביר למה הוא חושף מה שהיא לא | ריצת CI 31039326448 · פרובה שהוכיחה את המנגנון | **סגור 5.8.26** | 🟢 **סגור — מאובחן ותוקן.** ‏`app.js:14696` מפעיל `setTimeout(maybeAskUiLevel, 400)` באתחול, והבדיקה קראה לאותה פונקציה **ידנית, בתחרות עם הטיימר**. אם עברו יותר מ-400ms בין ה-reload ל-`evaluate` — בדיוק מה שמכונת CI עמוסה עושה — הטיימר הקדים, הציג את הכרטיס, קבע את הדגל, והקריאה של הבדיקה הפכה ל-no-op ⇒ `cardAfterClose=false`. **הוכח בפרובה ולא הוסק:** המתנה של 700ms אחרי `seedApp` הראתה `alreadyAsked=true` והכרטיס כבר על המסך. **⚠️ התיקון הראשון שלי היה שגוי ומזיק** — אבחנתי "הבדיקה לא ממתינה למוכנות" והוספתי המתנה, שרק **נותנת לטיימר עוד זמן לנצח**. מצאתי את הסיבה האמיתית רק כי ניסיתי לשחזר את הכשל עם התיקון מוסר, **לא הצלחתי לשחזר, והמשכתי לחפש במקום לשלוח סיפור סביר**. התיקון: לזרוע את הדגל כך שהטיימר יהיה no-op, ולמחוק אותו רגע לפני הקריאה הידנית — הבדיקה מתנהגת זהה בין אם הגיעה ב-10ms או ב-10s. ‏**52 מעברים, כולל תחת ה-700ms שהפילו אותה** |
| R-88a | 🟢 **כיוון הזרימה — הכרעת בעלים 3.8.26: ה-DB הוא מקור-האמת, עריכה דרך כלים, לעולם לא ביד.** הגיליון הופך ל**ייצוא**. **שלוש השלכות שנרשמות מראש:** ‏(1) `data.py`/`sources.py`/`sausages_new.py` חדלים להיות מקור — נוצרים מה-DB או ש-`build.py` קורא ממנו ישירות · (2) **המיגרציה החד-פעמית היא הרגע המסוכן בקשת** — ‏3.8 נשרו `rub`/`wood`/`diff`/`saved` בשקט מוחלט (סוקר 3, ‏NH-5); מיגרציה שתאבד עמודה תעשה זאת פעם אחת ולתמיד · (3) "עריכה דרך כלים" היא תכולה לבנייה, לא הערת שוליים. **החלטת עיצוב נגזרת — ✅ אושרה 3.8.26:** ‏**להפקיד `schema.sql` + dump טקסטואלי ולייצר את ה-`.db` בבנייה** — קובץ בינארי ב-git הופך את `git diff` לעיוור, והדיף הוא כלי הבקרה המרכזי כאן (רוב ממצאי 3.8 נתפסו בקריאת שינוי בקומיט). ‏dump טקסטואלי נותן גם אכיפת-סכימה וגם ביקורתיות, **וגם הופך את "לעולם לא נערך ידנית" לבר-אכיפה** — הבינארי כלל אינו ברפו. **ותוצאה נוספת שנפתחת רק בזכות זה: שער על הדיף** — אפשר לחסום קומיט שמשנה ערך בטיחות בלי ציטוט מתאים, כי השינוי גלוי בטקסט. מול `.db` בינארי שער כזה בלתי-אפשרי | הכרעת בעלים 3.8.26 | **עם R-88, בתוכנית הפעולה** | 🟢 הוכרע |
| R-89 | 🟢 **שלושת מספרי האמת — שלב A של תוכנית הבדיקות, נחת 4.8.26.** ‏**‏A.1 `b8f3175`** — מינון הניטריט הגיע מ-`calc.cureRate\|\|2.5` בעוד הפריסטים `smoked` ו-`dry` **לא נשאו את המפתח כלל**; ‏`dry` הוא המוצר המיובש הלא-מבושל, ושתי שורות מתחת האפליקציה עצמה כתבה "‏2.5 תקני ובטוח" מעל שורה שחישבה 2.0. **שני המספרים היו נכונים** (‏9 CFR 424.21(c) נותן 156ppm=2.5 ג׳/ק״ג; ‏424.22(b)(3) מתיר 200ppm לבייקון יבש ו-2.0 הם 125) — **אף אחד מהם לא נאמר במקום שבו קוראים אותו.** · **‏A.2 `9691a4b`** — לשון בקר נשלחה ב-63° (הנתון של **שריר** בקר) עם ציטוט ל-Baldwin שאומר 54.4, בעוד FSIS **נוקב בלשון בשמה** ב-71.1. ‏`cat=בקר` ולכן אף שומר איברים לא ראה אותה. · **‏A.3 `7c82645`** — "הזמן משפיע על מרקם בלבד" הוסר; זמן-בטמפרטורה **הוא** מנגנון הפִסטור, והלשון עצמה מוכיחה זאת (‏70°C/24-48ש מתחת לרצפה, ובטוח). ‏10 בדיקות, כל אחת נצפתה אדומה, כל תיקון הוחזר ונתפס | תוכנית הבדיקות 4.8 §5.1/5.2/5.4 | **נחת ב-`main`, טרם שוחרר** | 🟢 בוצע |
| R-90 | 🟢 **‏graphify הוחלף בזיכרון-סוכן על LlamaIndex + SQLite/JSONB (הוראת בעלים, 4.8.26).** ‏**הסיבה נמדדה:** שער הטריות של הגרף **מעולם לא עבר** — ‏115 מסמכים מיושנים, ‏`graph-freshness.yml` נכשל ב-**8 מ-8** ריצותיו, ולכן סומן `ADVISORY` ונעלם (סוקרים 9 ו-10); ‏ו-`sync-docs.sh` שלב 1 **לא עדכן דבר** — הוא הדפיס הוראה להריץ סקיל ביד. **מה שהוחלף:** ‏`MarkdownNodeParser`/`SentenceSplitter`/`CodeSplitter` ב-`IngestionPipeline` → ‏`TextNode` → ‏SQLite עם `jsonb()`. ‏**‏567 קבצים · ‏7,325 צמתים:** ‏396 מסמכים · **‏19 המקורות הראשוניים** (טבלאות Baldwin, ‏FSIS lethality, ‏degree-hours, ערכי D/Z — **עד 4.8 אף אחד מהם לא היה בזיכרון**) · ‏`app.js`+17 py+171 spec דרך tree-sitter. **השער חוסם עכשיו**, כי התיקון עולה 0.32 שנ׳ | הוראת בעלים 4.8.26 | הושלם | 🟢 בוצע |
| R-91 | 🟢 **חיפוש היברידי תלת-דרכי + embeddings מקומיים על ה-GPU (4.8.26).** ‏`bge-m3` על RTX 3090, ‏**‏100% כיסוי**, ‏12 דק׳ לריצה מלאה — **בשכבה שנייה שלעולם אינה חוסמת**, כי embedding בתוך `memsync` היה משחזר את מות graphify במדויק. **ההיתוך נקבע במדידה:** ‏`"פסטור"` — מונח בודד — קיבל 0.377 על תוכן שגוי בעוד התאמה ליטרלית החזירה 35 נכונות; ‏`"מתי אסור לוותר על דרישה מהמפרט"` — שאלה — קיבל 0.628 ל-§4 בעוד `LIKE` החזיר **אפס**. ‏`BM25Retriever` **אושר ולא ניתן להתקנה** (‏PyStemmer בלי wheel ל-3.14) → הוחלף ב-**‏FTS5/BM25 של SQLite**, ועם טוקנייזר **trigram** כי ברירת המחדל `unicode61` מחזירה **0 תוצאות** ל-`"ניטריט"` בגלל ה' הידיעה. ‏`MetadataFilters` אומץ (‏14 אופרטורים; מפתחות מאומתים מול הזרקה) | מחקר LlamaIndex 4.8 | **‏`HierarchicalNodeParser` נמדד ולא אומץ** — פי 10.6 צמתים על 9 קבצים, המאגר 7,325→~17,300; **ממתין להכרעת בעלים** | 🟢 בוצע |
| R-92 | 🟢 **שלושה באגים בקוד שלי שהתגלו רק בסבב הזה, ואחד מהם עבר דרך שער ירוק.** ‏(1) **אצווה** — ‏LlamaIndex שולח את כל הרשימה בבקשה **אחת** ו-Ollama מחיל את ההקשר על כולה; תקציב **בתווים** נבדק ונפסל: ‏118,000 תווי ASCII עברו בעוד **96,000 תווי פרוזה עברית נכשלו**, כי התקרה בטוקנים ולעברית עלות גבוהה בהרבה. **אין מספר שנכון לשתי השפות** → פיצול-בתגובה-לכשל · (2) **חיתוך 2,000 תווים** שהמצאתי בלי לבדוק, **ואז השתמשתי בו כטיעון** נגד גושי קוד גדולים — מספר מומצא שהתפשט להחלטת תכנון · (3) **‏`parent_node_id` — שדה מת**: ‏`MarkdownNodeParser` קובע NEXT/PREVIOUS/SOURCE ולעולם לא PARENT; ‏0 מתוך 4,847 צמתים נשאו ערך (‏DoD §3.5). **וקומיט עם pytest אדום עבר** כי `check-meta` לא הסתכל על Python → ‏`check-pytest` נוסף, חוסם, מוכח בשני הכיוונים | סבב 4.8 | הכל תוקן; **הלקחים נרשמו — L48 (שער עיוור לשפה), L49 (מספר מומצא שהופך לראיה), L50 (סף שנמדד בשפה הנוחה)** + adopted win על שער ההיתכנות | 🟢 סגור |
| R-48 | 🔴 **חור בטיחות חי: מספר בטיחות מאוית במילים עוקף את השומר הקולי** — `vcGuardSpoken` פותח ב-`if(!nAiNums && digitRuns!==1) return src;` ושני הגלאים (`aiSafetyNums`, `safetyNumRe`) מזהים **ספרות בלבד**. **אומת אמפירית על ה-build המשוחרר:** "משוך ב-96 מעלות" נחסם ומסומן "אינו מאומת", אבל "משוך ב**תשעים ושש** מעלות" ו-"הטמפרטורה הבטוחה היא **שבעים וארבע** מעלות" יוצאים **ללא נגיעה** — נשמעים כאילו אומתו. **קורה בפועל:** המודל כתב מיוזמתו "תשעים וחמש עד מאה מעלות צלזיוס" במדידת ה-latency. **קשת ה-streaming לא יצרה את זה** — השער החדש `vcStreamSafe` חזק בדיוק כמו השומר. **המלצה: לחייב ספרות בהנחיית המערכת** (סעיף אחד, לא נוגע בגלאי, מתיישב עם פסק R-46) ולא להרחיב גלאי למספרים מאויתים בעברית — "שעתיים"/"שלושה שלבים" יפילו אותו על טקסט תמים. `docs/analysis/2026-08-01-spelled-out-safety-numbers.md` | אומת 1.8 בזמן Task 3 של קשת ה-streaming | **הוכרע 1.8: הבעלים אישר את ההמלצה — לחייב ספרות בהנחיה, בלי גלאי מאויתים** | 🔄 בביצוע |
| R-47 | 🔴 **שלוש רגרסיות ב-v280 מבדיקה חיה של הבעלים (1.8)** — קדימות עליונה, מוצר משוחרר: (א) **הקראת השעה נעלמה** — בעבר הוקראה שעת המשימה, ב-v280 לא; חשד: ‏`ttsText` (R-33) הסיר שכתוב שהיה מרכיב את הזמן, או שהשעה יושבת בענף רינדור שלא מגיע ל-TTS; (ב) **הפסקה ארוכה בין שתי שורות סמוכות** — חשד ישיר: הצינור המקוטע מסנתז מקטע אחד קדימה בלבד, ולמקטע קצר זמן הסינתזה (~2.4 שנ' מינימום, מדוד) **ארוך מזמן ההשמעה** שלו → פער נשמע. נדרש lookahead עמוק יותר או איחוד מקטעים קצרים; (ג) **אנגלית: שגיאה ואין קול** — חשד: איחוד השפה (R-31) או `vcTranslateToEn` שנותק מ-`vcSpeakContent` ב-Tasks 6-8 והושאר יתום; **(ד) jitter נשמע במהלך ההשמעה עצמה** (דווח 1.8, אחרי (א)-(ג)) — **כנראה אותו שורש של (ב)**: תפר בין מקטעים. gap כשהמקטע הבא לא מוכן בזמן, jitter כשהמקטעים נתפרים בתזמון אירועים (`onended`) במקום על שעון האודיו. התיקון הנכון לשניהם: תזמון Web Audio עם סמן רץ (`start(cursor)`, `cursor += buffer.duration`) + מקטעים ארוכים דיים (מתחת ל-~4 שנ' דיבור אי-אפשר להספיק לסנתז את היורש). | בדיקת בעלים חיה 1.8 על v280 | **תיקון מיידי — לפני v281** | 🟡 (א),(ב),(ד) **סגורות ושוחררו ב-v281** (חי, מאומת 1.8) · **(ג) אנגלית 🟢 נסגרה 1.8** — הבעלים אימת ב-v282 שהיא עובדת, כולל הקראת קטע ארוך. השורש כמעט ודאי **R-51** (מפריד ה-SSE): `no-audio` מסטרימינג שבור מייצר בדיוק את התסמין "שגיאה ואין קול". |

---

## 5b · רגיסטר משימות-הדיון (Brainstorm Registry, H8-ג)

| # | משימת-דיון | מקור | מצב |
|---|---|---|---|
| BR-1 | **Brainstorm: הזנת פרמטרי מתכון** (טקסט חופשי → פרמטרי מנוע דרך AI) — שאלת-הליבה: התנגשות הבטיחות עם כלל הציטוטים | Phase 9 (רשומה שם מ-30.7) | רשומה — הדיון בשער Phase 9 |
| BR-2 | **close-or-commit: מודל ה-plan-depth** — 47 הדרישות הלא-בטיחותיות (R-6). לפי H8 גם הצעה-בלימבו חייבת פסיקה | `audit-orchestrator` §H + `outstanding-register` §A.24 | ✅ **פסיקת בעלים 2.8.26 — commit ל-Phase 10** (זהה ל-R-6; שורה זו היא ה-close-or-commit של אותה הצעה). לימבו נסגר |
| BR-3 | **close-or-commit: מחליף 3 המצבים beginner/home/pro** (R-13) — "explicit close-or-commit decision rather than indefinite limbo" (לשון המסמך) | `audit-product` UXR-1 | ✅ **כבר בוצע — נסגר בסקר H13 (2.8.26)** (זהה ל-R-13). לימבו נסגר בעובדה, לא בפסיקה |

---

## 6 · ריכוז שערי הבעלים

| # | שער | Phase |
|---|---|---|
| 1 | פסיקת H7 ×1/×2 (נכתבת) + אשרור תזמון חילוצים · אשרור הנחיתות + Phase 9b + Phase 12b — **ניתן 30.7** (יחד עם H8/H9/H10) | Phase 0 |
| 2 | אישור תוכנית Cooking Path 2 המרועננת (§4) | Phase 4 |
| 3 | מפרט ניווט DRAFT→מאושר + עיצוב toast (§10.9) | Phase 5 |
| 4 | מוקאפ Equipment 4 (§10.9, אם חורג) | Phase 6 |
| 5 | אישור מפרט S1 | Sync Thread |
| 6 | go/no-go סולבר (תוקף ספק 17.7) | Phase 10 |
| 7 | אישור מפרט קופיילוט (HARD-GATE) | Phase 11 |
| תמידי | כל סטייה ממפרט מאושר → שיחה (§4) · טריגרי C1 בכל שער-Phase · שילוח = סוויטה ×2 + אימות URL חי (§10.10) · **בדיקת no-unlanded-items (H8) + עדכון STATUS-BOARD (H10) בכל שער-Phase ובסגירת קשת** | — |

---

## 7 · היקף כולל

**~175-200 משימות ≈ 16-18 יחידות-CP2** (13 פאזות + Phase 9b + Phase 12b + שני Threads), בקצב הנמדד של התוכנית — **בתוספת עד ~25 משימות-שחזור** (סעיף 5a; ההיקף בפועל ייקבע פסק-אחר-פסק בשער H13). כל נקודת סיום-Phase = גרסה חיה ניתנת-לשימוש; אין מצב-ביניים שבור. המסמך מתעדכן רק דרך שערי-Phase או פסיקת בעלים — כל עדכון מתועד בכותרת עם תאריך.

## מרשם האישורים — מפרטים שאושרו על-ידי הבעלים

שער §6.4 (טריגר §1) קורא **מכאן ורק מכאן**. הכרעת בעלים 8.8.26: הראיה ל"קיים מפרט מאושר" היא רישום
במרשם, לא שורת-סימון בגוף הקובץ — שורה בקובץ נכתבת בלי שאיש ראה אותה.

**הוכרע 8.8.26.** הבעלים אישר את 19 המפרטים המסומנים `מאושר` והשאיר את 6 האחרים **חסומים במכוון** —
עבודה יוצרת מולם תיחסם עד שיוכרעו אחד-אחד. זו החלטה, לא פער: הראיה בכל שורה נמדדה 8.8.26 ולא הוערכה.

⚠️ **שורה אחת תוקנה ידנית לפני האישור, והלקח שבה שווה יותר מהשורה:** ספירת הקומיטים של קובץ המפרט
אינה מדד לאישור. מפרט שנכתב פעם אחת ובוצע במלואו מקבל 1, ולכן `docker-exit-design` — שהבעלים אישר
בשיחה ושמולו בוצעו 12 משימות — סווג בטעות כלא-מאושר. ייתכן שאותו כשל-מדידה פוגע גם באחרים מהשישה.

| מפרט | סטטוס | הראיה |
|---|---|---|
| `2026-07-15-cookout-orchestrator-equipment-2.0-design.md` | **מאושר (2026-08-08)** | 7 קומיטים מפנים אליו — עבודה בוצעה מולו |
| `2026-07-15-i18n-foundation-phase0-design.md` | **מאושר (2026-08-08)** | תוכנית `2026-07-15-i18n-foundation-phase0.md` + 4 קומיטים |
| `2026-07-17-cookout-orchestrator-phase3a-design.md` | **מאושר (2026-08-08)** | 9 קומיטים מפנים אליו — עבודה בוצעה מולו |
| `2026-07-20-equipment-consumption-layer-design.md` | **מאושר (2026-08-08)** | 6 קומיטים מפנים אליו — עבודה בוצעה מולו |
| `2026-07-20-equipment-properties-completion-design.md` | **מאושר (2026-08-08)** | תוכנית `2026-07-20-equipment-properties-completion.md` + 8 קומיטים |
| `2026-07-21-occupancy-slots-h4-design.md` | 🔴 **לא-מאושר — הכרעת בעלים 8.8.26** | 4 קומיטים, אין תוכנית |
| `2026-07-21-occupancy-view-phase2-spec.md` | **מאושר (2026-08-08)** | תוכנית `2026-07-21-occupancy-view-phase2.md` + 7 קומיטים |
| `2026-07-21-scheduler-phase4-spec.md` | **מאושר (2026-08-08)** | 7 קומיטים מפנים אליו — עבודה בוצעה מולו |
| `2026-07-22-gap-closing-program-charter.md` | **מאושר (2026-08-08)** | 10 קומיטים מפנים אליו — עבודה בוצעה מולו |
| `2026-07-24-navigation-model-design.md` | 🔴 **לא-מאושר — הכרעת בעלים 8.8.26** | 2 קומיטים, אין תוכנית |
| `2026-07-24-p0-app-spoken-safety-design.md` | **מאושר (2026-08-08)** | תוכנית `2026-07-24-p0-app-spoken-safety.md` + 7 קומיטים |
| `2026-07-25-cooking-paths-single-source-design.md` | **מאושר (2026-08-08)** | 5 קומיטים מפנים אליו — עבודה בוצעה מולו |
| `2026-07-25-equipment-cooking-constraint-design.md` | 🔴 **לא-מאושר — הכרעת בעלים 8.8.26** | 2 קומיטים, אין תוכנית |
| `2026-07-26-full-localization-design-v2.md` | **מאושר (2026-08-08)** | 7 קומיטים מפנים אליו — עבודה בוצעה מולו |
| `2026-07-26-full-localization-design.md` | 🔴 **לא-מאושר — הכרעת בעלים 8.8.26** | 1 קומיטים, אין תוכנית |
| `2026-07-31-metered-streaming-design.md` | **מאושר (2026-08-08)** | תוכנית `2026-07-31-metered-streaming.md` + 4 קומיטים |
| `2026-07-31-units-architecture-design.md` | 🔴 **לא-מאושר — הכרעת בעלים 8.8.26** | 2 קומיטים, אין תוכנית |
| `2026-07-31-voice-wave0-design.md` | **מאושר (2026-08-08)** | תוכנית `2026-07-31-voice-wave0.md` + 4 קומיטים |
| `2026-08-01-tts-provider-layer-design.md` | **מאושר (2026-08-08)** | תוכנית `2026-08-01-tts-provider-layer.md` + 3 קומיטים |
| `2026-08-01-voice-governance-design.md` | **מאושר (2026-08-08)** | שורת אישור בגוף המפרט |
| `2026-08-03-data-model-design-v2.md` | **מאושר (2026-08-08)** | תוכנית `2026-08-03-data-model.md` + 2 קומיטים |
| `2026-08-03-data-model-design.md` | **מאושר (2026-08-08)** | שורת אישור בגוף המפרט |
| `2026-08-04-testing-overhaul-plan.md` | 🔴 **לא-מאושר — הכרעת בעלים 8.8.26** | 0 קומיטים, אין תוכנית |
| `2026-08-06-process-enforcement-design.md` | **מאושר (2026-08-08)** | שורת אישור בגוף המפרט |
| `2026-08-07-docker-exit-design.md` | **מאושר (2026-08-08)** | **תוקן ידנית — הספירה האוטומטית טעתה.** הבעלים אישר בשיחה ("תכתוב את המפרט", ואז "מאשר את התוכנית, תתחיל"), 12 משימות בוצעו מולו והקשת נסגרה. ספירת הקומיטים של **הקובץ** אינה מדד לאישור: מפרט שנכתב פעם אחת ובוצע במלואו מקבל 1 |
| `2026-08-08-classification-criterion-design.md` | **מאושר (2026-08-08)** | עוצב בשיחה 8.8.26 ואושר בעל-פה ("מאשר את שניהם" · "מאשר שתוסיף"), אחרי שמדגם עיוור נתן **47% אי-הסכמה** — מעל הסף של 30% שנקבע מראש. שתי הכרעות בעלים בגוף המפרט: **סף הסכמה 85%** בין שני מסווגים בלתי-תלויים, ו**`fable` במאמץ `high` לפחות** לשניהם. ‏⚠️ הממצא שקבע את צורת הקשת: שתיים מאי-ההסכמות היו **עם עצמי באותו יום** — כלומר השיטה לא-יציבה, לא רק הנתונים |
| `2026-08-08-rule-coverage-design.md` | **מאושר (2026-08-08)** | עוצב בשיחה 8.8.26 ואושר בעל-פה ("צא לדרך" · "תרשום בשמי"). שלוש הכרעות בעלים בגוף המפרט: אוצר-מילים סגור למנגנון, הצהרת `RULE_IDS` בקוד ולא נתיב במסד, ושער כיסוי שחוסם **רק על נסיגה**. סייג מחייב: הורדת כלל ל-`none` מגיעה לבעלים עם העלות מול החשיבות — אין הורדה מעצמי |

**להוספת מפרט חדש אחרי אישור:** שורה חדשה כאן בצורה `| \`<שם הקובץ>\` | מאושר (YYYY-MM-DD) | <משפט> |`.
