# v281 — דו"ח UX (H14)

**חי:** `https://matkonetesh.pages.dev` · חותמת `מהדורה 281 · 1.8.26` · אומת ב-1.8.26 (חותמת + נוסח "מה חדש" + בדיקת-תכולה `VC_TTS_OPEN_MIN` בחבילה עצמה).
**שער:** `npx playwright test` — **889 עברו, exit 0, פעמיים**, על העץ שנשלח בפועל.

---

## מה תרגיש, ואיך לבדוק ידנית

### 1. השעה נקראת שוב ✅
**לפני:** פתחת משימה, והקול התחיל מהתוכן — **בלי שעה**.
**עכשיו:** השעה נאמרת כחלק מהמשפט הראשון.

**איך לבדוק:** פתח משימה שיש לה שעה בתוכנית העבודה והפעל הקראה. **צריך לשמוע את השעה בתחילת המשפט**, לא רק את ההוראה.

**למה זה קרה:** איחוד המקטעים היה יכול לשלוח את `"14:30."` כבקשה עצמאית בת 6 תווים. מקטע כה קצר, בלי הקשר, פשוט לא נקרא כשעה. עכשיו פתיח קצר מדי מתמזג לתוך המשפט שאחריו.

### 2. הדיבור רציף — בלי הפסקות ובלי jitter ✅
**לפני:** שקט ארוך בין שתי שורות סמוכות, וקרעים נשמעים **בתוך** ההשמעה.
**עכשיו:** רצף אחיד.

**איך לבדוק:** שאל שאלה שהתשובה עליה **ארוכה** (‏3-4 משפטים לפחות) — שם התקלה הייתה בולטת. **הקשב לתפרים בין המשפטים:** צריך להישמע כמו דיבור אחד, לא כמו קטעים מודבקים.

**למה זה קרה:** שתי התקלות שדיווחת היו **תקלה אחת**. כל מקטע התחיל "עכשיו" והמתין לאירוע סיום של הדפדפן לפני שהבא בכלל התחיל להיווצר — כלומר כל תפר שילם סיבוב-שרת שלם (~2.4 שנ') ועוד אי-דיוק של לולאת האירועים, 10-15 פעם בתשובה. עכשיו המקטעים מתוזמנים על **שעון האודיו** ברצף מדויק, והסינתזה של הבא מתחילה בזמן שהנוכחי עוד מתנגן.

**מה לא השתנה:** **מספר הבקשות ל-Google לא גדל.** תיקון המכסה מאתמול נשמר במלואו — רק העיתוי של כל בקשה הוקדם. אין סיכון לחזרה לחסימות ה-429.

### 3. קטיעה באמצע — עדיין מיידית ✅
**איך לבדוק:** התחל הקראה ארוכה ו**קטע באמצע** (שאלה חדשה או עצירה). הקול צריך להיפסק **מיד**, ובלי שקטע שכבר "נכנס לתור" יתפרץ אחר כך.

זו סכנה שהתיקון עצמו יצר — קטע שתוזמן מראש ימשיך להתנגן אלא אם עוצרים אותו במפורש. יש לה בדיקה אוטומטית.

---

## מה **לא** תוקן — ואני צריך אותך

### אנגלית: שגיאה ואין קול 🔴 **פתוח**

**לא שוחזר.** מה שנשלל בוודאות: ‏`vcTranslateToEn` הוא **קוד מת** (אפס קוראים) — לא הוא. בכל הבדיקות הישירות אנגלית ועברית התנהגו **זהה**: אין שגיאה, אין מסלול נפרד.

**לא שלחתי תיקון מנחש למוצר משוחרר.**

**מה יסגור את זה בדקה:** כשזה קורה אצלך —
1. פתח **DevTools → Console** וצלם את הודעת השגיאה האדומה;
2. עבור ל-**Network**, מצא את הבקשה האדומה (סטטוס 4xx/5xx) וצלם את השורה שלה;
3. ציין **באיזה מסך** ומה בדיוק שאלת.

עם זה יש לי שורש. בלי זה זו ניחוש.

---

## שקוף לגמרי

- **השער תפס תקלה שלי לפני שהגיעה אליך:** הריצה הראשונה על העץ הסופי **נכשלה** — נוסח "מה חדש" שכתבתי לא עבר את טקס התרגום, ודלף כעברית גולמית ל-12 מסכים ברוסית. תורגם לשש השפות הפעילות והורץ מחדש. זה בדיוק הכשל שהפיל את v278, והפעם הגדר עצר אותו.
- **בדיקה אחת בסוויטה נכשלת לסירוגין** (`voice-wave0.spec.ts:487`, שורת השאלה). **תקלת בדיקה, לא תקלת מוצר** — הוכח פעמיים שהאלמנטים קיימים ב-DOM ברגע הרינדור. השורש **עדיין לא ידוע**, ותיאוריה קודמת שלי בנושא **הופרכה ותוקנה במסמך**. בארבע הריצות האחרונות היא עברה. מסומן פתוח.
