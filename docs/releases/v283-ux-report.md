# v283 — דו"ח UX (H14)

> **נכתב באיחור (1.8, ערב).** ‏H14 היא הוראת בעלים, ודילגתי עליה בשחרור עצמו. האודיט תפס את זה, והבודק
> `check-release.mjs` יסרב מעכשיו לשחרור בלי דו"ח. **הדו"ח הזה משלים חוב, ואינו מתיימר להיות בזמן.**

**חי:** ‏`https://matkonetesh.pages.dev` · חותמת `מהדורה 283 · 1.8.26` · אומת מול ה-URL החי (חותמת + נוסח
"מה חדש" + בדיקת-תכולה `ackBcheck`).
**שער:** ‏`npx playwright test` — **949 עברו, exit 0, פעמיים**, על העץ שנשלח.

---

## 1. שאלת בטיחות חוזרת עם מספר — לא ריקה 🔴→✅

**איך לבדוק:** שאל "אני מבשל אסאדו, מה הטמפרטורה הבטוחה?". התשובה מסתיימת ב:
> "...לפי המדריך, הטמפרטורה הבטוחה עבור אסאדו: **63°C**"

**מה קרה לפני:** המודל ענה **נכון** — `63°C` — וזה **בדיוק הערך המצוטט שלנו** ל-`cut-2` ב-`data.py`.
השומר מחק אותו כי הופיעו לצידו עוד שני מספרים. **מחקנו נתון מאומת מהמקור שלנו ונתנו תשובה ריקה על השאלה
המרכזית של המוצר.**

**מה שלא השתנה — ובכוונה:** כלל הכשירות של השומר. הוא נולד אחרי ש**שלושה תיקונים תלויי-תחביר נכשלו**,
כל אחד בניסוח אחר של טווח. ההחלפה **מוסיפה מקור מאומת במקום שקט; היא אינה מרחיבה את מה שמותר לומר.**

**וגבול שנקבע בכוח הראיות:** ההחלפה פועלת רק כששאלת **בשם הפריט**, לא כשאתה סתם מבשל אותו. הגרסה הרחבה
שברה 10 בדיקות רגרסיה קיימות — *היותך באמצע בישול אינה עדות לכך שהמספר שנמחק עוסק בו.*

## 2. כל שפה נשמעת בשפה שלה 🌍

**איך לבדוק:** עבור לרוסית, פתח בישול קולי ולחץ **"מתי הבא?"**. המשפט כולו ברוסית.

**לפני:** שכבת הקול בנתה מחרוזות בתנאי **בינארי** — אנגלית או עברית. ל-ru/fr/de/es/it זה נפל **לעברית**,
ומכיוון שתווית המשימה **כן** תורגמה — נשמע משפט עברי עם מילים רוסיות בתוכו.

**‏7 אתרים נסרקו ותוקנו, ובדרך נמצא באג שני:** הלוקאל שנשלח למנוע הקול היה **מקודד קשיח** — כלומר גם טקסט
נכון היה נהגה במבטא הלא נכון.

## 3. התראת בדיקת הטמפרטורה לא חוזרת ⏱️

**איך לבדוק:** אשר "✓ נבדק", צא מהאירוע והיכנס שוב. **הכרטיס לא חוזר.** והוא נוקב עכשיו בשם האירוע —
"חזה בקר · **ארוחת שבת** · יעד 74°".

**השורש:** האישור **מחק** את הרשומה, ולכן "מעולם לא הוצג" ו"הוצג ואושר" נראו **זהים לקוד**.

**והעדינות שקבעה את התיקון:** לזכור "אושר" לתמיד היה **משתיק את שער הבטיחות לכל בישול עתידי של אותו
פריט**. האישור נקשר ל**מופע** (חותמת הזמן של השלב), ויש בדיקה שמוכיחה שבישול חדש **כן** מתריע.
**בלי אותה בדיקה, התיקון היה גרוע מהבאג.**

## 4. מה לא נכלל

- **‏R-60** — "הדלקת מעשנת (חימום מוקדם)" שגיאת הקראה: **לא שוחזר**, ולכן לא נשלח תיקון.
- **‏R-61/R-62** — שיבוץ המספר בגוף המשפט וההפרדה בין מספרי בטיחות למידע רגיל: **בתכנון**, לא בגרסה זו.
