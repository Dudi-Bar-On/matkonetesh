# v284 — דו"ח UX (H14)

> **נכתב באיחור (1.8, ערב)** — יחד עם דו"ח v283. ראו שם את ההסבר: ‏H14 היא הוראת בעלים שדילגתי עליה,
> והבודק `check-release.mjs` יסרב מעכשיו לשחרור בלי דו"ח.

**חי:** ‏`https://matkonetesh.pages.dev` · חותמת `מהדורה 284 · 1.8.26` · אומת מול ה-URL החי (חותמת +
נוסח "מה חדש" + בדיקת-תכולה `askAllItems`).
**שער:** ‏`npx playwright test` — **957 עברו, exit 0, פעמיים**, על העץ שנשלח.

---

## 1. שאלה על **קטגוריה** שלמה מקבלת תשובה 🍗

**איך לבדוק:** שאל "טמפרטורת בטיחות עוף" — בלי לנקוב בנתח מסוים. התשובה תכלול **74°C** מהמדריך.

**לפני:** התשובה חזרה עם שלושה `[…]` ובלי מספר. הסיבה: **"עוף" אינה פריט בקטלוג אלא קטגוריה** — יש
"חזה עוף", אין "עוף" סתם — ולכן ההחלפה מ-v283, שדורשת פריט נקוב, לא מצאה למה להתייחס.

### ✋ ואיפה זה **שותק** — וזה החלק החשוב

**שאל "מה הטמפרטורה הבטוחה לבקר" ולא תקבל מספר. זו התנהגות נכונה.**

| קטגוריה | פריטים | `safe` | |
|---|---|---|---|
| עוף · הודו · אווז · ברווז | 17 | 74 | ✅ אחיד — משיב |
| חזיר · טלה · דג · פירות ים | 43 | 63 | ✅ אחיד — משיב |
| **בקר** | 30 | **63 ו-71** | ❌ **שותק** |
| **איברים פנימיים** | 12 | **63/65/72/74** | ❌ **שותק** |

בבקר, ההבדל בין 63 ל-71 הוא **נתח שלם מול בשר טחון**. תשובה גורפת שם אינה "פחות מדויקת" — היא **מסוכנת**.
**האחידות מחושבת מהקטלוג בזמן ריצה**, לא מרשימה קבועה, כדי שלא תתיישן כשהנתונים ישתנו. ופריט ספציפי
("חזה עוף") תמיד גובר על קטגוריה.

## 2. תשובה קודמת נמחקת בהחלפת שפה 🌍

**איך לבדוק:** שאל שאלה בעברית, ואז עבור לרוסית. **התשובה הקודמת נעלמת.**

**לפי הפסק שלך: למחוק, לא לתרגם.** תמלול הוא רשומה של שיחה שהתקיימה בשפה מסוימת; תרגום בדיעבד מייצר
טקסט שאיש לא אמר.

## 3. מה **לא** נשלח — ולמה

**‏R-60, שגיאת ההקראה במעשנה: לא שוחזר בשלוש שכבות**, ולכן **לא נשלח תיקון.**

| נבדק | תוצאה |
|---|---|
| צינור הלקוח, כל וריאנטי `PREHEAT` | אפס חריגות, אפס מקטעים ריקים |
| ‏API חוסם, התווית המדויקת | ‏200 · 151KB אודיו · `finishReason:STOP` |
| ‏API בסטרימינג | ‏78 מקטעי אודיו · צליל ראשון 1,267ms |

**תיקון שנשלח נגד תסמין שלא שוחזר הוא ניחוש עם הודעת commit.** ההסבר הסביר הוא כשל רגעי שהוצג דרך הודעת
השגיאה הגנרית שלנו — **וזה מצביע על התיקון האמיתי:** סיבת השגיאה נרשמת ל-console **ולעולם לא מוצגת**,
ולכן אין דרך לדעת מה נכשל. **הפעולה הנכונה היא לחשוף אותה, לא לנחש.**
