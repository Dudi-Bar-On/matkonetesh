# דו"ח בוקר — 3.8.2026

> **מצב המסמך:** נכתב במהלך הלילה ומתעדכן עד הסוף. אם שדה מסומן 🔄 — העבודה עדיין רצה בזמן הכתיבה
> והשורה תעודכן לפני הסגירה. **שום שורה כאן אינה הערכה; מה שלא נמדד — כתוב שלא נמדד.**

---

## 1 · מה שוחרר — v290, חי ומאומת

| | |
|---|---|
| **מדבקה** | `מהדורה 290 · 3.8.26` — נקראה מה-URL החי אחרי 5 סבבי סקירה (‏Cloudflare בונה מהמקור) |
| **‏R-74** | תיקון **48 טענות הבטיחות** — הוקפא בהוראתך 2.8, **הופשר בהוראתך 3.8**, שוחרר |
| **‏R-82** | מנוע השאלות המקומי המציא **63°C ל-27 שורות ירקות/פירות** — תוקן |
| **בדיקת פיצ'ר חיה** | תירס → "אין לו טמפ׳ בטיחות" · בריסקט → "63°C" (בדיקת-נגד) · סריקה מלאה: `stillBroken: []` |
| **סוויטה** | ‏1144 ×2, `exit 0` בשתיהן, על העץ שנשלח |
| **חזותי** | `mockups/v290-live-corn-390x844.png` |

### איך R-82 נמצא — ושווה לשים לב לזה

לא בסריקה ולא בבדיקה. **ביקשתי מ-serena את כל צרכני `.safe` ב-`app.js`** לפני שתכננתי את הרפקטור.
‏~20 אתרים; שניים נשאו `${c.safe||63}`. ‏**R-69 תיקן את הבאג הזה ב-2.8 — בנתיב אחד מתוך שלושה.**
זה בדיוק הטיעון של R-75: במודל שטוח כל צרכן גוזר לעצמו, ולכן בסוף גוזר אחרת.

**הכשל שלי בדרך, מדווח:** אחת מארבע הבדיקות שכתבתי **עברה בריצה הראשונה והייתה חסרת ערך** — הרגקס ישב
בתוך template literal של TypeScript, שם `\s` מתקפל ל-`s`, ומה שהגיע לדפדפן היה `/…s*d/`. נתפס **רק** כי
החוזה פוסל בדיקה שעוברת מיד. נרשם כ-L47.

---

## 2 · הקורפוס — 19 מקורות ברפו

```
113 קבצים · 26MB · 19 PROVENANCE.md · כיסוי 12 🟢 / 6 🟠 / 1 🔴
```

**הפער הגדול נסגר:** ‏91 שורות הקטלניות של FSIS Appendix A, כולל **פיצול העוף לפי מין** — הערך היחיד
שרק המקור הזה מחזיק בעולם. חולץ ב-OCR דו-מעברי, ואומת על ידי בשלוש דרכים: מונוטוניות על **782 תאים**
(0 הפרות טמפרטורה), קריאת עמוד המקור בעיניים, והשוואה לעוגנים שחולצו כטקסט.

**אישוש כפול שלא ציפינו לו:** ‏eCFR XML מובנה **הסכים עם ה-CSV שגורדו מ-PDF על כל ערך** — שני נתיבי
אחזור עצמאיים, אפס סתירות.

**‏#17 Modernist נשאר 🔴** — ויתור מוצהר בהוראתך.

### הלקח שעלה הכי ביוקר — L46

שלב 1 מדד את הרשת עם `curl`, קיבל `000`, וכתב "אין רשת". **שלושה סוכני הורדה ירשו את זה כעובדה
מאומתת** ובנו עליה את כל האסטרטגיה. שורה אחת הפריכה: `node -e "fetch(...)"` → 200. ‏`curl` ב-sandbox;
‏`node` לא. ‏**‏fda.gov ו-ecfr.gov היו נגישים כל הזמן — ול-eCFR יש API רשמי שמחזיר XML מובנה** במקום
גירוד PDF. סבב 2 סגר בכ-40 דקות ארבעה מקורות שנחשבו אבודים.

> **הכלל:** טענת יכולת על הסביבה דורשת **שני כלים בלתי-תלויים**. הנחה שיורשת ל-fan-out היא הדבר בעל
> המינוף הגבוה ביותר להפריך — מסקנה שגויה עולה סוכן אחד, **הנחה שגויה עולה את כולם בבת אחת**.

---

## 3 · ביצועי הסוויטה — נמדד, וההמלצה הראשונה היא דחייה

**בסיס:** ‏191.8 שנ' · 1144 בדיקות · 20 workers · ‏`exit 0`.

| ממצא | מספר |
|---|---|
| רמת-רוויה מלאה (כל 20 עסוקים) | ‏~140 שנ' — **73% מהריצה** |
| זנב סדרתי של פרויקט `service-worker` | ‏~27 שנ' (14%) — 7 בדיקות, worker אחד, 19 בטלים |
| התנעה קרה | ‏~20 שנ' (10%) |

### ❌ העלאת workers ל-24 — נבדקה ונדחית

```
20 workers → 191.8 שנ' · 1144 עברו · exit 0
24 workers → 345.97 שנ' · 39 כשלים · חתימת nav-stall מלפני תיקון ה-loopback
```

**‏1.8× איטית יותר.** לא תיאוריה — נמדד. **התקרה של 20 אמיתית ואין לגעת בה.**

### ✅ ההמלצה: להעביר את הריצה השנייה ל-CI — **החלטה שלך**

`.github/workflows/test.yml` **כבר מריץ את אותו `npx playwright test` בכל push**, 9/10 ירוקים לאחרונה.
‏H7 דורש **שתי ריצות נקיות** — לא **היכן**. זה חותך את העלות המקומית **מ-6.4 דק' ל-3.2 דק'**, וה-CI
מספק את השנייה ברקע. ‏(‏CI לוקח שם 11.4 דק' על 2 workers — אבל זה לא זמן שלך.)

**שתי הזדמנויות שנרשמו ולא נוגעו:** הזנב של `service-worker` (הבידוד שלו **מכוון** ונושא-משקל — דורש
חקירה משלו) · ביקורת `isolatedPage` (‏16 קבצים, 83 בדיקות, **12.6% מזמן הבדיקות**, ‏~1.7× ליחידה).

---

## 4 · רפקטור מודל הנתונים — R-75

**ההכרעות שלך מהלילה, כפי שנרשמו בספק:** ‏94°C נשאר עם `provenance:'craft'` · ‏Modernist — ויתור מוצהר ·
**הכל בבת אחת** · ואחרי זה: **"תבנה הכל, תמיר הכל, תבדוק הכל תדווח — לא פחות מזה"** — מה שסגר את
גבול-ההיקף שהעליתי: **כל שישה המנגנונים, לא רק התרמי.**

### הקו שאני לא חוצה, ולמה הוא חשוב

‏DoD-10 אוסר לשנות ערך בטיחות. אז:

| מותר | אסור |
|---|---|
| **לחלץ** מה שהנתונים אומרים (`calc.salt = 18` → `salt_g_per_kg: 18`) | **לחשב** טענת בטיחות שהנתונים אינם אומרים |
| **לצרף** מגבלה רגולטורית מהקורפוס **כמגבלה** | להציג אותה כאילו היא הערך של הפריט |
| **לבדוק** את הערך שלנו מול המגבלה ולדווח חריגה | להצמיד ערך בשקט כדי שיעבור |
| **לדווח** מנגנון שרואים אך לא יודעים לכמת | לנחש את המספר החסר |

**זה הופך את הקורפוס מארון תיוק ל-validator.** כל מינון ריפוי נבדק מול תקרת 200 ppm של 9 CFR ורצפת
100 ppm של CFIA; כל ייבוש מול a_w 0.85 של FSIS; כל תסיסה מול degree-hours של AMI 1997.

### ✅ משימה 1 הושלמה — `9a31a1d`

```
177 פריטים (130 נתחים + 47 מיוחדים) · 177 מזהים ייחודיים
103 בלוקים תרמיים · 99 מהם נפתרים למקור אמיתי בקורפוס
מקורות בפועל:  Baldwin 63 · AmazingRibs 21 · FDA דגים 6 · טבלת FSIS 5 · AskUSDA 4 · ללא 4
DoD-10:  safety values altered: 0
```

**הסוכן תפס שני באגים בתוכנית שלי, ואימתתי את שניהם בעצמי:**

1. **‏`corpus_id` אינו קיים ב-`sources.py`** — אפס מופעים ברפו. התוכנית שלי הייתה מגדירה אותו כברירת מחדל
   ל-Food Code, כלומר **מצמידה מקור שגוי ל-99 בלוקים**. הוחלף במסווג שקורא את טקסט ה-`ref` האמיתי מול
   19 זהויות הקורפוס. ‏4 פריטים שמצטטים מקורות שאינם בקורפוס (כבד אווז, שקדי עגל, שקדי טלה, מוח עגל)
   נשארו **ללא מקור ומסומנים**, ולא הומצא להם אחד.
2. **התנגשות מזהים** — ‏`CUTS` ו-`SPECIALS` ממוספרים שניהם מ-1, ‏**47 התנגשויות**. התוכנית שלי הייתה
   ממזגת פריטים בשקט. תוקן ל-`<table>:<n>` עם בדיקת רגרסיה.

### 🔴 ממצא שגדול מהשאלה ששאלת

```
texture craft-flagged: 136 of 136
מפתחות הציטוט ב-sources.py:  sv=130 · smoke=177 · safe=144 · verified=177 · grill=131 · cure=14
                              tgt = לא קיים כלל
```

**אף יעד מרקם בקטלוג אינו נושא ציטוט — לא 94°C של הבריסקט, אלא כל 136.** ‏279 הציטוטים מכסים
סו-ויד, עישון, רצפות בטיחות וגריל, **ואף אחד לא את `tgt`** — המספר הכי גלוי במוצר, זה שהמשתמש
מבשל אליו. שאלת R-79 הייתה על פריט אחד; זו תכונה של שכבה שלמה.

### 🔴 הגיליון שלך — לא היה בשימוש, וזה שינה את העיצוב

**מצאתי אותו רק ב-02:00**, אחרי שכבר אישרת ספק. `BBQ_Recipes_RTL.xlsx`, חמישה גיליונות, מיוצא כעת
לרפו ב-`docs/sources/owner-sheet/`. הממצא:

```
בריסקט · גיליון "סו-ויד + עישון" :  טיפול באמצע = "צינון מלא"    · wrap = לא · יעד 94°C
בריסקט · גיליון "עישון בלבד"     :  טיפול באמצע = "עטיפה ב-70°C" · wrap = כן · יעד 95°C
```

**‏`somid` פירושו smoke-only mid.** האפליקציה שיטחה **שני מסלולים לשורה אחת**: `mid` הוא הטיפול של מסלול
הסו-ויד, `somid` של מסלול העישון, ו-`wrap` לקח את ערך הסו-ויד — ולכן הוא "לא" ב-127 מתוך 130.
**מסלול אינו תכונה של פריט; לפריט יש מסלולים, ולכל מסלול שלבים, יעד ופעולות משלו.**

**וגם:** ‏94°C הוא יעד מסלול הסו-ויד; 95°C הוא של העישון-בלבד. ‏`data.py` שמר רק את 95.

**המשמעות:** הספק שאישרת מודל מסלול **יחיד** — והוא שגוי בנקודה הזו. סינתזה מתקנת רצה עכשיו על
שלושת המקורות יחד (הגיליון + `data.py` + הקורפוס) ותפיק ספק v2.


### 🔄 דוח אי-ההמרה — הרשימה שנוקבת כל פריט בשם

_(מתעדכן)_

---

## 5 · מה שנשאר להכרעתך

_(מתעדכן)_

---

## 6 · תשתית

**‏serena תוקן ועובד** — 39 כלים, **שרת אחד משותף** על 9121 עם לוח-מחוונים יחיד, כפי ש-§10.17a דורש.
הוא זה שמצא את R-82 בשאילתה הראשונה.

**חוב עומד:** ‏103 מסמכים מקדימים את הגרף. הריענון הלילי חוסם; לא הרצתי `/graphify docs --update` הלילה
כדי לא להתחרות על CPU עם מדידות הסוויטה ועם ריצות הבדיקה — **וזו החלטה, לא שכחה.**
