# דו"ח UX — מהדורה 278 (Phase 1 · חוסמים מיידיים)

> **סטטוס: ✅ שוחרר ואומת חי — 31.7.2026 11:0x.**
> ‏`matkonetesh.pages.dev` מציג `מהדורה 278 · 31.7.26`; ה-Worker המוקשח נפרס (`matkonet-ai`,
> גרסה `ba9214b2`). שער ה-audit ל-KV רץ לפני הפריסה והיה **נקי** (‏exit 0 — כל הרשומות נושאות
> תקרה חיובית), כנדרש בפסק הבעלים מ-31.7.
>
> **בדיקות חיות שבוצעו על האתר החי (לא על build מקומי):**
> | מה נבדק | תוצאה |
> |---|---|
> | חותם המהדורה | `מהדורה 278 · 31.7.26` ✅ |
> | גודל העמוד הראשי | **1,993,767 בתים** (~1.99MB, לעומת 7.79MB לפני) ✅ |
> | מילון על-פי-דרישה | `lang-fr.json` → 200, ‏711,406 בתים ✅ |
> | כותרת CSP | `default-src 'self'; script-src 'self' 'unsafe-inline'; …` ✅ |
> | `X-Frame-Options` | `DENY` ✅ |
> | ‏health של ה-Worker | `{"ok":true,"service":"matkonet-ai"}` — **בלי `hasKey`** (E14 נסגר) ✅ |
> | מסלול ה-streaming (B19) | `404 not_found` — הוסר בפועל ✅ |

1. **מה השתנה:** האפליקציה יורדת ~2.1MB במקום ~7.8MB — טעינה ראשונה מהירה משמעותית; החלפת שפה
   מורידה את המילון ברקע (רגע קצר של טקסט באנגלית בפעם הראשונה — צפוי); גיבוי אינו מכיל עוד את
   פרטי הגישה המרכזית; ייבוא גיבוי מסנן מפתחות לא-מוכרים ומדווח כמה דולגו; כשל ברישום עדכוני-רקע
   מוצג בהודעה במקום להיבלע; הקשחות שרת-AI וכותרות אבטחה — ללא ביטוי חזותי.
2. **איפה רואים את זה:** מסך הבית ← שורת הדגלים (החלפת שפה) · תפריט ☰ ← גיבוי ושחזור.
3. **איך בודקים ביד (390×844, עברית תחילה) — המהדורה חיה, אפשר לבדוק עכשיו:**
   - רענן את האפליקציה החיה ← בדוק ב-DevTools/Network שההורדה ~2.1MB ← החלף לצרפתית ← ודא
     שהממשק כולו בצרפתית (קטלוג=Catalogue) וש-`lang-fr.json` נטען ברשת.
   - גיבוי ושחזור ← "ייצא קובץ גיבוי" ← פתח את הקובץ ← ודא שאין בו `mk-central` ואין מפתח AI.
   - "ייבא מקובץ" עם הגיבוי שיצרת ← ודא הודעת "הנתונים שוחזרו".
4. **ללא ביטוי חזותי:** הקשחת ה-worker — מאומתת ב-`worker/test/index.spec.js` (10/10 ✅ מקומית);
   כותרות E15 — מאומתות ב-`tests/security-headers.spec.ts`; שומר ה-build B24 — assert ב-`build.py`.
5. **הגארד הקולי (H13/R-2, R-3 — Task 13):** המרקר "מאומת" נצמד עכשיו למספר הנבדק בפועל
   (לא לכל המשפט), ומסרב לסמן מספר שנלקח מהשדה הלא-נכון; זיהוי תווי-מעלות יוניקוד וגם המילה
   העברית "פרנהייט" — כדי שערך פרנהייט לא יוקרא בטעות כצלזיוס. בדיקת-יד: פאנל הקול ← בקש קריאת
   טמפרטורה עם ערך פרנהייט מפורש ← ודא שההקראה נשארת ב-°F ולא הופכת ל-°C.

## הוכחות ריצה (מקומי, 31.7.26)

- `npx playwright test` — ריצה 1: **839 עברו**, exit=0. ריצה 2: **839 עברו**, exit=0.
- `cd worker && npm test` — **10 עברו**, exit=0.
- `python build.py` — נבנה בהצלחה; `dist/index.html` = 2,278,703 בתים (~2.17MB, מתחת ל-2.6MB);
  `foot-stamp` = `מהדורה 278 · 31.7.26`.
- פריסה ואימות חי: **בוצעו** — ראו טבלת הבדיקות החיות בתיבת הסטטוס למעלה.
