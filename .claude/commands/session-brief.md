---
description: "הצגת המפה המידי — איפה אנחנו כרגע (גרסה חיה/לוח מול בסיס הלוח/הכרעות ממתינות לבעלים/קומיטים אחרונים/הצעד הבא)"
---

הרץ `node scripts/session-brief.mjs` והצג את הפלט כפי שהוא — זהו הדיגסט הנגזר מ-git ומ-`docs/STATUS-BOARD.md`/`docs/ROADMAP-2026-07-30.md`, לא סיכום מהזיכרון.

זהו כלי **אוריינטציה בלבד** (read-only, ללא כתיבה), משלים ל-`check-meta.mjs` (השער) ול-`/status` (הלוחות המלאים):
`node scripts/check-meta.mjs` בודק ציות; `node scripts/session-brief.mjs` עונה "איפה אנחנו" —
גרסה חיה מול בסיס הלוח, פאזה פעילה, החלטות הממתינות לבעלים, קומיטים אחרונים, הצעד הבא המוצהר, וספירת
חוב עומד. אם שדה כלשהו לא ניתן לביסוס מהקבצים, הוא יוצג כ-"not established" — לעולם לא מוערך.

לפירוט מלא (טבלת הפאזות, היכולות, טבלת H9) — השתמש ב-`/status`.
